# Evidence Index

| Evidence | Exact identity | What it establishes | What it does not establish |
|---|---|---|---|
| Approved Foundational Audit II | SHA-256 `0092098ce085a63006bfcd6d63f5fca7f5dc2d25b4f7b112daa1dd0d8bdeb8cc` | Owner-approved architectural/research direction | Runtime implementation |
| Prior Magnum Opus Master Plan | SHA-256 `34f027207fa4ee6478d0fbbb217e2b6c0cbf5e9bd98673242f11844b223967f6` | Earlier execution program retained except where Audit II supersedes | That all tasks are still semantically valid |
| High-assurance starting candidate | Git SHA `31577d5b112653e5aa4dff5a0bdaae9fd58a982c` | Exact source baseline for reconstruction | Green CI |
| GitHub Actions CI run | Run `33222840344` | Package smoke/root 3-OS/team Windows+Ubuntu green; team macOS failed one OIDC nbf test | That candidate is merge-ready |
| macOS failing job | Job `99020364863` | Failure occurs in `verifyIdToken: a token with nbf beyond the clock-skew allowance is rejected` | Broader OIDC failure |
| OIDC test source | `team-server/test/oidc.test.ts` at `31577d5...` | Test constructs `nbf = now + 61` before async verification | Stable >60-second separation at verifier check |
| OIDC verifier source | `team-server/src/oidc.ts` at `31577d5...` | Verifier recomputes `now` after discovery/JWKS/signature work and rejects only `nbf > now + 60` | Deterministic test clock |
| Synchronized reconstruction checkout | Git SHA `4994582ad7fd389e29820cf3f3a2902a0a967ec7` | Fresh execution clone matched the live `origin/gpt56/magnum-opus-reconstruction` tip before editing | Does not prove the later implementation commits or CI |
| Robust decision implementation | Git commit `b27c0ef` (`src/decision/engine.ts`) | Strict interval dominance, minimax regret, explicit perfect-information VOI, validation and assumptions | Does not by itself prove causal or action-grade policy fitness |
| Revocation implementation | Git commit `5647c67` (`src/epistemic/revocation.ts`) | Additive transitive closure, sibling preservation, cycle-safe traversal, duplicate-edge rejection and idempotence | Does not yet provide persisted Evidence/Claim DAG or as-of reconstruction |
| Strict fixture migration | Git commit `685b14c` | Demo/integration fixtures supply explicit lifecycle evidence; unknown gates remain unresolved | Does not close universal WorkUnit/OutcomeAdapter migration |
| Local verification at implementation checkpoint | SHA `685b14c57cccf078679a37a929f6234f00522abd`; Node/browser typecheck, 968 root tests (968 pass/0 fail/4 platform skips), build, packed artifact smoke | Source tree and installable artifact pass the available local checks | Local execution is not the cross-platform CI evidence |
| Exact-SHA GitHub Actions CI | Run `33253835881`, head `685b14c57cccf078679a37a929f6234f00522abd`, conclusion `success` | All seven configured jobs succeeded: root test Ubuntu/macOS/Windows, package-smoke, team-server Ubuntu/macOS/Windows | Does not establish main-branch merge, production deployment, external gates, or final program completion |
| Canonical Evidence implementation | Git commit `c89d95d` (`src/epistemic/evidence.ts`) | Immutable Evidence envelope with explicit provenance, coordinates, trust axes, completeness, versioning, and safe payload/hash/reference handling | Does not itself issue claims, persist a DAG, or prove source truth |
| Canonical Claim implementation | Git commit `fa36cc3` (`src/epistemic/claim.ts`) | Immutable typed Claim envelope with evidence dependencies, derivation identity, uncertainty, profile matching, and additive lifecycle metadata | Does not yet enforce Derivation/Witness legality or universal downstream migration |
| M1 focused kernel verification | SHA `fa36cc380e6883cc868e2e5b46517ae118a038ae`; 29 focused kernel checks plus full local suite 983 total (979 pass/0 fail/4 platform skips), Node/browser typecheck and build | Evidence/Claim additions preserve existing kernel and product behavior | Local checks are not cross-platform CI evidence |
| Exact-SHA GitHub Actions CI | Run `33255107581`, head `fa36cc380e6883cc868e2e5b46517ae118a038ae`, conclusion `success` | All seven configured jobs succeeded for the Claim checkpoint | Does not establish persisted DAG, main-branch merge, production deployment, external gates, or final completion |
| Derivation/Witness implementation | Git commit `c457b95` (`src/epistemic/derivation.ts`) | Immutable derivation records and witness-gated legality across coordinates, epistemic/profile axes and trust/finality | Does not yet persist derivation events or make every product issuance path use the checker |
| Immutable DAG implementation | Git commit `8928bb4` (`src/epistemic/dag.ts`) | Validated append snapshots, cycle prevention, graph projections, as-of filtering, support/cut analysis and revocation traces | Does not yet provide SQLite event persistence, full conjunction semantics or universal product migration |
| M1 dependency-graph verification | SHA `8928bb474ecb93477caa7605c6710b432653a631`; 38 focused kernel checks plus full local suite 995 total (991 pass/0 fail/4 platform skips), Node/browser typecheck and build | Derivation and DAG additions preserve existing kernel/product behavior | Local checks are not cross-platform CI evidence |
| Exact-SHA GitHub Actions CI | Run `33256214683`, head `8928bb474ecb93477caa7605c6710b432653a631`, conclusion `success` | All seven configured jobs succeeded for the DAG checkpoint | Does not establish persistence, main-branch merge, production deployment, external gates, or final completion |
| SQLite epistemic ledger implementation | Git commit `9d8d364` (`src/epistemic/ledger.ts`, `src/store/schema.ts`, `src/store/db.ts`) | Canonical Evidence/Claim/Derivation persistence, digest verification, atomic dependency writes, append-only triggers and Store same-handle integration | Does not yet provide first-class assumptions, time-filtered revocation events, or universal product migration |
| Persistence verification | Run `33257825704`, head `9d8d364013d22f37fc4c3548f57110a26a274b6a`, conclusion `success` | All seven configured jobs succeeded after schema-ownership refactor; local suite 1001 total (997 pass/0 fail/4 platform skips) | Does not establish final program completion or external gates |
| First-class Assumption implementation | Git commit `ddf17f7` (`src/epistemic/assumption.ts`, Claim/ledger integration) | Named immutable assumptions, Claim assumption IDs, evidence links and revocation propagation | Does not establish assumption quality or external validity |
| Event-time revocation replay | Git commit `820bad0` (`src/epistemic/ledger.ts`) | As-of revocation projections exclude later events while current projection remains transitive | Does not complete all as-of dimensions or product replay surfaces |
| Canonical serialization implementation | Git commit `5ab1440` (`src/epistemic/serialization.ts`) | Deterministic JSON, SHA-256 record envelopes, canonical-byte/digest verification and safe rehydration for four kernel kinds | Does not yet define the portable `.fiscuspack` bundle or independent verifier |
| Serialization verification | Run `33258932047`, head `5ab1440e7495a58dcf08083696591e7cf1747d07`, conclusion `success` | All seven configured jobs succeeded; local suite 1007 total (1003 pass/0 fail/4 platform skips), Node/browser typecheck and build pass | Does not establish product-wide kernel adoption, deployment or external gates |
| Canonical witness registry implementation | Git commit `7746584` (`src/epistemic/witness.ts`, ledger/schema/serialization integration) | Immutable, evidence-grounded Witness envelopes; append-only SQLite persistence; exact derivation-reference binding; evidence-to-witness and witness-to-claim revocation edges | Does not establish universal product issuance migration, complete event projections or external evidence |
| Witness-registry verification | Run `33260005454`, head `774658431739673183066f3c45dd9d01e84346a2`, conclusion `success` | All seven configured jobs succeeded; local suite 1011 total (1007 pass/0 fail/4 platform skips), Node/browser typecheck, build and package smoke pass | Does not establish main-branch merge, production deployment, universal product adoption or final program completion |
| Hindsight-safe replay implementation | Git commit `a7ef589` (`EpistemicLedger.replayAsOf`, `test/epistemic-replay-conformance.test.ts`) | One immutable graph-plus-revocation projection filters node availability and event-recording time independently; repeated and cross-handle replay is deterministic | Does not yet model every future lifecycle timestamp or prove universal product issuance conformance |
| Replay/conformance verification | Run `33260565547`, head `a7ef58946d941e0e035a38641f59f234b180a0b8`, conclusion `success` | All seven configured jobs succeeded for the replay checkpoint | Does not establish product-wide as-of migration or external validation |
| Billing kernel vertical implementation | Git commit `9a0254a` (`src/billing/epistemic.ts`, Store/CLI/dashboard integration) | Validated operator OpenAI exports issue exact Money-backed provider Evidence and billed Claims; mixed-basis reconciliation Claims conserve provider-minus-local residuals; CLI replay and API ClaimProfile summaries are bounded | Does not establish provider-authenticated observation evidence, full legacy Money migration, economic subledger/FX, or universal product adoption |
| Billing-kernel verification | Run `33261466500`, head `9a0254ab0766057b53569b396f81c1d7c8dd96e2`, conclusion `success` | All seven configured jobs succeeded; local suite 1018 total (1014 pass/0 fail/4 platform skips), Node/browser typecheck, build and package smoke pass | Does not establish main-branch merge, production deployment, universal product adoption or final program completion |
| Persisted reconciliation-kernel implementation | Git commit `ec8e67a` (`buildOpenAiReconciliationKernelIssuance`, Store/CLI/API integration) | Provider-observed and local-capture Evidence plus a unique mixed-basis reconciliation Claim are persisted after a recorded run; provider totals and residuals are conservation-checked and later runs cannot collide | Does not establish complete legacy Money migration, economic subledger/FX, or provider/account external gates |
| Persisted reconciliation-kernel verification | Run `33262733403`, head `ec8e67ad79211389ba0b8caa5cf40ceb3ccb2872`, conclusion `success` | All seven configured jobs succeeded; latest local suite 1023 total (1019 pass/0 fail/4 platform skips), Node/browser typecheck and build pass | Does not establish main-branch merge, production deployment, universal product adoption or final program completion |
| Local economic-event foundation candidate | Git commit `5ef8b58d5af5824b439fcf5c41abb3b424fcb4e` | Immutable typed economic events, exact Money, canonical serialization, append-only SQLite persistence, recorded-time replay and currency/basis-separated projections | Local-only; no remote SHA/CI evidence; not yet wired to legacy request/budget/allocation/value authority |
| Local economic canonicalization/replay hardening candidate | Git commit `5c8d9c6343ac5bd95abcd23faef30df38df3c075` | Strict Money/event canonicalization, resource limits, reload-time reference closure/reversal checks and SQLite-bounded replay filtering; focused and full local checks pass | Local-only; no GitHub Actions run; event-role/conservation semantics remain partial |
| Local exact decimal pricing candidate | Git commit `ad0b6a4781d26afad8df10cfe9d0dd27ec12b155` | Canonical decimal rate strings plus safe integer tokens produce exact Money component/total values and refuse numeric rates | Local-only; no canonical exact bundled/refresh manifest or live pricing-path integration yet |
| Local atomic exact request-charge candidate | Git commit `ac0efa7555b4b87f7f24319c602f2b77f2ab2b85` | Opted-in request rows and exact charge events commit atomically/idempotently on one Store handle; conflicts roll back and legacy rows remain unresolved | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; live proxy/import callers still use legacy numeric pricing |
| Local economic candidate verification | Local HEAD `ac0efa7555b4b87f7f24319c602f2b77f2ab2b85` | 1,040 root tests (1,036 pass/0 fail/4 skips), root/browser typecheck, build and isolated-cache package dry-run pass | Local execution is not remote CI or release qualification |
| Local bundled exact-rate/live issuance candidate | Git commit `3763c9e86a81c365c7fe08e1ddf4bb9a5f7edb76` | Shipped pricing card carries validated decimal companions; `computeCost` exposes exact Money; proxy/native import/demo request writers pass exact totals into atomic Store issuance; numeric-only refreshed cards remain non-exact | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; legacy read-model consumers and full economic semantics remain open |
| Local role-aware economic projection candidate | Git commit `6285bccd0f223075bc97c30f34a76db9f9377b98` | Economic balances include explicit event roles; kind/basis compatibility and exact allocation-reversal source/subject/sign/bound checks fail closed; focused and full local checks pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; correction/FX/close and legacy consumer migration remain open |
| Local normalized economic source-link candidate | Git commit `8fcd2624df2a2041fc2709c0827907e17216be34` | Foreign-key/append-only source-link table, deterministic pre-link backfill, and reload-time JSON/link equality checks close the direct-write dangling-edge gap | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; event-role/conservation, correction/FX/close and legacy consumer migration remain open |
| Local exact budget projection candidate | Git commit `05f26e6110b45a03e34d27d42890ca2535ece6fd` | Complete exact request coverage now governs daily/session/runaway budget checks via an explicit effective control basis; live-only/imported scope is preserved and unresolved legacy rows use the numeric compatibility fallback | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; allocation/value/export migration and complete economic semantics remain open |
| Local exact economic CLI inspection candidate | Git commit `e3a280fb1d1ab3874b946259e251ab8b5ffbf3c0` | Read-only CLI report exposes exact role-aware ledger balances, canonical Money strings, source bases, charge event IDs and unresolved legacy request coverage | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; API/dashboard surface and legacy consumer migration remain open |
| Local additive price-correction lineage candidate | Git commit `2b811730a1a7a56a5a2acfe6840e5af205ddf162` | `price_corrected` is an exact signed delta with typed previous/replacement Money, one local estimated/list-price source, source-time ordering, provider/billed relabelling refusal, and deterministic single-correction-per-source protection; focused correction tests plus the full 1,048-test local suite pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; correction integration into effective projections, historical FX, allocation/close semantics and legacy consumers remain open |
| Local historical exact FX lineage candidate | Git commit `11a92129733c19bdad31d90e3315304c51996db3` | `fx_translated` retains one monetary source, exact positive rational rate, source/rate identity, historical provenance, effective time, convention and explicit no-rounding policy; same-currency, cross-basis, non-terminating, ungrounded and source-time-invalid translations fail closed; 1,051-test local suite passes | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; FX consumer/effective-projection integration, allocation/close semantics and legacy consumers remain open |
| Local effective correction projection and legacy-reprice convergence candidate | Git commit `60592584621d530c4a07d6ddd7dd806082aed4e7` | Exact request charges project through a source-indexed `effective` basis that applies validated local corrections; `reprice --apply` emits the canonical correction transactionally for exact rows and aborts numeric-only exact reprices; legacy rows remain compatibility projections; 1,053-test local suite passes | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; effective projection coverage for allocation/value/export/API/dashboard and period-close semantics remain open |
| Local exact-money allocation projection candidate | Git commit `d8d260f` | Read-only allocation adapter preserves arbitrary-precision Money, partitions currency/basis identities, carries source economic event IDs, refuses non-terminating proportional shares without a quantization policy, and marks legacy request rows unresolved; focused allocation tests plus the full 1,058-test local suite pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; exact allocation persistence, value/export/API/dashboard integration and period-close semantics remain open |
| Local schema-owned exact allocation persistence candidate | Git commit `3fa9a20e2b58284f0cd9ad4730a6de5e92bde51f` | Exact allocation runs serialize canonical Money/digest records with digest-derived immutable IDs, persist per-line and unallocated source-event links under foreign keys and append-only triggers, and replay idempotently; full 1,058-test local suite passes | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; effective allocation consumers, value/export/API/dashboard migration and period-close semantics remain open |
| Local exact economic export surface candidate | Git commit `c1f7ec482f0d676c1301b3d3876551f391e2256d` | `Store.economicRequestsInRange()` and `fiscus export --economic` expose original/effective Money, bases, source/correction IDs and explicit legacy coverage; the compatibility export remains unchanged; full 1,063-test local suite passes | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; value/API/dashboard migration, refreshed-card provenance and period-close semantics remain open |
| Local exact-allocation replay/parity hardening candidate | Git commit `baee5c659aba906b2959df7f8322a1092dad1018` | Proportional pools ignore archived placeholder targets in exact and legacy paths; exact result ordering is permutation-invariant; strict envelope/row validation and recomputed identity, lineage and conservation checks fail closed; full 1,062-test local suite passes | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; value/export/API/dashboard migration, effective coverage beyond requests and period-close semantics remain open |
| Local exact economic dashboard/API projection candidate | Git commit `ccda34d` (`src/dashboard/routes.ts`, browser API contract, economic report discriminator) | Read-only `GET/HEAD /api/economic` serves the shared JSON-safe exact report, validates bounded `days` windows, rejects unsupported methods, and exposes exact request coverage and role-aware balances; full 1,064-test local suite plus root/browser typechecks and build pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; dashboard rendering, value migration and period-close semantics remain open |
| Local coding value exact-attribution candidate | Git commit `3871b6fe54b008c22cdc9e12cbe6e549181e7d6c` (`src/store/economicReadModel.ts`, `src/git/correlate.ts`, realization/reprice integration) | Store-owned request rows preserve alias/live scope and strict request/event dimensions; WorkUnits and realization rollups carry JSON-safe effective Money/source lineage with explicit partial/legacy coverage; exact snapshot lineage follows reprice corrections; full 1,070-test local suite plus root/browser typechecks and build pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; usage/cohort/frontier/team/receipt consumers, refreshed-card provenance, dashboard rendering and period-close semantics remain open |
| Local grouped value exact-read migration candidate | Git commit `5e54250083ab0c3ba7db44e4b350a1f2ab150a28` (`src/store/economicReadModel.ts`, usage/cohort/report integration) | Exact session, user, provider/model and fixed-width series groups preserve dimensions, effective Money/source lineage and unresolved legacy coverage; usage, cohort and budget advice consume them with safe numeric compatibility projections; full 1,076-test local suite plus root/browser typechecks and build pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; frontier presentation, receipts/team transport, refreshed-card provenance, dashboard rendering and period-close semantics remain open |
| Local frontier/time exact projection and Value-view disclosure candidate | Git commits `3c71c51` and `ebbc571` (`src/value/frontier.ts`, `src/value/timeReclaimed.ts`, modern Value view) | Frontier model/task cells and time-reclaimed strata carry exact effective coverage; complete amounts drive finite numeric projections; modern Value view discloses exact/partial/legacy status and unresolved counts; full 1,079-test local suite plus root/browser typechecks and build pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; signed/remote artifacts, classic-dashboard parity, refreshed-card provenance and period-close semantics remain open |
| Local exact receipt v2 protocol candidate | Git commit `443c74d` (`src/value/receipt.ts`, `src/cli/teamCmd.ts`) | Complete exact-covered units can emit a semantically validated signed v2 receipt carrying effective Money/source/correction lineage; incomplete, legacy and oversized numeric projections fall back to v1; full 1,081-test local suite plus root typecheck pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; remote team-rollup storage/protocol, trust-anchor governance, classic-dashboard parity, refreshed-card provenance and period-close semantics remain open |
| Local exact team-rollup v2 protocol/storage candidate | Git commit `2d567c28027a264fdba1bb302bafbdc4f9e944e3` (`src/team/rollup.ts`, `src/cli/teamCmd.ts`, `team-server/schema.sql`, `team-server/src/store.ts`) | Complete exact-covered project values can emit a signed v2 body with canonical effective/source lineage; v1 remains the compatibility fallback; the server validates v2 before storage and retains additive `economic_json` JSONB plus the signed raw body; root 1,084 total (1,080 pass/0 fail/4 skips), team-server 61/61, root/team typechecks, build and pack dry-run pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; live Postgres migration/execution, trust-anchor governance, classic-dashboard parity, refreshed-card provenance and period-close semantics remain open |
| Local bounded classic Value/API exact-coverage parity candidate | Git commit `16c5a15f24604b9bdff06282beeb6fb3e7111557` (`src/dashboard/web/classic.html`, `src/dashboard/web/app/core/api.ts`, `test/dashboard-value.test.ts`) | Legacy Value rendering now discloses exact/partial/legacy effective coverage for mature, usage, budget, project and team sections; missing coverage is explicit `legacy_unknown`; browser Value types include project lineage; classic parser/import-contract checks pass; root 1,086 total (1,082 pass/0 fail/4 skips) | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; universal generated dashboard/schema contracts, live Postgres execution, refreshed-card provenance and period-close semantics remain open |
| Local immutable refreshed-card provenance candidate | Git commit `47ed621c988b8f3df700e606c216645f52525a12` (`src/cost/pricing.ts`, `src/store/db.ts`, `src/cli/runCmd.ts`, dashboard/API contracts and pricing tests) | Newly accepted cards retain hash-addressed immutable provenance sidecars; card/sidecar identity and model count are validated on read; pricing cohorts, active status, CLI text and the classic health card expose source/acceptance metadata or explicit unavailable status; root 1,088 total (1,084 pass/0 fail/4 skips), team-server 61/61, root/team typechecks, build and pack dry-run pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; pre-sidecar historical cards remain unknown, live Postgres execution, universal generated contracts, trust-anchor governance, period-close semantics and publication remain open |
| Local bounded dashboard runtime type-conformance candidate | Git commit `e244115e340b8aab49b791e1ce2d699fa755c35e` (`test/dashboard-contract.test.ts`) | Seeded browser read endpoints are checked against browser-declared primitive, array, record, named-interface and nullable-union shapes; a non-vacuous wrong-primitive regression fails before the checker and passes after it; root 1,089 total (1,085 pass/0 fail/4 skips), team-server 61/61, root/team typechecks, build and pack dry-run pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; this is not a generated single-source schema, and route/CLI/docs generation, live Postgres execution, period-close semantics and publication remain open |
| Local exact economic period-close lifecycle candidate | Git commit `28905c54436680561354b1c939e50194d7cf48fa` (`src/economics/close.ts`, `src/economics/ledger.ts`, `src/cli/economicCmd.ts`, dashboard contract/tests) | Canonical half-open periods finalize an exact basis-separated event snapshot with sorted source IDs, event count and projection digest; status replays at an `asOf` boundary; append-only reopen controls preserve history; competing transitions remain conflicted; late in-period events are blocked; CLI finalize/status/reopen and read-only `/api/economic` expose JSON-safe close state; root 1,095 total (1,091 pass/0 fail/4 skips), team-server 61/61, root/team typechecks, full build and npm pack dry-run pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; universal generated dashboard/API/schema contracts, live Postgres execution, trust-anchor governance, broader kernel issuance and publication remain open |
| Local canonical/generated dashboard route-contract candidate | Git commits `a9ed216ceee2e66b3b0f118e7a530515347ba98e` + `d6243c76a41839e594c378adab8a8129d9d2566c` (`src/dashboard/contracts.ts`, generated browser copy, `src/dashboard/routes.ts`, `scripts/build.mjs`, conformance test) | One no-node descriptor drives all 19 API route paths, methods, historical Allow headers and CSRF gates; explicit browser bindings are checked across modern API/classic/action surfaces; the build regenerates the browser copy under the publication lock; route/security/runtime contract tests and build-race pass; root 1,096 total (1,092 pass/0 fail/4 skips), team-server 61/61, root/team typechecks, full build and npm pack dry-run pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; payload-field schema generation, CapabilitySpec/docs generation, live Postgres execution, trust-anchor governance, broader kernel issuance and publication remain open |
| Local bounded CapabilitySpec and shared payload-envelope contract candidate | Git commits `0467830dff8ba8635e4bd429de3534fa59986dd8` + `92ecbbdaae5fa72ef313e866c90ff967ec97f8be` (`src/dashboard/web/app/core/registry.ts`, System view, shared/generated dashboard contracts, modern API validator, conformance tests) | Every GUI capability now has immutable conservative input/preview/output schema classes, authority/egress/credential/reversibility/assurance metadata and CLI/API/GUI/docs bindings; every dashboard API method has a shared top-level JSON/text envelope, seeded runtime conformance, and modern-client fail-closed validation (typed 502 on drift); root 1,100 total (1,096 pass/0 fail/4 skips), team-server 61/61, root/team typechecks, full build and npm pack dry-run pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; nested payload schemas, full docs/claim/egress generation, unavoidable kernel issuance, live Postgres execution, trust-anchor governance and publication remain open |
| Local generated nested dashboard-interface contract candidate | Git commit `aad39bc2931a96768321f2c8eb292e0c9bc6b01f` (`scripts/generate-dashboard-payload-contract.mjs`, generated interface metadata, `scripts/build.mjs`, `test/dashboard-contract.test.ts`) | Deterministic generation flattens exported browser-interface inheritance, records the exact `api.ts` SHA-256, regenerates under the publication lock, and feeds seeded runtime conformance for every named browser interface while top-level payload contracts cover all JSON envelopes; build-race, dashboard contract, CapabilitySpec and root 1,100-test suite pass (1,096 pass/0 fail/4 skips), team-server 61/61, root/team typechecks, full build and npm pack dry-run pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; a single generated/shared type source, docs/claim/egress generation, unavoidable kernel issuance, live Postgres execution, trust-anchor governance and publication remain open |
| Local period-close-to-kernel Evidence/Claim candidate | Git commit `e2ea654d3bc04dec4b50f835cdc51a162eba8438` (`src/economics/epistemic.ts`, Store bridge, CLI finalize integration, kernel tests) | Active finalized economic periods issue one idempotent exact Evidence/Claim pair with source-event IDs, basis-separated balances, projection digest and provisional local finality; forged/reopened/conflicted snapshots are refused, CLI finalize reports kernel identities, and root 1,102 total (1,098 pass/0 fail/4 skips), team-server 61/61, root/team typechecks, full build and npm pack dry-run pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; kernel issuance at value/allocation/billing/decision/control boundaries, single generated/shared dashboard types, live Postgres execution, trust-anchor governance and publication remain open |
| Local exact-allocation-to-kernel Evidence/Claim candidate | Git commit `1428a776185ee6bef3d8cb09a18253072573ef96` (`src/alloc/epistemic.ts`, Store bridge, exact allocation tests) | Persisted exact allocation runs issue one idempotent Evidence/Claim pair keyed by digest-derived run identity; the Evidence retains canonical result/source-event lineage and currency/basis groups, while the Claim asserts provisional local allocated showback with complete/partial coverage; root 1,104 total (1,100 pass/0 fail/4 skips) and focused exact allocation kernel tests pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; allocation-specific close binding, remaining value/billing/decision/control issuance, single generated/shared dashboard types, live Postgres execution, trust-anchor governance and publication remain open |
| Local coding-realization-to-kernel Evidence/Claim candidate | Git commit `ad74ea3` (`src/value/epistemic.ts`, `src/store/db.ts`, `src/epistemic/ledger.ts`, value/repricing tests) | The canonical realization persistence path automatically and atomically issues one digest-bound `value.realization_recorded` Evidence/Claim per mature/current/all-pass unit with complete exact USD effective attribution re-derived from the Store; partial, maturing, stale, synthetic and legacy snapshots remain compatibility-only; root 1,111 total (1,107 pass/0 fail/4 skips), team-server 61/61, root/team typechecks, full build and package smoke pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; revised-value supersession/latest-as-of selection, allocation-specific close binding, remaining billing/decision/control issuance, single generated/shared dashboard types, live Postgres execution, trust-anchor governance and publication remain open |
| Local canonical dashboard shared-type source candidate | Git commit `e00f7f9` (`src/dashboard/shared-types.ts`, generated browser types/metadata, API/build/tests) | Thirty-nine named payload interfaces plus `Range` and a response map now have one no-runtime canonical source; the locked generator emits a hash-bound browser copy and nested metadata, `api.ts` no longer contains duplicate interfaces, and the server Overview builder consumes the shared map; root 1,112 total (1,108 pass/0 fail/4 skips), team-server 61/61, root/team typechecks, full build and npm pack dry-run pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; inline route responses, full docs/claim/egress generation, revised-value supersession, allocation-specific close binding, remaining billing/decision/control issuance, live Postgres execution, trust-anchor governance and publication remain open |
| Local named dashboard-response contract candidate | Git commit `cfb1fc7` (`src/dashboard/contracts.ts`, `src/dashboard/shared-types.ts`, generated browser types/metadata, API/tests) | Remaining JSON route descriptions are named shared interfaces for importers, discovery, scan setup, pricing, realization, guide, judge and proposal clearing; browser write methods consume those types, and a regression rejects inline JSON/Record response descriptions; text/CSV and opaque nested realization/report details remain bounded; root 1,113 total (1,109 pass/0 fail/4 skips), team-server 61/61, root/team typechecks, full build and npm pack dry-run pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; opaque nested detail schemas, full docs/claim/egress generation, revised-value supersession, allocation-specific close binding, remaining billing/decision/control issuance, live Postgres execution, trust-anchor governance and publication remain open |
| Local typed realization-report detail candidate | Git commit `2f1e509` (`src/dashboard/shared-types.ts`, generated browser types/metadata) | Read-only realization responses now carry named eight-gate results, maturity/currentness, waste buckets, serial realization bounds, unit lineage and exact-economic coverage instead of an opaque report record; root 1,113 total (1,109 pass/0 fail/4 skips), team-server 61/61, root/team typechecks, full build and npm pack dry-run pass | Local-only; no GitHub Actions run because push was blocked by Codex usage/approval limit; deeper Value/judge nested-field generation, full docs/claim/egress generation, revised-value supersession, allocation-specific close binding, remaining billing/decision/control issuance, live Postgres execution, trust-anchor governance and publication remain open |
| Published reconstruction checkpoint | Git commit `200b9a40fba6dd0cbe2ab2bf314e39eb214bbe32` on `origin/gpt56/magnum-opus-reconstruction`; local/remote exact match and zero divergence | The complete reconstruction history through the typed realization dashboard detail is visible on GitHub; local root 1,113 tests (1,109 pass/0 fail/4 skips), team-server 61/61, root/team typechecks, full build and npm pack dry-run were verified before publication; GitHub Actions run `33400471876` completed successfully across all seven configured jobs | Main-branch merge, production deployment, live Postgres qualification and other external gates are not established by this row |

Rows labeled “Local candidate” preserve the historical evidence state at the
time each slice was recorded. Those slices are included in the published
reconstruction checkpoint above; the labels are not a statement that the
current branch remains unpublished.

| Dossier III WP-A01–A04 checkpoint | Git commit `66320e4f81b9b2ad492ec117f4ada9b864180e5c` on `origin/gpt56/magnum-opus-reconstruction` | Coherent VoI, exact-head CI identity, completeness-gated clean, shared bounded resource/truncation semantics, intrinsic proposal/storage limits, bounded native imports/transcripts, canonical serialization guards, typed team-server 413 and contention-safe publication; local root 1,142 total (1,138 pass/0 fail/4 Windows skips), team-server 62/62, typechecks/build/pack pass | GitHub Actions run `33432485480` for this exact head concluded **failure**, read on 2026-09-01: `team-server-test` red on Ubuntu/macOS/Windows and `candidate-head` red with it, TS1294 from a team-server parameter property, which the root typecheck cannot see. The local totals above are accurate; they never established that the published head compiled. Repaired at `31911cb`, run `33433920022` success. `.fiscuspack` design/verifier and remaining program/external gates stay open |

| Dossier III WP-A05 exact model-authority checkpoint | Code commits `7678b7b` + CI portability repair `31911cb` on `origin/gpt56/magnum-opus-reconstruction` | `canonicalModelAttribution()` ranks provider/model groups from one effective request-row snapshot, compares exact Money before numeric projection, computes exact purity, withholds partial/legacy winners, preserves provider identity through realization/reprice/frontier and keeps compatibility labels display-only; local root 1,146 total (1,142 pass/0 fail/4 Windows skips), team-server 26/26 focused plus typecheck, browser/root typecheck/build/pack pass | Run `33433809771` for `7678b7b` concluded **failure** (TS1294, team-server parameter property). The corrective tip `31911cb` concluded **success** in run `33433920022` across all eight jobs, read on 2026-09-01 — so this packet is remotely green at `31911cb` and was never green at `7678b7b`. Remaining causal/value/control/external gates stay open |

| Dossier III WP-A06 checkpoint | Git commit `359e4b96771bc19c1f94b935727778238470bc29`; GitHub Actions run `33439468753` | Joint cost/quality causal decisions and publication reads are explicit; seven of eight CI jobs green | Run concluded **failure** on `package-smoke`. The cause was not in the A06 work: the launcher deleted its private runtime snapshot while a long-lived `fiscus start` was still using it. No local gate exercises a process that outlives its own completion promise |

| Launcher runtime-snapshot repair | Git commit `404a5904c2d7c88121923c626d34799b99665dd3`; GitHub Actions run `33473535818` (**success**, all eight jobs) | Snapshot cleanup moved to `process.on('exit')`; orphans reaped by owner PID liveness, never by pathname or age alone; the package-surface test derives required tarball contents from the launcher's own imports. Live probe: server serves correctly, a force-kill leaves exactly one orphan, the next CLI invocation reaps it to zero | Does not measure the ~2.8 MB `dist/` copy the launcher makes on every invocation, and does not address the pre-existing Windows `EBUSY` flake between concurrent builders on `generated-contract.ts` |

| Dossier III WP-A07 checkpoint | Git commit `3068350fd5ae2545d904e8b7b1eb1484d0fc7279`; GitHub Actions run `33474731747` (**success**, all eight jobs) | The value layer's legacy semantic overclaims are corrected and each prohibition is pinned by pattern in the `public-claims-contract` sweep: descriptive RoI composite, θ as the CES substitution parameter with σ = 1/(1−θ) stated separately, disclosed lens preferences, `instrumentationSensitivity` is not VoI, `localDataWeight` is a mixing weight, the frontier's strongest label is `observational_separation` | Does not close AII-008, AII-009, AII-025 or AII-027: retyping the four lenses, a composite decision-fitness consumer, control-path refusal of observational input, and the decision-theoretic VoI consumer are all still required. A corrected claim is not a satisfied acceptance condition |

| Dossier III WP-A08 declared-model checkpoint | Git commit `b38aee44eb0541e03d39f635d73978348736fa96`; GitHub Actions run `33475544810` (**success**, all eight jobs) | `DECLARED_REACH_UTILITY` and `DECLARED_LIFT_FLOOR_FRACTION` replace inline constants and are overridable per call; `boundedLift` reports `lowBasis`/`highBasis` so a partially identified set is distinguishable from a disclosed scenario band; the budget advisor is labelled a heuristic scenario generator; the rate-drift alarm names what it observes | The declared utilities are still declarations, not measurements: no evidence establishes that a shipped unit is worth twice a kept one. AII-010, AII-011 and AII-026 remain PARTIAL for that reason |

| Dossier III WP-A08 value/cost split | Git commit `c1f7ac5ca7d7e66a56349d4707fb5dfcb40b01cf`; GitHub Actions run `33477085151` (**failure**; see the final column) | `realizedValueUsd` split into cost-named and value-named identifiers across `src/`, the browser app, the classic dashboard and the tests, with both old names banned by a tree-walking test; verified against a live `fiscus start --demo` wire rather than against the sibling declaration | Run concluded **failure**: sixteen TS2339/TS2353 errors in `team-server/`, a third compilation domain the root typecheck cannot see, which imports `ProjectValue` directly from `src/team/rollup.ts`. The migration renamed `src/` only. The local totals recorded for this commit are accurate and were insufficient |

| Dossier III WP-A09 program-evidence reconciliation | Git commit `0497b0e184a25d7f7902952709867d7269d7d3e0`; GitHub Actions run `33478486392` (**success**, all eight jobs, read 2026-09-01) | Every run identifier cited in `docs/program/` and `docs/RELEASE-GATE.md` was read and its actual conclusion recorded; the value/cost split extended through `team-server/` including its Postgres columns; the identifier ban walks `team-server/`; `test/program-evidence-contract.test.ts` requires a stated outcome beside every cited run and forbids predicting one | The checker cannot tell whether a `PENDING` was ever revisited — that remains discipline, not enforcement. It also cannot verify that a stated outcome is the true one; it only refuses the silence that hid two failures |

| Dossier III WP-B01 claim-issuance map | Git commit `711441eb294f2055110fe771ad29bb098f7ef1d2`; GitHub Actions run `33479649350` (**success**, all eight jobs, read 2026-09-01) | All twelve claim-issuance boundaries are declared in `src/epistemic/issuance-map.ts` with their class of authority, and `test/issuance-map.test.ts` checks the declaration against the source tree: a canonical boundary that stops calling the kernel fails, a non-canonical one that starts fails, and any file under `src/` that calls `claim({...})` while absent from the map fails. Each module states its class in its own docblock. The AII-020 overclaim in the receipt docstring is corrected. The Windows `EBUSY` build-race flake is fixed at its cause | Does not close AII-036. Three boundaries — `causal.qualification`, `causal.estimate`, `decision.certificate` — are classified `unmigrated_authority`: each decides outside the kernel, and none binds its conclusion to its evidence by a Derivation, so revoking a source cannot invalidate what depends on it. The map establishes that the canonical boundaries route through the kernel, not that they are correct. Seven consecutive passes on the build race is evidence the cause was identified, not proof the race is gone |

| Dossier III WP-B02 claim-support axes | Git commit `fdc3c58a3e960cfbf3b998059cec306eb14ad258`; GitHub Actions run `33480393565` (**success**, all eight jobs, read 2026-09-01) | The GUI's `established: boolean` is replaced by `LayerSupport` on four named axes with specific call-site predicates; three visible defects fell out of the same collapse (a supported-but-unpriced Realized band reading "not established", a reconciled Billed band rendering a bare em dash, and held-but-unreconciled provider records indistinguishable from none). The browser's axis unions are drift-checked against `src/epistemic/` | Does not close AII-014. The browser derives `support` from payloads that still describe status in collapsed fields: the ClaimProfile axes reach the projection, not the wire. Persisted records are untouched |

| Dossier III WP-B03 conflict-preserving gate ladder | Git commits `4446fb907f74d035c9860be18f88af2d37aa05a7` (**success**, run `33495925527`, all eight jobs, read 2026-09-01) and `82351d9563c2215db9bb95f7d8b590aea1ec566d` (**success**, run `33496578485`, all eight jobs, read 2026-09-01) for the legacy-rehydration repair | `signalVerdict` became `signalPolarity`: independent observations aggregate into four-valued state, so a gate with a passing CI run and a failing one is `conflicted` rather than `fail`. `GateResult` carries `polarity` beside the legacy `verdict`; `verdictFromPolarity` is the only projection and never maps `conflicted` to `pass`; `FunnelOutcome.conflicts` surfaces the disagreement; terminal realization is blocked by the conflict condition independently of the projection; kernel issuance refuses a conflicted gate with its own message; the CLI renders `!` and `conflicted:<gate>` instead of `✗` and `died:<gate>`; the waste rollup buckets contradictions separately from failures; the GUI names them. Polarity and conflicts survive persistence, verified by writing a conflicted unit through `computeRealization(persist)` and reading it back from the store | Covers the coding gate ladder only. `classifySession` and the outcome contract were already conflict-preserving and were verified as such, not changed. A snapshot written before WP-B03 carries no polarity: it is normalized through `polarityFromVerdict` and reports no conflict, which is the only thing a three-valued row can say and not a finding that none occurred. No migration is possible. The required `conflicts` field crashed the waste rollup and the CLI status line on such a row until the normalization was added — found by probing the legacy path after the packet was already committed. Run `33495925527` was green with that crash present: no test read a pre-WP-B03 snapshot, so eight green jobs established that the new path works and said nothing about the old one |

| Contested completeness cannot license an absence inference | Git commit `33eb0d4da07c1fdb3d458885777471d782b34674`; GitHub Actions run `33497215172` (**success**, all eight jobs, read 2026-09-01) | `assessCompleteness` returns four-valued `state` and `conflictingWitnessIds`; a witness refuting coverage of the target's own scope and period blocks the absence inference instead of being dropped by the same line that drops an irrelevant one. Support and refutation use opposite containment tests — completeness inherits downward, incompleteness upward — and both directions are pinned, including the case that must NOT refute | Does not close AII-002. Negative claims outside the coding `clean` channels, `no off-path spend` above all, still carry no completeness requirement at all. It also does not establish that any real deployment emits refuting witnesses: the capability now exists and is correct, and nothing yet produces one |

| Conflict-lane assessment beyond coding realization | Read 2026-09-01 against the tree at `82351d9` | `classifySession` (`src/value/usage.ts`) already returns `conflicted` for a session with both positive and negative reports; `evaluateOutcomeContract` (`src/outcomes/contract.ts`) already carries `conflictedPredicates` and a `conflicted` status; billing reconciliation already models cross-observation disagreement as `snapshotStability: changed_across_observations` with the specific unstable days listed. None was changed | An assessment by reading, not by test. Only `assessCompleteness` was found collapsing a genuine conflict, and only the lanes named here were examined — the sweep looked for aggregations of several observations of one proposition and is not a proof that no other lane collapses one |

| The reconciliation residual states what it bounds | Git commit `2a83319b31ad45a2dfaccefede677c7a282d3fd5`; GitHub Actions run `33498295069` (**success**, all eight jobs, read 2026-09-01) | A `ReconciliationRun` carries `offPathBound`, derived from the two totals alone with no threshold: the residual bounds off-path spend from above only while the local estimate stays at or under the provider total, and a residual below zero refutes that condition outright rather than reading as "nothing went off-path". The CLI prints the condition beneath the number, the classic dashboard marks the no-bound case on its state line, and the qualification travels inside the issued `billing.reconciled_with_residual` Claim rather than only on the surfaces | Does not measure off-path spend and does not close AII-002: it names the condition under which the residual is an upper bound rather than establishing that the condition holds. Whether the local rate-card estimate exceeds the true on-path billed cost is still unobservable here, and the route declaration remains unverified with the provider |

| The first real completeness witness | Git commit `896c093a8cec3673b96bd25d6b20f809740d7ce0`; GitHub Actions run `33499814021` (**success**, all eight jobs, read 2026-09-01) | `revertScan` reports what it read — commits examined, the oldest one reached, and whether it stopped at its window or at the start of available history — and `revertCompletenessWitness` turns that into a `supported` witness for the `commit_reverted` channel over `[oldestExaminedMs, observedAt]`, scoped to the project. `computeRealization` emits it when the caller supplies none. Probed against this repository: the witness is emitted, it qualifies for the unit under assessment, and the `clean` gate stays `unknown` | Does not close AII-002 and deliberately does not change any gate outcome: `linked_incident` has no local source, so `clean` still cannot pass on git evidence alone. It witnesses coverage, not absence of reverts. It is a `supported` witness — the refuting path added in D-067 is still unexercised by any real source, so that guard stays correct and unexercised outside tests. A scan that read nothing returns null rather than a refuting witness, which keeps "we did not look" as unknown |

| Dossier III claim-support axes on the wire | Git commit `afca277ba08c0f98dbbee5a71352745e7820e25c`; GitHub Actions run `33502986214` (**failure**; see the final column) | All four claim routes carry a required `claimSupport` derived in `src/dashboard/claim-support.ts` from the evidence the server holds; the browser renders it and keeps only the prose and the unreachable-endpoint case. Three defects that the browser's two-branch inference could not express are fixed and pinned: contradicted provider snapshots read as established, an empty window read as complete pricing coverage, and a residual bounding nothing read as complete. `snapshotStability`, `unstableDayStartMs` and `offPathBound` are declared on the run record — all three were already on the wire and unreadable. The browser's second mirror of the kernel vocabularies is gone; `shared-types.ts` is the one remaining mirror and the drift test reads it. Verified by seeding a contradicted run into a scratch ledger and reading the compiled binary's wire, the rendered spine and the rendered Evidence card | Does not close AII-014. PERSISTED records still carry collapsed status fields and no migration exists. The wire carries four of `ClaimProfile`'s nine axes — the four the GUI reads — so integrity, authenticity, measurement, causality and finality still reach no consumer. And a `display_only` boundary that states a claim's strength as a VALUE is a new place from which a stronger semantics could be minted; it is on the issuance map for that reason, not because the risk is closed. Run `33502986214` concluded **failure**: seven of eight jobs green, `test (windows-latest)` red on `test/build-race.test.ts` with an ENOENT thrown out of `acquirePublicationLock`. The cause was not in this packet — see the repair row below — but this head was never green, and the local totals recorded for it were accurate and insufficient |

| Publication-lock contention repair | Git commit PENDING; GitHub Actions run PENDING | One condition — the canonical lock directory removed by a contender — wears three error codes on Windows, and `acquirePublicationLock` recognised one of them. `EEXIST` from `mkdir` while it is there, `EPERM` from `mkdir` while it is in pending-delete, `ENOENT` from the owner write once it is gone, and `EPERM` from the owner rename mid-publish now all mean not-acquired-yet and route to the wait loop; a transient code that outlasts `PATH_CONTENTION_MS` is still reported as itself, so a read-only parent fails fast rather than as a wait timeout. The pathname-based cleanup that ran after a failed owner publish is gone: it could take a fresh lock away from another process inside its own publish window. `test/publication-lock-race.test.ts` reproduces the CI stack trace on the unrepaired code (5 of 5 runs) and passes 20 of 20 after. Generated dashboard types now publish by same-directory rename. Root 1,210 total (1,206 pass / 0 fail / 4 skipped), team-server 62/62, all three typechecks, build and pack | Does not establish what made the directory vanish under two concurrent builders in CI — that interleaving was never observed, only its signature in the log — so this covers the RESPONSE to a lost lock, not its cause. The atomic generated-file write closes a window that produced one unreproduced full-suite failure; four paired re-runs did not reproduce it, so it is a window closed rather than a failure diagnosed. Windows remains the only platform on which any of this has been observed |
