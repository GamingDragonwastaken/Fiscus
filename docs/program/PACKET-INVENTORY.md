# Dossier packet inventory

Mechanically extracted from `FISCUS_EXECUTION_DOSSIER_III.md` — **76 packets**, not
the fourteen a previous round reported. That error came from grepping
`^## WP-[AB][0-9]{2}`, which filters to the A and B frontiers, and then reporting
the filter's output as the dossier's contents. Regenerate rather than edit by hand:

```bash
grep -cE '^## WP-[A-Z][0-9]{2}' FISCUS_EXECUTION_DOSSIER_III.md   # -> 76
```

States: `NOT_STARTED` `IN_PROGRESS` `PARTIAL` `COMPLETED` `BLOCKED_EXTERNAL`
`SUPERSEDED_WITH_REASON`. Every packet carries exactly one.

| Packet | Subject | State | Evidence / remainder |
|---|---|---|---|
| `WP-A01` | Make perfect-information VoI probabilistically coherent | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-A02` | Separate exact-head CI from synthetic merge-ref CI | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-A03` | Replace the coding `clean` closed-world inference with completeness-gated semantics | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-A04` | Bound every consequential proxy/capture buffer and make truncation epistemically visible | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-A05` | Make model attribution/ranking use one exact economic authority | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-A06` | Repair joint causal cost-quality inference | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-A07` | Correct remaining legacy RoI/frontier/reliability overclaims | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-A08` | Finish the remaining legacy value-semantic corrections | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-A09` | Reconcile program evidence after the corrective frontier | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-B01` | Universal issuance legality at claim-strengthening boundaries | `PARTIAL` | `causal.issuance` and `decision.epistemic` bind causal and strict interval-dominance claim strengthening to kernel records. Budget enforcement carries an explicit projection basis and fails closed on incomplete exact coverage (D-107). Generic negative claims now require a typed witness-reference contract at Claim construction and persistence (D-109); cited Evidence must also cover the event type, target scope, and full claim interval. Witness production and remaining high-consequence control/policy boundaries remain. |
| `WP-B02` | Remove alternate `established:boolean` and trust-score semantics | `PARTIAL` | `established:boolean` removed; the wire now carries the whole ten-axis `ClaimProfilePayload` and the spine reads a projection of it, not a second opinion (D-082). No score, no derived boolean. Persisted records still carry collapsed status fields with no migration — the AII-014 remainder. |
| `WP-B03` | Conflict-preserving adapters everywhere | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-B04` | Countermodel and assumption-fragility engine | `PARTIAL` | The engine remains applied to the reconciliation residual, which states that four of its five conditions can be closed by nothing and reports a negative residual as an ESTABLISHED broken condition rather than a small number (D-084). `FragilityAssessment.certified` now withholds certification for uncovered, live, pending, or realized countermodels, while `decisionCountermodels()` emits explicit actionable witnesses for the existing interval-certificate assumptions. Value gates, allocation runs, ten-axis profiles, minimal cut/support sets, and broader packet completion remain open. |
| `WP-B05` | Claim-relative evidence ordering instead of one global strength ladder | `PARTIAL` | `admissibility.ts` states bars as per-axis predicates and `compareForUse` returns `incomparable` rather than inventing a rank; `atLeast` on `monetaryBasis` is rejected at construction. `CLAIM_USES` replaces three disagreeing vocabularies and the dashboard now reads a reconciliation record's own exclusions instead of contradicting them (D-086). Three of the five uses have no stated bar and are recorded as unexamined rather than filled in; the persisted tuples are unmigrated and `compareForUse` has no product consumer yet. |
| `WP-C01` | End-to-end accounting-number authority audit | `PARTIAL` | Audited six money-bearing areas against AII-017 (D-087). The request, receipt and realization paths its remainder names are already migrated — `cost_usd` is a refused-if-lossy projection of an exact economic event, receipts validate the float against the exact `amountText`, and realization withholds cost/share for legacy coverage. What remains is structural: exact Money is a PARALLEL authority (`/api/economic`) beside thirteen `SUM(cost_usd)` read paths, and each migration re-implements the exact-vs-float preference locally and differently. `team-server/`, export and FX were NOT audited. |
| `WP-C02` | Complete economic event-role and basis semantics | `PARTIAL` | Adjustment kinds may no longer carry a basis no charge can hold, and aggregate negative credits/discounts cannot exceed their referenced charge (D-089). Source-link validation now refuses a `price_corrected` source that is not a local charge and an `fx_translated` source that is not a charge or prior translation, including cross-chaining and unrelated monetary roles (D-110). `price_corrected` and `fx_translated` stay basis-open by design. Also closed under this heading: a reopened period accepted an in-period event recorded BEFORE the close it thereby falsified, after which `events()`, `periodCloseStatus`, `finalizePeriod` and `reopenPeriod` all threw permanently and the append-only ledger had no way back — a durable denial of service reachable through the documented API, and with budget enforcement failing closed on an unreadable ledger, a permanent stop on provider forwarding (D-100). Event validation now refuses a monetary amount on `usage_observed`, including allocation-source attempts (D-120). Local price corrections now support a typed linear append-only predecessor chain with exact per-link deltas and read-boundary tamper validation (D-121); correction occurrence identity is now enforced while later recorded knowledge remains legal (D-128). Remaining: per-link basis agreement with the charge named in `sourceEventIds`, the role assignments themselves are unaudited, and a ledger already in the bricked state still has no recovery path. |
| `WP-C03` | FX integration and historical projection correctness | `PARTIAL` | A charge can no longer be translated into the same currency twice, which projected EUR 17 for a USD 10.00 bill through `closeBalances` (D-090). The guard lives in the closure check so a stored pair fails closed on read, and it is keyed on source + target currency so multi-currency reporting survives. Verified RED on the real `finalizePeriod` surface. Local `price_corrected` events now support a typed multi-step chain, and optional FX rate-validity intervals survive construction/persistence/replay with `effectiveAt` required to fall inside the half-open interval (D-122/D-123). Historical rate books now validate explicit interval-covered observations and supersession, select deterministic visible tips by effective time and recorded-time `asOf`, and refuse overlapping independent candidates; the explicit effective FX read model applies the selected rate to the full corrected charge while retaining raw lineage and close identity (D-124/D-125). Remaining: provider FX authority, universal export/read-model adoption beyond the bounded Store/export/dashboard/CLI paths, and complete close/allocation/team reconciliation. |
| `WP-C04` | Period close, allocation, supersession, and latest-as-of semantics | `PARTIAL` | Allocation reversals are now bounded in aggregate, not one at a time: two $8.00 reversals of a $10.00 allocation each passed the single-event bound and closed the period at `allocated -6` (D-091). Exact allocation persistence now binds every cited source to a charge root or its cited local price-correction event, recomputes the exact currency/basis total and source-ID set, refuses non-charge and fabricated sources, and revalidates the same rule on read (local 84/84 affected tests; checkpoint `110b3dc`; CI run `33854265175` concluded `success` across all eight jobs, read 2026-09-04). Checkpoint `c3e0034` additionally binds every new exact allocation run to an immutable matching finalized-close event, retains the close/finalization ID, projection digest, event count and period, refuses open/mismatched/conflicted or source-outside-snapshot saves, preserves bound history across reopen, and protects the binding side table against update/delete (local close-binding/exact-allocation tranche 18/18; root and browser TypeScript, build and diff check passed; CI run `33871721123` concluded `success` across all eight jobs, read 2026-09-04). Same defect class as D-090 — a set property checked against a single member — and the search that followed also found `test/build-race.test.ts` asserting the repository lock was absent, which measured the harness schedule rather than the build; that assertion was strengthened, not removed. The generic kernel Claim boundary now enforces typed linear supersession and deterministic `latestClaims(asOf?)` selection (D-117). Local price corrections now support a typed multi-step predecessor chain with exact per-link deltas and read-boundary tamper validation (D-121); exact-allocation persistence now walks the full validated correction chain to its charge root and requires every cited predecessor to be present (D-127, checkpoint `1a4e406`, CI run `33909224912` concluded `success` across all eight jobs, read 2026-09-04). A deterministic replay property now compares correction-before-close with the same correction after close/reopen/reclose: final close digest, event count and sorted source set agree; the first close remains immutable; ordinary as-of balances retain the $10 charge plus a separate $2 price delta, while the explicit effective projection moves from $10 to $12 (`test/economic-replay-property.test.ts`, economic glob 76/76, checkpoint `cba1535`, CI run `33876114608` concluded `success` across all eight jobs, read 2026-09-04). Optional FX rate-validity provenance is now preserved through construction, persistence and replay (D-122). Historical rate selection and corrected-charge FX replay are now bounded by D-124/D-125, including close-boundary rate visibility and preservation of the finalized close digest. The integrated checkpoint adds occurrence-domain correction legality, exact Store billing refusal for undeclared microdollar quantization, and typed value/adapter/database safeguards (D-128/D-129). Remaining: adjustments are unbounded against the charge they adjust, provider-authoritative historical-rate policy and universal mixed-currency consumer coverage remain open, and receipt/team reconciliation is incomplete. |
| `WP-C05` | Billing/FOCUS interoperability | `COMPLETED` | `src/export/focusBilling.ts` provides a pure, credential-free FOCUS v1.4-shaped compatibility projection for immutable provider billing evidence. It maps provider-billed fields, leaves EffectiveCost/AllocatedCost explicitly null and unmapped, preserves source lineage, and refuses unsupported bases/types. Focused tests cover mapping, lineage, refusal, and immutability; see DECISION-LOG D-046 and `docs/FOCUS-COMPATIBILITY.md`. This is not FOCUS conformance, invoice validation, reconciliation, or allocation. |
| `WP-C06` | Receipts and team rollups: integrity is not truth | `PARTIAL` | The team server validated each project dollar figure and never compared them, so a correctly signed rollup could publish `realizedSpendShare: 100` — $1000 of realized spend inside $10 of total spend. Containment is now enforced at ingestion (D-095), the same rule the `strata` block already applied one field over. Remaining: `src/team/rollup.ts` does not apply it, so a push is refused by the server rather than never emitted; nothing checks a rollup against the ledger it summarises; a `--project`-scoped push is now refused rather than silently replacing a developer's complete snapshot and erasing their other projects from every team total — and, because `developerCount` fell with them, hiding a colleague's project behind a k-anonymity notice (D-101); and an exact EUR amount is no longer accepted as agreeing with a field named `costUsd` (D-096). Unequal, self-chosen observation windows were summed into one figure that named no period, the same collapse `parsePeriodFilter` already refuses for the QUERY window; both dashboard endpoints now return the coverage of what they summed (D-102). Remaining: team totals carry no completeness claim on the wire, so D-101's client-side refusal is the only thing stopping a scoped rollup being read as a whole snapshot; the Postgres implementations are exercised by no test, the suite running against a fake store; the coverage describes window lengths but not their overlap; and `validateRollupBody` still does not apply the containment the server enforces. |
| `WP-D01` | Canonical domain-neutral WorkUnit / OutcomeAdapter | `PARTIAL` | A versioned domain-neutral `WorkUnit`/`OutcomeAdapter` contract now evaluates explicit non-coding outcomes and the coding gate lifecycle; coding realization consumes the adapter-produced funnel projection rather than bypassing it (D-118). Conformance tests preserve supported, refuted, unresolved and conflicted states. An allowlisted versioned adapter registry now canonicalizes and digest-verifies durable descriptors, and plugin invocation refuses unapproved capabilities, credentials and direct egress (D-129). Full Store persistence, executable plugin hosting, and CLI/API/GUI migration remain. |
| `WP-D02` | Reframe survival as artifact persistence, not quality | `PARTIAL` | Artifact persistence is now a literal, separately named Git observation with explicit survival/churn inputs; it does not assert code quality, maintainability, impact, or value. Focused artifact-persistence tests pass; full quality-model migration and product integration remain. |
| `WP-D03` | Contribution attribution engine | `PARTIAL` | Contribution evidence now records exact patch/generated lineage, hunk/AST/text similarity, bounded temporal association, confounders, and unresolved multi-source candidate sets. `WorkUnit.contributionEvidence` exposes the association-only result without feeding outcome, quality, or value gates. Broader language coverage, independent attribution validation, and packet-level integration remain. |
| `WP-D04` | Adversarial contribution benchmark corpus | `NOT_STARTED` |  |
| `WP-D05` | MeasurementModel migration and construct-laundering prohibition | `PARTIAL` | Two holes of one shape — a measurement strength granted because a field was populated rather than because it was checked. `assessMeasurementFitness` tested only the bottom rung (`validation === 'proxy_unvalidated'`), so an unrecognized value passed, and it collapsed `proxy_validated` with `validated`, telling a caller intending to assert a validated direct measurement that a validated surrogate would do. The requirement now carries `requiredValidation`, defaulting to the STRONGEST question, and the rank comes from an exported ordered `MEASUREMENT_VALIDATIONS` so the gate cannot disagree with `mergeClaimProfiles`, which already treats the identical axis as a ladder. Separately, `claim()` checks that a `measurementModelRef` was written and nothing checked it pointed anywhere, so a forged reference, a dangling one, and a real construct-matching model were indistinguishable — and the most valuable reference to forge is a true one, since Fiscus's only declared model is Git line retention, marked `artifact_persistence` and `proxy_unvalidated` by its own author. `src/measurement/registry.ts` resolves references against an immutable registry built from an explicit list, re-running every entry through `measurementModel()` (D-146). Fourteen tests, RED verified in the integrating tree. Remaining, and it is the substantive remainder: nothing is enforced at a boundary yet — `claim()` still accepts any non-null string, no production call site resolves through the registry, and no registry of Fiscus's own models is assembled anywhere. Neither function can check that a model's procedure measures the construct written on it; the wider migration of existing measurement claims onto declared models is untouched. |
| `WP-D06` | CompletenessWitness migration beyond coding clean | `PARTIAL` | Two production negative claims outside the coding `clean` gate asserted absence from detectors that could not have seen the thing. `fiscus value` printed a green `stable — no drift`, but the drift e-process guarantee is one-sided — it bounds false alarms, not missed ones — and measurement shows a TOTAL regime change (rate 0 then rate 1) does not fire it at n=10 or n=20, the range where the watch first speaks; the silent case now reads `unknown` and carries n, alpha, window and peak evidence (D-140). `fiscus ops` printed a green `all clear` on a default install where all six alert channels are structurally dark — caps are opt-in, there is no spike baseline, value is uninstrumented — so coverage is now reported per channel with the setting that would light each one (D-141). Thirteen tests, twelve behaviourally RED. Remaining: the dashboard renders its own `holding steady` sentence and its alerts surface carries no coverage; `computeAlerts` still returns a bare array for other callers; no power number is offered because the available analytic bound overstates detectability; and the other negative claims named in AII-002 (no provider charge, no duplicate, no policy violation) are untouched. The browser half is now closed too: `views/value.ts` no longer says the rate is holding steady, the `drift` payload carries `referenceDriftWouldFire` — whether the same e-process fires on a total regime change of this length, which is the measurement D-140 declined to approximate with a bound — and the overview carries `alertCoverage` beside `alerts` (D-144). Eighteen tests total. Remaining: neither surface renders the per-channel dark reasons; the reference probe is one stream shape; and the other AII-002 negative claims are untouched. |
| `WP-D07` | Surrogate bridges | `PARTIAL` | `proxy_validated` reads as "this surrogate's relationship to the construct has been checked", and measured against the pre-fix tree a `proxy_validated` model cited for its target construct was admissible with ZERO reasons, with no field anywhere holding what had been checked, against what, in which direction, or how strongly. `src/measurement/surrogate.ts` makes the relationship a record and treats it as a CEILING: it can only lower the rung a model's own author declared, never raise it. Only `empirical_association` licenses `proxy_validated`, and only when its reference measurement resolves, is itself `validated`, and targets the same construct — pre-registration fixes a metric in advance and says nothing about whether it measures the construct, which matters because that is exactly the basis `src/causal/epistemic.ts` offers for its own two `proxy_validated` claims. No bridge reaches `validated` in any of the twenty-seven basis/status/direction combinations a test enumerates. A bridge with no stated failure mode is refused at declaration. `src/measurement/CONTEXT.md` gives the module the contract it lacked (D-149). Twenty-seven tests, 24 RED against a delegating baseline re-run in this tree. **The first product boundary is now wired.** `src/causal/measurement.ts` declares the measurement model and the surrogate bridge behind the causal quality metric, both reconstructible from the committed protocol alone, and `src/causal/epistemic.ts` takes its `measurement` rung from what they earn instead of hard-coding one. Both issued causal claims move from `proxy_validated` to `proxy_unvalidated`, because a pre-registered bridge ceilings there and nothing in this repository records an empirical association between any quality metric and any quality construct. The reference the claims carry now resolves; before, it was a string synthesized at issuance that matched no registered model, so the validation the rung asserted had no record to check. The quality evidence class is recorded in the model's procedure and in a named failure mode and moves no rung, because the four admitted classes differ in how the observed value was produced and not in whether the metric measures the construct (D-153). Six further tests, RED verified twice. Remaining: `claim()` still accepts any non-null `measurementModelRef`, and `measurementRegistry` has no OTHER production caller — this is one boundary resolving its own reference, not a repository-wide registry. The bridge's three failure modes are declared and not tested for. A bridge's asserted association is checked for the SHAPE of its evidence and never for strength, sign or reproducibility. `validTime` is carried and never consulted, so an expired bridge reads as current. |
| `WP-E01` | One EstimandDefinition registry | `PARTIAL` | Added an immutable canonical randomized ITT definition registry with explicit population, assignment, contrast, outcome, horizon, and missing-data policy; supported causal outputs and kernel issuance now carry its reference, while unsupported estimands remain explicitly unregistered (D-111). Existing protocol/estimator migration and universal estimand references remain. |
| `WP-E02` | One design/estimator registry; retire duplicate causal systems | `NOT_STARTED` |  |
| `WP-E03` | ITT primary, block-aware estimation, and noncompliance | `NOT_STARTED` |  |
| `WP-E04` | Missingness, attrition, and interference | `NOT_STARTED` |  |
| `WP-E05` | Treatment identity and transportability | `NOT_STARTED` |  |
| `WP-E06` | Inference ledger and precision planning | `PARTIAL` | `estimateCausalStudy` is pure in its evidence, so it cannot know how many times it has been asked. Ten successive looks at one accumulating study — truncating whole assignment repeats at n/arm = 50…500 on the existing fixture — had the tenth cross the pre-registered decision rule the ninth refused, with disclosure text byte-identical to the first look's and no mention of the nine that preceded it; twenty acts at α=0.025 means simultaneous confidence of at most 50%. `inference-ledger.ts` now records one act per endpoint in the protocol's REGISTERED family and appends the union-bound arithmetic to the estimate's own limitations, never correcting the interval — `claimAfterMultiplicity` is a separate field that withholds. `precision.ts` prices interval width before evidence is bought, from a half-width extracted bit-identical from the estimator so the planner cannot drift from it (D-145). Nineteen tests RED first, causal suite 199/199. **The ledger is now the reporting boundary and is persisted.** `causal_inference_acts` holds the act chain, `Store.reportCausalStudy()` records the look and returns the conclusion after multiplicity beside the single-look one, and both surfaces an operator reaches — `fiscus causal inspect`/`verify` and `GET /api/causal` — go through it (D-157). Recording on a read path does not breach read-only-by-default: an act changes no routing, budget, configuration or operator data, and is an audit record of a report already made, like an egress receipt. A chain that does not verify is never extended; the claim is withheld instead, because appending would make a smaller look count look intact. No plan is registered by any surface, so the honest basis stays `recorded_acts_only` and every report says so. Ten tests, RED 0/10 against the committed tree, two of them driving the real HTTP endpoint and the packaged CLI rather than a Store method. Remaining: the count covers what passes through this boundary and nothing else — `issueCausalStudyToKernel` still calls the estimator directly, which is an issuance rather than a report, and `saveCausalAnalysis` does too on a path that cannot currently succeed for any input (a separate defect, recorded and not fixed); nothing registers a pre-registered plan, so no family-wise GUARANTEE is available from any surface; the ledger is per-study, so two studies asking the same underlying question are separate families and nothing notices; a slice is an unvalidated caller-supplied string; multiplicity is counted, not adjusted for correlation; precision states width and never the probability of reaching a decision; and `claimAfterMultiplicity` is a conservative Fiscus policy rather than a theorem. |
| `WP-E07` | Sequential/adaptive inference lane | `PARTIAL` | A standalone sequential Bernoulli lane now commits registered looks and protocol/observation/result hashes, refuses unregistered stopping, sliding/clustered/adaptive/post-selected data, and revalidates nested result semantics plus the anytime interval before use. Store/CLI/dashboard persistence and integration, cluster-aware or adaptive methods, and full packet completion remain open. |
| `WP-F01` | Robust utility and preference representation | `PARTIAL` | `buildUtilityIntervalProblem` normalizes explicit intervals and point utilities only when an uncertainty bound is declared; unjustified point utilities are refused. Preference ranges, constraints, reversibility, and policy consumers remain. |
| `WP-F02` | DecisionCertificate integration | `PARTIAL` | Strict interval-dominance certificates now persist through an Evidence-bound adapter with a decision-fitness Witness and Derivation; undetermined comparisons issue observation only. Minimax-regret, approved-policy, and action integration remain. |
| `WP-F03` | Preference robustness and value of waiting | `PARTIAL` | `preferenceRobustness()` now evaluates a finite explicitly declared admissible preference set, preserves per-preference ties, and returns only the intersection of optimal actions; disagreement is `preference_sensitive` with no forced action (D-130). Value-of-waiting/option value and product decision integration remain. |
| `WP-F04` | True VoI -> evidence-debt planner | `NOT_STARTED` |  |
| `WP-F05` | Decision Assurance Levels | `PARTIAL` | Measured counterexample: `computeFrontier` on 8/8 candidate vs 2/40 incumbent units returns `confidence: 'observational_separation'` with no confounders; its per-unit saving turned into two action intervals makes `certifyDecision` return `proven_dominant` for `switch_default_model`; and that certificate persisted `claim:decision:fitness:switch-default-model` with `decisionFitness: 'sufficient'` and `causality: 'none'` without refusing. Models were never assigned and nothing on the path asked. `src/decision/assurance.ts` derives a level from the ten-axis `ClaimProfile` of every declared input and from the certificate's own result — a caller cannot assert one, there is no field for it — and `buildDecisionKernelIssuance` now refuses when a declared consequence class requires more than the inputs reach. DAL-3 requires `causality: 'randomized'` (AII-025) and a problem with fewer than two evaluated actions is capped at the floor (AII-026). `decisionFitness` is excluded from the ladder so a claim cannot raise the level governing it; every cap is the weakest input, never an average; `authorizesAction` is permanently false (D-150). Five tests: the two refusal tests RED first, plus three guard rails that pin that a randomized, directly measured, completely covered input DOES reach DAL-3 and issue — a gate that refused everything would satisfy the RED tests while making the surface useless. **Remaining, and it is larger than the fix: AII-025 is NOT closed.** The gate refuses only when a caller declares a consequence, and a grep finds NO production caller of `issueDecisionToKernel` or `buildDecisionKernelIssuance` anywhere in `src/` — `decision.certificate` is exactly the boundary AII-036 classifies `unreached`. The refusal now exists and is checked; no surface passes through it, and `recommendBudget`, the frontier and every other advisory surface still reach an operator without it. Nothing evaluates whether a declared input set is complete, and the ladder is a Fiscus policy choice rather than a derived threshold. |
| `WP-F06` | Recommendation -> policy proposal -> approval -> action | `NOT_STARTED` |  |
| `WP-F07` | Shadow, canary, TTL, and epistemic circuit breaker | `PARTIAL` | `src/decision/control.ts` adds an immutable preview-then-commit lifecycle for shadow → simulated effect → canary → monitored expansion → full rollout and terminal rollback, with revision checks and fail-closed stale/revoked/conflicted/incomplete evidence, regime-change, measurement, harmful/unobservable outcome, completeness, and TTL guards (D-131). No persistence, authorization, or external target execution is implemented. |
| `WP-G01` | Finish CapabilitySpec and generated contracts | `PARTIAL` | Generated dashboard interface contracts now test exact canonical-interface coverage and hash-bound browser copies (D-112). Full request/response schema generation and server/browser validation parity remain. |
| `WP-G02` | Plugin contracts | `PARTIAL` | Additive plugin contracts define categories, capabilities, request/message boundaries, egress, credential handling, reversibility, and assurance metadata. No executable plugin host or universal generated contract exists yet. |
| `WP-G03` | Plugin isolation | `PARTIAL` | Additive isolation metadata and boundary validation make declared permissions and transport constraints inspectable. `runPluginProcess()` now mediates one bounded child-process/stdio exchange, scrubs the child environment, binds evidence to the active request ID, and refuses capabilities requiring unsupported OS controls. This is not runtime sandboxing: filesystem, direct-network, credential-access, CPU, memory, and descriptor hard limits remain explicitly unenforced. |
| `WP-G04` | Standards interoperability | `NOT_STARTED` |  |
| `WP-G05` | `.fiscuspack` | `PARTIAL` | Versioned manifests, canonical bytes, attachment-path validation, resource limits, and conservative verification now provide a portable bundle foundation. `signFiscusPack()` and `verifyFiscusPack()` add canonical Ed25519 signing, explicit attachment completeness, and separate integrity/authenticity/truth outcomes; embedded-key verification is integrity-only unless a caller supplies a matching trust anchor. Executable interoperability, an independent verifier, and CLI/API integration remain. |
| `WP-G06` | Independent verifier | `NOT_STARTED` |  |
| `WP-H01` | Database integrity review | `PARTIAL` | Added an adversarial append-only schema integrity regression covering tampered trigger protection (D-113). Store startup now checks configured SQLite pragmas and pre-repair full integrity/append-only trigger authority; backup source and copied-artifact inspection reuse the same integrity boundary (D-139). Full SQLite migration-interruption, corruption/fault-injection, confidentiality and recovery review remains. |
| `WP-H02` | OIDC/JOSE production-grade decision | `NOT_STARTED` |  |
| `WP-H03` | Fuzzing, mutation, and fault injection | `PARTIAL` | Deterministic high-consequence mutation support now removes selected safeguards and checks that the mutation oracle detects each removal; focused H03 tests pass. This is a bounded mutation harness, not broad fuzzing, fault injection, or coverage of every control. |
| `WP-H04` | Backup/recovery and migration assurance | `PARTIAL` | Backup manifests now expose the authoritative SQLite schema generation, reject unsupported newer generations, preserve legacy manifests that omit the field, and revalidate retained economic/historical-FX payload digests before restore. Old-schema restore/migration and no-overwrite/refusal paths are covered; epistemic payload replay and full migration-interruption coverage remain. |
| `WP-H05` | Supply-chain assurance | `PARTIAL` | The checkout was already in a good state — actions SHA-pinned, `permissions: contents: read`, `npm ci`, every lock entry on `registry.npmjs.org` with `sha512` integrity — and nothing held it there. Five counterexamples verified by mutating the real files, each passing every existing gate: a runtime `dependencies` block added with no complaint; `uses: some-org/publish-action@v1` inserted while `ci-hardening` stayed green, because its pin check was an allowlist of two action names; `typescript` repointed at `evil.example.com` with a matching fake hash, since `npm ci` verifies tarballs against the lockfile's OWN integrity field; `.codex` appended to `files`, packing 17 internal operations records because the npm allowlist overrides `.gitignore`; and `npm ci` swapped for `npm install` plus a `curl | sh` step. `scripts/check-supply-chain.mjs` now audits all five as a pure function over read inputs, driven by tests against mutated copies of the real files, and `npm run verify:supply-chain` runs it sub-second (D-142). Twelve tests. No workflow was modified, so CI topology is unchanged. Remaining: it reads declarations rather than an installed tree; a pinned sha is immutable, not trustworthy; `npm ci` still runs dependency lifecycle scripts in CI because `--ignore-scripts` would break the load-bearing `prepare` build; `prepublishOnly` still runs only the root typecheck of three; and there is no SBOM or signed provenance, both of which need a dependency or `id-token: write`. |
| `WP-H06` | Performance and scale | `NOT_STARTED` |  |
| `WP-I01` | Claim Inspector as a real kernel viewer | `NOT_STARTED` |  |
| `WP-I02` | Additional epistemic UX surfaces | `NOT_STARTED` |  |
| `WP-I03` | Progressive disclosure and parity | `NOT_STARTED` |  |
| `WP-I04` | WCAG 2.2 AA and runtime accessibility | `PARTIAL` | Four defects repaired, each RED first. The skip link — the only control in the shell that exists purely for keyboard and screen-reader users — wrote `#main` into `location.hash`, which the shell routes on and which is not in `ALL_ROUTES`, so following it moved the operator to Metered; it now moves focus without going through the router, and the test extracts the route whitelist and asserts `main` is absent from it. A drawer commit's outcome had no role and no live region, and was rendered at the instant the same signal disabled the button focus was sitting on, so the one action in the GUI that changes state was the one message nothing spoke. The Data detection run announced neither its wait nor its success. The daily-cap input — the only field where empty and `0` mean opposite things — had its consequence in a loose sibling with no `aria-describedby` (D-143). Browser typecheck, 6/6 contract, and 64/64 across the twelve other files reading the browser app. Remaining, all needing scope this packet did not have: `app.css` hides `.ledger-head` at narrow widths, so `role="table"` keeps rows of cells with no headers; the value ledger has no table semantics at all; heading order skips h1→h3 in two `notyet` blocks; the drawer's disabled commit button hides its blocking reason in a `title` a disabled control cannot expose; and the system parity table has no accessible name or header scope. No runtime accessibility was observed — the tests read source, not a DOM. |
| `WP-I05` | Data inventory, privacy, retention, and evidence consequences | `PARTIAL` | `docs/DATA-BOUNDARIES.md` described kinds of traffic, but the authorization vocabulary an operator actually writes into `egress.rules` is a purpose token and a data-class token, and neither list appeared on the page a public reader trusts instead of the source. All nine declared paths are now tabulated with data class, what may cross, and the command that reaches it — including that `provider_inference` carries prompts, source snippets and the caller's credential, and `provider_cost_observation` sends `OPENAI_ADMIN_API_KEY` from the process environment — pinned against `EGRESS_PURPOSES` and `EGRESS_DATA_CLASSES` in both directions. Five retained columns that can carry a person's own material (`requests.cwd`, `requests.user`, `git_commits.subject`, `proposals.files_json`, `scan_snapshots.repos_json`) are named individually rather than folded into 'labels'. And deleting evidence now costs something: the receipt history's absence alone previously decided genesis, so deleting it left `fiscus egress verify` reporting a valid chain with no trace a longer one had existed; a surviving checkpoint sidecar beside an absent history is now a discontinuity, since a checkpoint is only published after a receipt was appended (D-147). Nine tests, RED verified in the integrating tree. Remaining: the table is checked against the tokens the code accepts, not against observed traffic; the column list is hand-maintained, so a sixth sensitive column added later needs the test updated too; deleting history AND checkpoint together is still indistinguishable from a fresh home, which needs an off-machine anchor; and no retention period, deletion command, or subject-access path is specified anywhere. A fourth instance of the absence-as-finding class was then found and repaired in the same subsystem: `fiscus egress verify` printed a green `Receipt chain valid` over `Receipts: 0` and exited 0 on a home that had never sent anything, and `fiscus diagnostics` said `Egress OK (0 receipt(s))` — a hash chain over an empty set verifying vacuously, so the most reassuring output the command can produce appeared exactly when Fiscus knew least. `verifyEgressReceipts` now returns a state, one of four bases (`no_record`, `chain_intact`, `chain_broken`, `discontinuity`), the window the chain covers, and both halves of what the result is worth; `no_record` is yellow rather than red, because dressing an absence of evidence as a fault is the same collapse in the other direction. The positive half is real and checked rather than assumed: every declared egress path goes through one chokepoint that appends before forwarding, and a test walks `src/` and fails if any module outside `src/egress/` reaches the network directly (D-148). Nine further tests, eight RED. Remaining beyond the above: the coverage claim bounds what Fiscus recorded and not what the machine sent; the window would narrow silently under a retention policy, and no retention period, deletion command or subject-access path is specified anywhere; and the chokepoint premise is checked by a source walk a sufficiently indirect call could evade. |
| `WP-I06` | Documentation truth and reproducibility | `NOT_STARTED` |  |
| `WP-J01` | Adaptive experimentation and provenance-aware OPE | `NOT_STARTED` |  |
| `WP-J02` | Constrained online control | `NOT_STARTED` |  |
| `WP-J03` | Complexity Lab | `NOT_STARTED` |  |
| `WP-J04` | AI capital, showback, spend decomposition, opportunity gaps | `NOT_STARTED` |  |
| `WP-J05` | Current-market capability matrix | `NOT_STARTED` |  |
| `WP-J06` | Originality, substitution, and complexity-theater review | `NOT_STARTED` |  |
| `WP-J07` | Public-interest/open-source launch readiness | `NOT_STARTED` |  |
| `WP-R01` | Evidence abstract interpretation | `PARTIAL` | Two findings, the first touching the distinction the product rests on. `assessDerivationLegality` iterates `PROFILE_STRENGTH_AXES`, which cannot include `monetaryBasis` because that axis has no ladder — so the rule places no constraint on it at all. Measured: a derivation whose input carries `monetaryBasis: 'estimated'` and whose output carries `'billed'` is `allowed: true` with ZERO required witnesses, and `appendDerivationWithinTransaction` stores it, because the ledger checks every input claim against a rule that never looks at the money axis. `metered usage != provider-billed cost` collapsed inside the component that exists to prevent it. Second, structurally: nothing bounded what a whole CHAIN licenses, so a conclusion several merges downstream of its leaves was compared with its neighbours and nothing else. `src/epistemic/abstract.ts` is the abstract interpretation — domain the axis lattice, transfer function the derivation rule, soundness "the concrete result is below the abstract bound" — reusing the ordered/unordered split `admissibility.ts` already declares so `monetaryBasis` acquires no ordering. `PROFILE_STRENGTH_AXES` is now exported and READ rather than restated. Every choice narrows: no inputs is BOTTOM not TOP, an unresolved input is BOTTOM, two inputs MEET at the weaker, disagreeing bases become `mixed`, a conflicted join admits only `conflicted`. `BASIS_DERIVATIONS` is deliberately empty — nobody has declared a legitimate re-basing, which is not the same as none existing (D-151). Fourteen tests, RED verified two ways. **The money axis is now wired, and it is the first wiring rather than another mechanism.** D-151 built a function that refuses the estimated-to-billed re-basing and nothing called it, so the kernel still accepted it; D-152 added a `monetary_rebasing` witness kind and pushed the requirement into `assessDerivationLegality`, which is what `appendDerivationWithinTransaction` runs, so the unwitnessed re-basing is now refused at the boundary that persists. A witness rather than a blanket refusal, because allocation is a legitimate re-basing and making it inexpressible would push the work outside the kernel. Two moves stay free and both are weakenings: keeping the basis, and dropping to `none` or `mixed`. Six further tests, five RED. The assertion that recorded the hole was INVERTED rather than deleted, and now checks that the per-step rule and the chain abstraction agree. **Remaining: the chain half is still unwired.** `analyzeDerivationChain` is called by nothing, so chain-level bounds remain unenforced — one axis moved from the abstraction into the boundary and the rest of the abstraction did not. The witness is a declaration and not a proof: nothing checks that its evidence supports the re-basing it licenses. `BASIS_DERIVATIONS` is still empty, so neither rule knows which re-basings are sound — they agree on refusing and on nothing else. And no existing Fiscus derivation re-bases, so nothing in the product exercises the new path: this closes a hole rather than enabling a feature. It never says a proposition is true; it takes each leaf at face value and bounds only what a chain ADDS; and a witness lifts its axis to that axis's top, so the abstraction is only as tight as the witness discipline it inherits. |
| `WP-R02` | Epistemic Preservation | `PARTIAL` | Preservation helpers and tests retain claim/evidence distinctions, explicit unknown/partial/conflicted states, and non-escalating transformations across the bounded surfaces implemented in this tranche. Universal preservation review and product-wide integration remain. |
| `WP-R03` | No Granularity Laundering | `PARTIAL` | `grain.ts` now declares directed domain rollups and the direct claim boundary uses `grainIsSupportedBy`, so legitimate billing/provider-period rollups are accepted while undeclared dimension invention is refused (D-108). Scope now has the analogous `scopeIsSupportedBy` boundary guard: a claim cannot narrow, contradict, or otherwise change every cited evidence scope without a scope bridge; equal and broader aggregation remain admissible. RED-first scope tests cover narrower/disjoint refusal and broader/equal permission (D-114). Remaining: rollup/scope declarations are hand-maintained and not independently validated; dimension partition identity and scope hierarchies remain structural; derivation transformations still use their typed witness path. |
| `WP-R04` | Negative-Claim Soundness | `PARTIAL` | The reachable coding-realization negative `clean` predicate is withheld from kernel issuance when modern exact/mature/realized snapshots lack a qualifying `CompletenessWitness` for both `commit_reverted` and `linked_incident`; compatibility snapshot persistence remains intact. Generic negative Claims now refuse incomplete cited Evidence across event type, target scope, and the full claim interval (D-109). RED-first coverage proves no kernel Claim is issued and valid complete persistence remains idempotent. Reconciliation's residual/off-path bound is already typed and carried through its Claim. Remaining: other negative claims need the same boundary audit; local git source only witnesses revert coverage, so coding `clean` remains unresolved without an incident-feed witness. |
| `WP-R05` | Trust Non-Escalation | `PARTIAL` | Direct Claim persistence enforces a weakest-cited-evidence ceiling on three axes — integrity, authenticity and coverage — at the unavoidable `EpistemicLedger.appendClaim` boundary, so a caller cannot bypass it by constructing a Claim directly (D-104, `e7f2b79`). The ceiling is the WEAKEST cited evidence rather than the strongest, because every cited evidence is a prerequisite — the reading D-098 settled — so one verified invoice cannot launder any number of unverified notes. Six adversarial tests, three of which were RED first. Remaining: `monetaryBasis` is deliberately excluded because it is not a ladder and `mergeClaimProfiles` refuses to rank it; measurement, causality, finality and decision-fitness have no evidence-side counterpart to bound them against; a claim citing NO evidence has no ceiling to compare with and is unbounded here; and coverage cannot compose — two partial exports that tile a scope are refused rather than unioned, which needs a `CompletenessWitness` rather than a comparison. |
| `WP-R06` | Monetary Conservation | `PARTIAL` | Three of four sites reconciling an exact amount against its USD-named float compared magnitudes only, so an exact EUR 100.00 agreed with `costUsd: 100` and was summed into `total_cost_usd` (D-096); one shared helper now performs that reconciliation and checks the unit, which `src/value/epistemic.ts` already did. FX chains and disguised allocation reversals cannot mint money that no rate or allocation produced (D-093), adjustment bases and aggregate negative adjustments are bounded (D-089), exact allocation persistence now reconciles source-event IDs, identities, and amounts against the economic charge ledger (checkpoint `110b3dc`), and new exact allocation runs now require an immutable finalized-close binding (checkpoint `c3e0034`). The explicit effective FX read model preserves raw source/correction lineage and applies one selected exact rate without turning the projection into a raw ledger balance (D-125). Exact allocation persistence now resolves full validated multi-hop correction chains to charge roots and requires complete cited lineage (D-127). Exact Store reconciliation now consumes effective Money before compatibility floats and refuses non-USD/sub-micro exact values without a declared quantization policy (D-129). Remaining: the compatibility field still carries its unit in its name rather than on the wire, provider-authoritative rate policy and universal mixed-currency consumer coverage remain open, and nothing reconciles a receipt or rollup against the ledger it summarises. |
| `WP-R07` | Revocation Closure | `PARTIAL` | The closure itself was already correct; the three kernel-claim readers that serve `/api/billing` ignored it and returned revoked claims as `supported`/`verified` (D-094). They now apply the projection through one shared helper: the claim is still served, carries `revoked`, and its `epistemic` axis reads `unknown` while `integrity` is deliberately untouched. Two further contradictions in the same packet are closed: `minimalSupportingSets` read dependency edges as alternatives while the closure read them as prerequisites, so cut sets said an auditor must revoke both invoices to cut a claim that one revocation already cuts (D-098); and the `revocation` envelope that `Evidence` and `Claim` both carry was validated, stored and never projected, so a provider statement that said on its face it had been withdrawn read as live along with everything derived from it (D-099). Remaining: only one of the three readers is exercised end to end; these readers take no as-of boundary, so a revocation recorded today rewrites how every past read renders; `RevocationProjection` has no effective-time dimension, so a future-dated revocation cannot be represented as pending and is treated as current; an envelope's `eventId` is not checked against the event table in either direction; and neither set function has a caller outside its own module, so cut sets reach no user. |
| `WP-R08` | Decision-Certificate Soundness | `PARTIAL` | Strict interval dominance is now persisted as an Evidence-bound decision-fitness Claim with a witness and Derivation; undetermined intervals issue observation only. The adapter does not yet certify minimax-regret actions or integrate an approved action policy. |
| `WP-R09` | Minimal evidence acquisition | `NOT_STARTED` |  |
| `WP-R10` | Model pluralism | `NOT_STARTED` |  |
