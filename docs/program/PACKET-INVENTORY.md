# Dossier packet inventory

Mechanically extracted from `FISCUS_EXECUTION_DOSSIER_III.md` — **76 packets**, not
the fourteen a previous round reported. That error came from grepping
`^## WP-[AB][0-9]{2}`, which filters to the A and B frontiers, and then reporting
the filter's output as the dossier's contents. Regenerate rather than edit by hand:

```bash
grep -cE '^## WP-[A-Z][0-9]{2}' FISCUS_EXECUTION_DOSSIER_III.md   # -> 76
```

States: `NOT_STARTED` `IN_PROGRESS` `PARTIAL` `COMPLETED` `BLOCKED_EXTERNAL`
`SUPERSEDED_WITH_REASON`. Every packet carries exactly one.

Latest reconciliation (2026-09-21): the synchronized remote tip is
`481383638f1a2519891b923159301385d50c66ea` with exact-head CI run
`35641090995` green across all nine jobs. J01 now has a bounded, persisted
provenance-aware OPE foundation and remains `PARTIAL`; J02 remains an explicit
no-action/not-started gate. The remaining C01 budget-window, legacy read-model,
receipt/team reconciliation and provider-FX requirements remain as recorded in
their packet rows.

| Packet | Subject | State | Evidence / remainder |
|---|---|---|---|
| `WP-A01` | Make perfect-information VoI probabilistically coherent | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-A02` | Separate exact-head CI from synthetic merge-ref CI | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-A03` | Replace the coding `clean` closed-world inference with completeness-gated semantics | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-A04` | Bound every consequential proxy/capture buffer and make truncation epistemically visible | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-A05` | Make model attribution/ranking use one exact economic authority | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-A06` | Repair joint causal cost-quality inference | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-A07` | Correct remaining legacy RoI/frontier/reliability overclaims | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-A08` | Finish the remaining legacy value-semantic corrections | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-A09` | Reconcile program evidence after the corrective frontier | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-B01` | Universal issuance legality at claim-strengthening boundaries | `COMPLETED` | `causal.issuance` and `decision.epistemic` bind causal and strict interval-dominance claim strengthening to kernel records. Budget enforcement carries an explicit projection basis and fails closed on incomplete exact coverage (D-107). Generic negative claims now require a typed witness-reference contract at Claim construction and persistence (D-109); cited Evidence must also cover the event type, target scope, and full claim interval. Witness production and remaining high-consequence control/policy boundaries remain. **D-192 closed the boundary itself: the DIRECT append path now has a floor.** The kernel had two append boundaries guarding disjoint axis sets -- `assessDerivationLegality` demanded a witness for every upward move on the derivation path, while `assertClaimWithinItsEvidence` guarded only the axes Evidence carries a comparable field for -- so `causality`, `decisionFitness` and `finality` fell between them and the derivation registry was effectively optional. One `integrity: unknown` observation plus a claim declaring `causality: randomized` stored a randomized causal claim supported by its own declaration. Obligations are now recorded per transaction and refused immediately before COMMIT. **Remaining:** `monetaryBasis` is unguarded on the direct path (the derivation path guards it with `monetary_rebasing`, D-152), and a witness still proves nothing about its own content -- `detail` is a free string with no per-kind obligation. **D-201: witnesses now carry typed content obligations** (`src/epistemic/obligation.ts`): `causal_identification` must cite evidence the output claim cites; `measurement_validation` must cite evidence declaring the output's model; `monetary_rebasing` must declare the exact basis pair moved. Six kinds are recorded kind-only by name with the reason none has a typed referent; the record is exhaustive over the kind union so a new kind without a decision fails to compile. **Remaining:** those six kind-only obligations; the measurement obligation is containment only (Evidence records which model, never how validated). **Closed at D-226: the six kind-only obligations are kind-only by decision (no typed referent), not by omission.** |
| `WP-B02` | Remove alternate `established:boolean` and trust-score semantics | `COMPLETED` | `established:boolean` removed; the wire now carries the whole ten-axis `ClaimProfilePayload` and the spine reads a projection of it, not a second opinion (D-082). No score, no derived boolean. Persisted records still carry collapsed status fields with no migration — the AII-014 remainder. **D-194 removed the second statement of the claim from the wire.** The remainder text in this row and in AUDIT-REGISTER AII-014 had gone stale from D-082 and said the wire carried four of ten axes; it did not. The live defect was a flat three-axis copy transported BESIDE the profile which the browser believed -- every spine predicate and the Evidence headline read `claimSupport.epistemic`, never `claimSupport.profile.epistemic` -- so the projection was made on the server and shipped rather than derived at the point of render. It is now `{ profile, figure, note? }` with a named `projectRenderedAxes` in the browser. **Remaining:** persisted records still carry collapsed status fields with no migration, and no screen renders the other seven axes, so they are reachable rather than read. **Closed at D-226: all ten axes render in the Claim Inspector (D-216); persisted collapsed fields are inputs the routes no longer project from, and rewriting them would backfill provenance.** |
| `WP-B03` | Conflict-preserving adapters everywhere | `COMPLETED` | Checkpointed and remotely green at or before `896c093`; see DECISION-LOG and EVIDENCE-INDEX. |
| `WP-B04` | Countermodel and assumption-fragility engine | `COMPLETED` | The engine remains applied to the reconciliation residual, which states that four of its five conditions can be closed by nothing and reports a negative residual as an ESTABLISHED broken condition rather than a small number (D-084). `FragilityAssessment.certified` now withholds certification for uncovered, live, pending, or realized countermodels, while `decisionCountermodels()` emits explicit actionable witnesses for the existing interval-certificate assumptions. Value gates, allocation runs, ten-axis profiles, minimal cut/support sets, and broader packet completion remain open. **D-195 added the one function the dossier names that existed nowhere.** `minimalInvalidatingAssumptionSets` returns the minimal sets of declared assumptions whose joint failure removes a certification, built on the hitting-set fold already behind `minimalCutSets` rather than a second algorithm -- the two now share `minimalHittingSets`, and `minimalCutSets` passes no limit so its behaviour is unchanged. **The bound is part of the answer:** the count of minimal invalidating sets is 2^k for k disjoint pairs, so the search is capped at 256, the cap is echoed, `truncated` is reported, and `inertAssumptions` degrades to `null` when it fires -- because a capped search cannot tell "in no minimal set" from "in a set it never reached", which is this program's own defect class applied to its own new code. `emptyBecause` separates `certification_not_in_force` from `no_supports_declared` so an empty result can never read as robustness. **Remaining, and it is an owner decision rather than a fix:** `src/decision/` is an island -- verified by grep across `src/ test/ bin/ scripts/ team-server/`, nothing outside that directory imports it and `decisionCountermodels` has no call site in `src/`. Only the reconciliation domain can show a user a "why not certified?" witness today (`fiscus billing reconcile` via `src/cli/billingCmd.ts`); **the value domain emits no countermodels at all.** Wiring the decision engine to a surface is reserved (§17) and was deliberately not done. **D-220:** the countermodel engine is applied to a product decision certificate — the budget-cap decision — and the surface says which assumption set would flip it. |
| `WP-B05` | Claim-relative evidence ordering instead of one global strength ladder | `COMPLETED` | `admissibility.ts` states bars as per-axis predicates and `compareForUse` returns `incomparable` rather than inventing a rank; `atLeast` on `monetaryBasis` is rejected at construction. `CLAIM_USES` replaces three disagreeing vocabularies and the dashboard now reads a reconciliation record's own exclusions instead of contradicting them (D-086). Three of the five uses have no stated bar and are recorded as unexamined rather than filled in; the persisted tuples are unmigrated and `compareForUse` has no product consumer yet. **D-193 stated two of the three unexamined bars and gave the third a real reason.** `outcome_attribution` requires supported/conditional/complete and is admissible at `causality: 'none'` deliberately and in writing -- attribution is a claim over a named population, not about what produced the outcome. `model_recommendations` requires `proxy_validated` and `decisionFitness: sufficient`, which do not substitute for one another: dominance on a surrogate is dominance on the surrogate. `roi` stays UNSTATED because it divides a value claim by a cost claim while `admits` bars one profile and `monetaryBasis` collapses disagreement to `mixed` -- a bar admitting `mixed` admits the contamination it exists to catch. **Remaining:** the ordering requirement itself is met (no global total order exists anywhere), but `admits`/`compareForUse` are consulted by nothing outside the tests; the hand-written `excludedFrom` literals remain the operative authority on every surface. **Closed at D-228:** every surface's `excludedFrom` is `barredUses(profile, floor)` — the written floor united with what `admits` refuses the profile the kernel adapter issues; a partial-coverage reconciliation or allocation run now bars `outcome_attribution`, and the store's incomplete-run path goes through `withUnresolvedRequests`. Limitation: `compareForUse` has no production caller because no surface ranks two claims for a use. |
| `WP-C01` | End-to-end accounting-number authority audit | `COMPLETED` | Audited six money-bearing areas against AII-017 (D-087). The request, receipt and realization paths its remainder names are already migrated — `cost_usd` is a refused-if-lossy projection of an exact economic event, receipts validate the float against the exact `amountText`, and realization withholds cost/share for legacy coverage. What remains is structural: exact Money is a PARALLEL authority (`/api/economic`) beside thirteen `SUM(cost_usd)` read paths, and each migration re-implements the exact-vs-float preference locally and differently. `team-server/`, export and FX were NOT audited. **D-196 moved budget enforcement onto exact Money.** Caps are parsed once at configuration load into `Money`, every threshold and daily-headroom subtraction compare with `compareMoney`, and `exactNumber` survives for the wire payload only. **D-208 closes the final guard-input hole:** `BudgetGuard.evaluate()` refuses non-finite, non-positive or over-limit `runawayWindowSec` before any Store query, reusing `MAX_RUNAWAY_WINDOW_SEC`; focused cap coverage is 8/8. Reporting fields (`daySpendUsd`, `SpendBasis` projections) remain compatibility floats by design, so consumers must not re-derive decisions from them. **Closed at D-226: export and FX are inside the D-200 sweep corpus, team reads under D-199; reporting floats are compatibility fields by design.** |
| `WP-C02` | Complete economic event-role and basis semantics | `COMPLETED` | Adjustment kinds may no longer carry a basis no charge can hold, and aggregate negative credits/discounts cannot exceed their referenced charge (D-089). Source-link validation now refuses a `price_corrected` source that is not a local charge and an `fx_translated` source that is not a charge or prior translation, including cross-chaining and unrelated monetary roles (D-110). `price_corrected` and `fx_translated` stay basis-open by design. Also closed under this heading: a reopened period accepted an in-period event recorded BEFORE the close it thereby falsified, after which `events()`, `periodCloseStatus`, `finalizePeriod` and `reopenPeriod` all threw permanently and the append-only ledger had no way back — a durable denial of service reachable through the documented API, and with budget enforcement failing closed on an unreadable ledger, a permanent stop on provider forwarding (D-100). Event validation now refuses a monetary amount on `usage_observed`, including allocation-source attempts (D-120). Local price corrections now support a typed linear append-only predecessor chain with exact per-link deltas and read-boundary tamper validation (D-121); correction occurrence identity is now enforced while later recorded knowledge remains legal (D-128). Remaining: per-link basis agreement with the charge named in `sourceEventIds`, the role assignments themselves are unaudited, and a ledger already in the bricked state still has no recovery path. **D-197 property-tested the algebra rather than examples, and every law held.** A hand-written mulberry32 generator (zero dependencies, per-law seeds printed on failure, no `Math.random()` anywhere) drove ~4,000 generated money cases plus 294 cross-basis and cross-currency refusals, and 40-60 generated event histories of 12-17 events each touching every money-bearing role. Laws: commutativity, associativity, two-sided zero at every basis, `a - a` exactly zero in coefficient AND scale, negation involution, `compareMoney` as a translation-invariant total order, canonical normal form, and exact round-trips through `formatMoneyAmount` and JSON text. Conservation: group sums, insertion-order independence of both projection and close digest, projection as a function of recorded time, adjustment and allocation-reversal aggregate bounds under three arrival orders, and the close floor. **The result is negative and the enumeration is the point** -- so the sweep was proven able to fail: three mutants were run against copies outside the tracked tree, and each was caught with a shrunk counterexample, including the exact WP-C02 splitting defect (net billed position -7.3658). **One law is stated rather than generated and is named, not hidden:** `addMoney` aligns scales by a power of ten and `normalize` refuses a coefficient beyond `MAX_MONEY_COEFFICIENT_DIGITS`, so addition is PARTIAL near the ceiling and associativity is not a theorem there -- a real property of a resource-limited exact type, asserted directly. **`reallocation` has no kind and the absence is deliberate:** it is a composition, and `validateReferenceClosure` already forces it to be spelled as reversal-then-allocation. **Closed at D-226: the remaining sentences are properties of the money type and a deliberate absence, not work.** |
| `WP-C03` | FX integration and historical projection correctness | `COMPLETED` | A charge can no longer be translated into the same currency twice, which projected EUR 17 for a USD 10.00 bill through `closeBalances` (D-090). The guard lives in the closure check so a stored pair fails closed on read, and it is keyed on source + target currency so multi-currency reporting survives. Verified RED on the real `finalizePeriod` surface. Local `price_corrected` events now support a typed multi-step chain, and optional FX rate-validity intervals survive construction/persistence/replay with `effectiveAt` required to fall inside the half-open interval (D-122/D-123). Historical rate books now validate explicit interval-covered observations and supersession, select deterministic visible tips by effective time and recorded-time `asOf`, and refuse overlapping independent candidates; the explicit effective FX read model applies the selected rate to the full corrected charge while retaining raw lineage and close identity (D-124/D-125). Remaining: provider FX authority, universal export/read-model adoption beyond the bounded Store/export/dashboard/CLI paths, and complete close/allocation/team reconciliation. **D-200:** the effective-FX read model was already adopted by every surface that can name a target currency; what was missing was the enumeration. 169 monetary read sites across 29 files are now classified by a test that derives its corpus from source and fails on an unclassified newcomer. Three unit-projection defects repaired by refusing. **Remaining:** provider FX authority (product policy, deliberately undecided; five sites enumerated). **Closed at D-237: all repository work is done and gated; provider-authoritative FX precedence is an owner product policy, deliberately undecided, with its five expression sites enumerated in `test/economic-fx-consumer-sweep.test.ts` and a self-naming refusal at each.** |
| `WP-C04` | Period close, allocation, supersession, and latest-as-of semantics | `COMPLETED` | Allocation reversals are now bounded in aggregate, not one at a time: two $8.00 reversals of a $10.00 allocation each passed the single-event bound and closed the period at `allocated -6` (D-091). Exact allocation persistence now binds every cited source to a charge root or its cited local price-correction event, recomputes the exact currency/basis total and source-ID set, refuses non-charge and fabricated sources, and revalidates the same rule on read (local 84/84 affected tests; checkpoint `110b3dc`; CI run `33854265175` concluded `success` across all eight jobs, read 2026-09-04). Checkpoint `c3e0034` additionally binds every new exact allocation run to an immutable matching finalized-close event, retains the close/finalization ID, projection digest, event count and period, refuses open/mismatched/conflicted or source-outside-snapshot saves, preserves bound history across reopen, and protects the binding side table against update/delete (local close-binding/exact-allocation tranche 18/18; root and browser TypeScript, build and diff check passed; CI run `33871721123` concluded `success` across all eight jobs, read 2026-09-04). Same defect class as D-090 — a set property checked against a single member — and the search that followed also found `test/build-race.test.ts` asserting the repository lock was absent, which measured the harness schedule rather than the build; that assertion was strengthened, not removed. The generic kernel Claim boundary now enforces typed linear supersession and deterministic `latestClaims(asOf?)` selection (D-117). Local price corrections now support a typed multi-step predecessor chain with exact per-link deltas and read-boundary tamper validation (D-121); exact-allocation persistence now walks the full validated correction chain to its charge root and requires every cited predecessor to be present (D-127, checkpoint `1a4e406`, CI run `33909224912` concluded `success` across all eight jobs, read 2026-09-04). A deterministic replay property now compares correction-before-close with the same correction after close/reopen/reclose: final close digest, event count and sorted source set agree; the first close remains immutable; ordinary as-of balances retain the $10 charge plus a separate $2 price delta, while the explicit effective projection moves from $10 to $12 (`test/economic-replay-property.test.ts`, economic glob 76/76, checkpoint `cba1535`, CI run `33876114608` concluded `success` across all eight jobs, read 2026-09-04). Optional FX rate-validity provenance is now preserved through construction, persistence and replay (D-122). Historical rate selection and corrected-charge FX replay are now bounded by D-124/D-125, including close-boundary rate visibility and preservation of the finalized close digest. The integrated checkpoint adds occurrence-domain correction legality, exact Store billing refusal for undeclared microdollar quantization, and typed value/adapter/database safeguards (D-128/D-129). Remaining: adjustments are unbounded against the charge they adjust, provider-authoritative historical-rate policy and universal mixed-currency consumer coverage remain open, and receipt/team reconciliation is incomplete. **D-200 verified the adjustment remainder is NOT stale** and made it precise: sourceless adjustments are ungated (three `write_off -8.00` project billed -24 against a $10 charge into an immutable close) and the bound compares against the raw rather than corrected charge. Both are in flight as their own packet under the conservation invariant. Sourceless adjustments are refused at construction and on read, and the credit bound compares against the charge as corrected, in either arrival order (D-206, `d4ec82e`); the two conservation laws that generate those shapes are in `test/economic-conservation-laws.test.ts`. Remaining: positive adjustments have no upper law; provider FX authority undecided. **Closed at D-226: a positive adjustment is bounded by nothing because a true-up may exceed its charge; provider FX authority is tracked on WP-C03.** |
| `WP-C05` | Billing/FOCUS interoperability | `COMPLETED` | `src/export/focusBilling.ts` provides a pure, credential-free FOCUS v1.4-shaped compatibility projection for immutable provider billing evidence. It maps provider-billed fields, leaves EffectiveCost/AllocatedCost explicitly null and unmapped, preserves source lineage, and refuses unsupported bases/types. Focused tests cover mapping, lineage, refusal, and immutability; see DECISION-LOG D-046 and `docs/FOCUS-COMPATIBILITY.md`. This is not FOCUS conformance, invoice validation, reconciliation, or allocation. |
| `WP-C06` | Receipts and team rollups: integrity is not truth | `COMPLETED` | The team server validated each project dollar figure and never compared them, so a correctly signed rollup could publish `realizedSpendShare: 100` — $1000 of realized spend inside $10 of total spend. Containment is now enforced at ingestion (D-095), the same rule the `strata` block already applied one field over. Remaining: `src/team/rollup.ts` does not apply it, so a push is refused by the server rather than never emitted; nothing checks a rollup against the ledger it summarises; a `--project`-scoped push is now refused rather than silently replacing a developer's complete snapshot and erasing their other projects from every team total — and, because `developerCount` fell with them, hiding a colleague's project behind a k-anonymity notice (D-101); and an exact EUR amount is no longer accepted as agreeing with a field named `costUsd` (D-096). Unequal, self-chosen observation windows were summed into one figure that named no period, the same collapse `parsePeriodFilter` already refuses for the QUERY window; both dashboard endpoints now return the coverage of what they summed (D-102). **The last two of those are now closed.** `buildRollupBody` and `buildEconomicRollupBody` run `validateRollupBody` before returning and throw rather than hand back a body the receiver will answer 400 on (D-162) -- at minting rather than at the push call site, so a new caller cannot re-inherit the hole, and so no signature is ever produced over a payload whose own containment fails; a signature is a commitment, and retracting one on a remote 400 is worse than never making it. Two existing tests changed construction route without changing an assertion: the v1 validator test builds its candidate as a literal, and team-server's hostile-shape test mutates projects after building and signs the result, which models a client that never ran our builder at all. Coverage now also states POSITION and not only length (D-164): two rollups both declaring thirty days, one in January and one in June, reported `spanning 2026-01-01 to 2026-06-30` while the length comparison found nothing to say -- six months named, sixty days observed. `overlap`, `overlapFrom` and `overlapTo` give the intersection of every contributing window, an intersection rather than a pairwise check because a total is summed across all contributors at once, and half-open so windows that merely touch share nothing. The span fields are untouched: the union was never wrong, it was being read as coverage. Remaining: team totals still carry no completeness claim on the wire, so D-101's client-side refusal is the only thing stopping a scoped rollup being read as a whole snapshot; nothing checks a rollup against the ledger it summarises; and the Postgres implementations are exercised by no test, the suite running against a fake store, because this environment has no live Postgres and faking one would be inventing the coverage. **D-199:** scope claim travels on the payload with an `unknown` sentinel; containment enforced at emit against the unfiltered ledger population; coverage derived from the retention floor. **Remaining:** receipt-vs-ledger reconciliation (WP-R06's other half); Postgres paths untested (no live Postgres — standing judgement). **Closed at D-231: receipt-vs-ledger reconciliation exists (`fiscus receipt --reconcile`); rollups are checked at emit (D-199) and not re-read on receipt; Postgres paths remain untested without a live Postgres, a standing judgement rather than a remainder.** |
| `WP-D01` | Canonical domain-neutral WorkUnit / OutcomeAdapter | `COMPLETED` | A versioned domain-neutral `WorkUnit`/`OutcomeAdapter` contract now evaluates explicit non-coding outcomes and the coding gate lifecycle; coding realization consumes the adapter-produced funnel projection rather than bypassing it (D-118). Conformance tests preserve supported, refuted, unresolved and conflicted states. An allowlisted versioned adapter registry now canonicalizes and digest-verifies durable descriptors, and plugin invocation refuses unapproved capabilities, credentials and direct egress (D-129). Full Store persistence, executable plugin hosting, and CLI/API/GUI migration remain. **Closed at D-247: both product paths reach terminal outcomes only through the two canonical adapters (gated), the coding evaluation carries its adapter identity, and the durable registry refuses non-allowlisted identities; executable-adapter persistence stays refused (D-129) and hosting is D-234.** |
| `WP-D02` | Reframe survival as artifact persistence, not quality | `COMPLETED` | Artifact persistence is now a literal, separately named Git observation with explicit survival/churn inputs; it does not assert code quality, maintainability, impact, or value. Focused artifact-persistence tests pass; full quality-model migration and product integration remain. **Closed at D-235: no quality-score vocabulary survives in `src/`; `fiscus value` is the product surface and names the lens as artifact persistence, not quality.** |
| `WP-D03` | Contribution attribution engine | `COMPLETED` | Contribution evidence now records exact patch/generated lineage, hunk/AST/text similarity, bounded temporal association, confounders, and unresolved multi-source candidate sets. `WorkUnit.contributionEvidence` exposes the association-only result without feeding outcome, quality, or value gates. Broader language coverage, independent attribution validation, and packet-level integration remain. **Closed at D-246: the association tally reaches the operator in `fiscus realize` with its non-claims printed beside it; language coverage is a collector concern by design; independent validation is WP-D04.** |
| `WP-D04` | Adversarial contribution benchmark corpus | `COMPLETED` | `scripts/contribution-benchmark.mjs` and its 42-case synthetic corpus exercise renamed/moved files, formatting, generated lineage, partial retention, semantic rewrites, copied boilerplate, concurrent edits, multiple sources, revert/reapply, vendored/unsupported and binary artifacts. The harness measures false-attribution pressure and unresolved outcomes rather than treating a baseline as truth; it is deterministic, deeply immutable, machine-readable, and rejects a deliberately wrong evaluator. Focused RED-first suite is 7/7 at canonical commit `9519aaa`. **Limit:** synthetic oracle evidence is not independent field attribution validation and remains association-only; it does not establish authorship, quality, success, or realized value.** |
| `WP-D05` | MeasurementModel migration and construct-laundering prohibition | `COMPLETED` | Two holes of one shape — a measurement strength granted because a field was populated rather than because it was checked. `assessMeasurementFitness` tested only the bottom rung (`validation === 'proxy_unvalidated'`), so an unrecognized value passed, and it collapsed `proxy_validated` with `validated`, telling a caller intending to assert a validated direct measurement that a validated surrogate would do. The requirement now carries `requiredValidation`, defaulting to the STRONGEST question, and the rank comes from an exported ordered `MEASUREMENT_VALIDATIONS` so the gate cannot disagree with `mergeClaimProfiles`, which already treats the identical axis as a ladder. Separately, `claim()` checks that a `measurementModelRef` was written and nothing checked it pointed anywhere, so a forged reference, a dangling one, and a real construct-matching model were indistinguishable — and the most valuable reference to forge is a true one, since Fiscus's only declared model is Git line retention, marked `artifact_persistence` and `proxy_unvalidated` by its own author. `src/measurement/registry.ts` resolves references against an immutable registry built from an explicit list, re-running every entry through `measurementModel()` (D-146). Fourteen tests, RED verified in the integrating tree. A boundary now enforces something, though not resolution: `assertClaimWithinItsEvidence` refuses a claim whose `measurementModelRef` is not declared by at least one cited Evidence, so a measurement citation can no longer appear at the claim layer out of nothing, and the causal outcome record — which cited the protocol's quality model while carrying `null` itself — now declares the model it was collected under (D-168). Eight tests, RED re-verified at 3 of 8. That is a containment check and explicitly not a fourth profile ceiling; a model reference is an identity rather than a rung, so the deliberate refusal to rank `monetaryBasis` is untouched. Remaining, and it is still the substantive remainder: RESOLUTION is not enforced anywhere — the evidence can name a model that does not exist just as the claim could, `claim()` still accepts any non-null string, and no registry of Fiscus's own models is assembled anywhere. That last one turns out to be harder than the frontier assumed: the only reference the product writes names a model synthesized from a stored protocol, so a static list cannot hold it and a resolver would put the kernel's pure constructor behind a database. `Evidence` also records a model reference but no validation strength, so `profile.measurement` has no evidence-side ceiling the way integrity, authenticity and coverage do. Neither function can check that a model's procedure measures the construct written on it; the wider migration of existing measurement claims onto declared models is untouched. **D-224:** a direct claim's measurement model must RESOLVE — registered, target construct = proposition predicate, validation ≥ asserted rung — not merely be written; a ledger without a registry refuses every direct claim above the bottom rung. Limitation: `Evidence` records which model, never how validated. |
| `WP-D06` | CompletenessWitness migration beyond coding clean | `COMPLETED` | Two production negative claims outside the coding `clean` gate asserted absence from detectors that could not have seen the thing. `fiscus value` printed a green `stable — no drift`, but the drift e-process guarantee is one-sided — it bounds false alarms, not missed ones — and measurement shows a TOTAL regime change (rate 0 then rate 1) does not fire it at n=10 or n=20, the range where the watch first speaks; the silent case now reads `unknown` and carries n, alpha, window and peak evidence (D-140). `fiscus ops` printed a green `all clear` on a default install where all six alert channels are structurally dark — caps are opt-in, there is no spike baseline, value is uninstrumented — so coverage is now reported per channel with the setting that would light each one (D-141). Thirteen tests, twelve behaviourally RED. Remaining: the dashboard renders its own `holding steady` sentence and its alerts surface carries no coverage; `computeAlerts` still returns a bare array for other callers; no power number is offered because the available analytic bound overstates detectability; and the other negative claims named in AII-002 (no provider charge, no duplicate, no policy violation) are untouched. The browser half is now closed too: `views/value.ts` no longer says the rate is holding steady, the `drift` payload carries `referenceDriftWouldFire` — whether the same e-process fires on a total regime change of this length, which is the measurement D-140 declined to approximate with a bound — and the overview carries `alertCoverage` beside `alerts` (D-144). Eighteen tests total. Remaining: neither surface renders the per-channel dark reasons; the reference probe is one stream shape; and the other AII-002 negative claims are untouched. `fiscus today` now prints the same per-channel dark reasons `ops` does, text and `--json` (D-204, `0fcfc3d`). Remaining: the dashboard renders no per-channel dark reasons; the other AII-002 negative claims are untouched. **Closed at D-226: both dashboards render per-channel dark reasons (D-218); the other AII-002 negative claims are WP-R04's remainder.** |
| `WP-D07` | Surrogate bridges | `COMPLETED` | `proxy_validated` reads as "this surrogate's relationship to the construct has been checked", and measured against the pre-fix tree a `proxy_validated` model cited for its target construct was admissible with ZERO reasons, with no field anywhere holding what had been checked, against what, in which direction, or how strongly. `src/measurement/surrogate.ts` makes the relationship a record and treats it as a CEILING: it can only lower the rung a model's own author declared, never raise it. Only `empirical_association` licenses `proxy_validated`, and only when its reference measurement resolves, is itself `validated`, and targets the same construct — pre-registration fixes a metric in advance and says nothing about whether it measures the construct, which matters because that is exactly the basis `src/causal/epistemic.ts` offers for its own two `proxy_validated` claims. No bridge reaches `validated` in any of the twenty-seven basis/status/direction combinations a test enumerates. A bridge with no stated failure mode is refused at declaration. `src/measurement/CONTEXT.md` gives the module the contract it lacked (D-149). Twenty-seven tests, 24 RED against a delegating baseline re-run in this tree. **The first product boundary is now wired.** `src/causal/measurement.ts` declares the measurement model and the surrogate bridge behind the causal quality metric, both reconstructible from the committed protocol alone, and `src/causal/epistemic.ts` takes its `measurement` rung from what they earn instead of hard-coding one. Both issued causal claims move from `proxy_validated` to `proxy_unvalidated`, because a pre-registered bridge ceilings there and nothing in this repository records an empirical association between any quality metric and any quality construct. The reference the claims carry now resolves; before, it was a string synthesized at issuance that matched no registered model, so the validation the rung asserted had no record to check. The quality evidence class is recorded in the model's procedure and in a named failure mode and moves no rung, because the four admitted classes differ in how the observed value was produced and not in whether the metric measures the construct (D-153). Six further tests, RED verified twice. Remaining: `claim()` still accepts any non-null `measurementModelRef`, and `measurementRegistry` has no OTHER production caller — this is one boundary resolving its own reference, not a repository-wide registry. The bridge's three failure modes are declared and not tested for. A bridge's asserted association is checked for the SHAPE of its evidence and never for strength, sign or reproducibility. **The dead `validTime` field is now read (D-159).** Both the model and the bridge declared an optional validity window, `grep` found no reader anywhere in the module, and a bridge whose window closed in 2020 licensed exactly the same rung as one declared today. `BridgedMeasurementRequest` takes an optional `asOf` and three windows are checked against it -- the model's, the bridge's, and the empirical reference measurement's -- each lowering on its own and naming itself. A time-bounded citation asked with NO instant ceilings at `proxy_unvalidated` rather than assuming now, because the permissive reading is how the field became decorative; declaring no window at all is a different claim (unbounded by declaration) and is unaffected, which is why nothing existing churns. `interval()` now canonicalises the window at construction, so one ending before it begins is refused. Seven tests, RED 5/7, the two passes being the controls. Remaining: no caller supplies `asOf` yet, so the withholding branch is reached only by tests, and nothing decides which instant a given surface should ask about. **D-224:** the surrogate's registered validation is now what a direct claim resolves against. **Closed at D-227:** the ledger's direct floor passes the claim's own `time.asOf` to the model window (an expired window, or a windowed model asked by a claim naming no instant, is refused), and the causal issuer passes its issuance instant to the bridge window; `windowReason` is one function in the registry. Limitation: a claim's `validTime` interval is not itself checked against the window, only its `asOf`. |
| `WP-E01` | One EstimandDefinition registry | `COMPLETED` | The canonical randomized ITT registry now names population, treatment/control and treatment versions, outcome construct and measurement model, time horizon, assignment mechanism, target, interference and missingness assumptions, transport target, identification assumptions, and estimator version. Valid v1 and v2 estimator/issuance paths resolve the same immutable registry definition; unsupported or invalid protocols remain unresolved. RED-first registry completeness test plus causal core/issuance suites 60/60, root typecheck/browser/team typechecks, build, and serial full suite 2,097 total / 2,093 pass / 0 fail / 4 skips at local commit `8659b4b` plus the packet commit. |
| `WP-E02` | One design/estimator registry; retire duplicate causal systems | `COMPLETED` | `src/causal/registry.ts` is the single executable design/estimator authority: retained blocked ITT v1 resolves to `blocked_equal_itt_hoeffding_v1`, v2 is explicitly deferred, and the archived paired-return and standalone sequential lanes are named without causal estimands or issuance authority. Estimates and kernel issuance require the same registry identity; legacy snapshots project `null` without rewriting bytes. RED-first registry/issuance/snapshot tests pass 4/4; typecheck/build pass. **Limit:** this closes identity and routing, not implementation of the deferred v2 analysis or archived/noncausal lanes.** |
| `WP-E03` | ITT primary, block-aware estimation, and noncompliance | `COMPLETED` | ITT remains the primary assigned-arm estimand; execution deviations with verifiable actual plans stay in their assigned arm and are disclosed, while per-protocol/CACE/LATE effects are explicitly not estimated. Qualification refuses incomplete or conflicting blocks, and the estimator uses assignment-count-weighted within-block contrasts with conservative bounded-outcome radii. RED-first causal-core coverage includes noncompliance, incomplete/conflicting blocks, permutation invariance, and adversarial evidence cases; the causal core/ledger/issuance tranche is green. **Limit:** this is a bounded local ITT estimator and does not establish enrollment coverage or interference absence.** |
| `WP-E04` | Missingness, attrition, and interference | `COMPLETED` | `src/causal/design.ts` adds a versioned protocol-hash-linked design declaration for target population, missingness indicators/reasons/mechanism, attrition-sensitivity range, cluster/interference assumptions, exposure mappings and estimand requirements. `assessCausalDesign()` withholds cluster/exposure estimands without the required cluster source and mapping, preserves unknown missingness, and labels no-interference as an assumption rather than measured absence. `fiscus causal design --options <file> --json` is a bounded review-only consumer; focused RED-first coverage 6/6, root typecheck/build green. It does not establish observed missingness, real-world interference absence, exposure validity or causal effect; those remain external study evidence gates. |
| `WP-E05` | Treatment identity and transportability | `COMPLETED` | `src/causal/transport.ts` adds explicit protocol-linked transport declarations and conservative pooling assessment: source/target population, treatment, measurement and time coordinates are named; target evidence and consistency/exchangeability/positivity assumptions are required; changed coordinates are surfaced; and cross-study pooling is refused unless all estimand coordinates match. `fiscus causal transport --options <file> --json` is a bounded review-only consumer with focused RED-first coverage 6/6, root typecheck/build green. The existing `causal_transport` witness remains the kernel issuance obligation. No external transport validity, causal effect, exchangeability, or business-value claim is implied. |
| `WP-E06` | Inference ledger and precision planning | `PARTIAL` | `estimateCausalStudy` is pure in its evidence, so it cannot know how many times it has been asked. Ten successive looks at one accumulating study — truncating whole assignment repeats at n/arm = 50…500 on the existing fixture — had the tenth cross the pre-registered decision rule the ninth refused, with disclosure text byte-identical to the first look's and no mention of the nine that preceded it; twenty acts at α=0.025 means simultaneous confidence of at most 50%. `inference-ledger.ts` now records one act per endpoint in the protocol's REGISTERED family and appends the union-bound arithmetic to the estimate's own limitations, never correcting the interval — `claimAfterMultiplicity` is a separate field that withholds. `precision.ts` prices interval width before evidence is bought, from a half-width extracted bit-identical from the estimator so the planner cannot drift from it (D-145). Nineteen tests RED first, causal suite 199/199. **The ledger is now the reporting boundary and is persisted.** `causal_inference_acts` holds the act chain, `Store.reportCausalStudy()` records the look and returns the conclusion after multiplicity beside the single-look one, and both surfaces an operator reaches — `fiscus causal inspect`/`verify` and `GET /api/causal` — go through it (D-157). Recording on a read path does not breach read-only-by-default: an act changes no routing, budget, configuration or operator data, and is an audit record of a report already made, like an egress receipt. A chain that does not verify is never extended; the claim is withheld instead, because appending would make a smaller look count look intact. **D-260 adds a Store-owned pre-registration boundary:** `causal_inference_plans` persists one immutable plan per study before the first act, rejects invalid/unknown studies and post-act registration, survives reopening, and loads into the ledger so reports can honestly use `pre_registered_plan` rather than defaulting to `recorded_acts_only`. Ten tests, RED 0/10 against the committed tree, two of them driving the real HTTP endpoint and the packaged CLI rather than a Store method. Remaining: the count covers what passes through this boundary and nothing else — `issueCausalStudyToKernel` still calls the estimator directly, which is an issuance rather than a report, and `saveCausalAnalysis` does too on a path that cannot currently succeed for any input (a separate defect, recorded and not fixed); plans can now be registered through the Store, but no operator-facing setup surface creates them; the ledger is per-study, so two studies asking the same underlying question are separate families and nothing notices; multiplicity is counted, not adjusted for correlation; precision states width and never the probability of reaching a decision; and `claimAfterMultiplicity` is a conservative Fiscus policy rather than a theorem. **D-254: kernel issuance now shares this ledger — `issueCausalStudyToKernel` records its own look and withholds the causal escalation (identification/effect/derivation) when multiplicity would withhold the conclusion, so the durable kernel path can no longer read an exhausted family as a fresh look.** **The withheld guarantee named the wrong reason (D-160).** `actsExceedPlan` is the union of three conditions and emitted one message for all three, so one act on an unregistered slice against a plan of eight reported that the recorded acts had EXCEEDED the plan -- nothing was exceeded, and the real reason was named nowhere. The arithmetic was already right and the test file records that a hole was looked for and not found; the defect was the description. Each condition now has its own message naming the counts or the slices, and a plan that no longer bounds the family no longer opens with `Basis:` directly beneath `Basis: recorded acts only`. `recordInferentialActs` also refuses an act with no slice identity, which `validatePlan` already refused inside a plan and which `actKey` treats as part of an act's identity. Nine tests, RED 4/9, the five passes being controls. **The dashboard's own study list is no longer silent either (D-165).** `causalStudySummaries()` filters version-2 protocols out, deliberately, and did so invisibly: with one version-1 study beside two version-2 ones the response listed a single row and reported the omission nowhere, so a list of one could not be told from a store holding one study. `studiesOmitted` now carries the count and reason on both branches of the endpoint. Each row also carries `analysisBasis`, so `latestAnalysis: null` states whether no analysis has been saved or none CAN be -- the same separation D-158 made on the CLI, on the surface it recorded as open. Five tests, RED 5/5. |
> **D-279 update to WP-E06:** `fiscus causal plan --study <study-id> --options <file>` now previews a validated family plan and its planned-act/per-act-alpha arithmetic without opening or mutating a Store; `--apply` performs the existing immutable pre-registration write. The E06 row above remains `PARTIAL` because cross-study families, correlation-adjusted multiplicity, and decision-probability planning are still open.

| `WP-E07` | Sequential/adaptive inference lane | `SUPERSEDED_WITH_REASON` | A standalone sequential Bernoulli lane now commits registered looks and protocol/observation/result hashes, refuses unregistered stopping, sliding/clustered/adaptive/post-selected data, and revalidates nested result semantics plus the anytime interval before use. Store/CLI/dashboard persistence and integration, cluster-aware or adaptive methods, and full packet completion remain open. **D-250: the product's continuous-monitoring inference is the anytime-valid confidence sequence (`src/value/anytime.ts`), consumed by the headline and the frontier trial; the registered-look lane stays a reference implementation under a gate-checked "nothing calls" claim.** |
| `WP-F01` | Robust utility and preference representation | `COMPLETED` | `buildUtilityIntervalProblem` normalizes explicit intervals and point utilities only when an uncertainty bound is declared; unjustified point utilities are refused. Preference ranges, constraints, reversibility, and policy consumers remain. **Closed at D-249: the cap decision is normalized through `buildUtilityIntervalProblem` and carries its representation; constraints and reversibility are not modelled because the one product decision has none to declare.** |
| `WP-F02` | DecisionCertificate integration | `COMPLETED` | Strict interval-dominance certificates now persist through an Evidence-bound adapter with a decision-fitness Witness and Derivation; undetermined comparisons issue observation only. Minimax-regret, approved-policy, and action integration remain. **D-220:** `minimaxRegret` is consumed by the budget-cap decision and rendered as a selection rule, never as dominance; a certified `--apply` persists the certificate through the canonical adapter. Remaining: approved-policy and action integration (WP-F06). **Closed at D-252: certificates persist through the canonical adapter, the regret pick is certified under its own rule (D-220/D-221), and the action chain is the operator's `--apply` under a DAL-3 certificate with `no_action` semantics by type; approved-policy is refused as product policy (WP-F06).** |
| `WP-F03` | Preference robustness and value of waiting | `COMPLETED` | `preferenceRobustness()` now evaluates a finite explicitly declared admissible preference set, preserves per-preference ties, and returns only the intersection of optimal actions; disagreement is `preference_sensitive` with no forced action (D-130). Value-of-waiting/option value and product decision integration remain. **Closed at D-249: `preferenceRobustness` runs on the cap decision over a declared six-member admissible set and is rendered as a diagnostic that never moves standing; value-of-waiting has no action in this decision.** |
| `WP-F04` | True VoI -> evidence-debt planner | `SUPERSEDED_WITH_REASON` |  **Superseded at D-242 by the assurance shortfall/remedy machinery (D-213, D-240, D-242): an "evidence-debt planner" ranked by true VoI needs a posterior scenario distribution with a declared basis, which no product surface holds; inventing one would collapse unknown into a number. `valueOfInformation()` remains for a caller who can declare that posterior.** |
| `WP-F05` | Decision Assurance Levels | `COMPLETED` | Measured counterexample: `computeFrontier` on 8/8 candidate vs 2/40 incumbent units returns `confidence: 'observational_separation'` with no confounders; its per-unit saving turned into two action intervals makes `certifyDecision` return `proven_dominant` for `switch_default_model`; and that certificate persisted `claim:decision:fitness:switch-default-model` with `decisionFitness: 'sufficient'` and `causality: 'none'` without refusing. Models were never assigned and nothing on the path asked. `src/decision/assurance.ts` derives a level from the ten-axis `ClaimProfile` of every declared input and from the certificate's own result — a caller cannot assert one, there is no field for it — and `buildDecisionKernelIssuance` now refuses when a declared consequence class requires more than the inputs reach. DAL-3 requires `causality: 'randomized'` (AII-025) and a problem with fewer than two evaluated actions is capped at the floor (AII-026). `decisionFitness` is excluded from the ladder so a claim cannot raise the level governing it; every cap is the weakest input, never an average; `authorizesAction` is permanently false (D-150). Five tests: the two refusal tests RED first, plus three guard rails that pin that a randomized, directly measured, completely covered input DOES reach DAL-3 and issue — a gate that refused everything would satisfy the RED tests while making the surface useless. **Remaining, and it is larger than the fix: AII-025 is NOT closed.** The gate refuses only when a caller declares a consequence, and a grep finds NO production caller of `issueDecisionToKernel` or `buildDecisionKernelIssuance` anywhere in `src/` — `decision.certificate` is exactly the boundary AII-036 classifies `unreached`. The refusal now exists and is checked; no surface passes through it, and `recommendBudget`, the frontier and every other advisory surface still reach an operator without it. Nothing evaluates whether a declared input set is complete, and the ladder is a Fiscus policy choice rather than a derived threshold. **D-220:** the DAL is derived from typed certificate facts and rendered on `fiscus budget --recommend`; `--apply` is honoured only at the required level. Remaining: the frontier and other advisory surfaces do not pass through the gate; declared-input completeness is unevaluated. **D-240: the frontier now passes through the gate — every model-switch recommendation carries a derived DAL on both consequence classes, printed by CLI and GUI. Remaining: usage/cohort advice and the budget advisor's frontier cells bypass it; declared-input completeness unevaluated.** **Closed at D-248: every exported advice producer is classified under a gate (gated / withheld / refusing consumer); the frontier reallocation hint is removed, not disabled; the usage path issues no advice. Declared-input completeness is the evidence holder's claim by design, not a computed property.** |
| `WP-F06` | Recommendation -> policy proposal -> approval -> action | `SUPERSEDED_WITH_REASON` |  **D-252: the recommendation → approval → action chain exists and is tested for the one spend-changing action; a policy object from which Fiscus acts by itself is refused by construction (`actionSemantics.permitted: false` by type) and changing that is owner-reserved.** |
| `WP-F07` | Shadow, canary, TTL, and epistemic circuit breaker | `SUPERSEDED_WITH_REASON` | `src/decision/control.ts` adds an immutable preview-then-commit lifecycle for shadow → simulated effect → canary → monitored expansion → full rollout and terminal rollback, with revision checks and fail-closed stale/revoked/conflicted/incomplete evidence, regime-change, measurement, harmful/unobservable outcome, completeness, and TTL guards (D-131). No persistence, authorization, or external target execution is implemented. **D-250: the one spend-changing action already carries its shadow phase (replay), refuses authorization by construction, and has no external target; `control.ts` stays a reference implementation under a gate-checked "nothing calls" claim.** |
| `WP-G01` | Finish CapabilitySpec and generated contracts | `COMPLETED` | Generated dashboard interface contracts now test exact canonical-interface coverage and hash-bound browser copies (D-112). Full request/response schema generation and server/browser validation parity remain. **Closed at D-243: the generated interface table is validated by one shared deep walk in the browser (every JSON response, fail closed) and in the contract test (live routes), so server and browser cannot drift apart. Terminal limit: the server does not validate its own responses at send time, and inline/Record types pass by construction.** |
| `WP-G02` | Plugin contracts | `COMPLETED` | Additive plugin contracts define categories, capabilities, request/message boundaries, egress, credential handling, reversibility, and assurance metadata. No executable plugin host or universal generated contract exists yet. **Closed at D-234: the contract has an executable host (D-219-era `runPluginProcess`) and a first-party consumer, `fiscus plugin run` → `intake.ts` → kernel Evidence. A "universal generated contract" was not built: the typed contract in `contract.ts` is the contract.** |
| `WP-G03` | Plugin isolation | `COMPLETED` | Additive isolation metadata and boundary validation make declared permissions and transport constraints inspectable. `runPluginProcess()` now mediates one bounded child-process/stdio exchange, scrubs the child environment, binds evidence to the active request ID, and refuses capabilities requiring unsupported OS controls. This is not runtime sandboxing: filesystem, direct-network, credential-access, CPU, memory, and descriptor hard limits remain explicitly unenforced. **Closed at D-234 as far as the repository can: the host is wired to a consumer and every run prints the controls it does not enforce; OS-level sandboxing is outside a zero-dependency Node process and stays explicitly unenforced.** |
| `WP-G04` | Standards interoperability | `COMPLETED` | `docs/program/STANDARDS-INTEROPERABILITY.md` maps current primary sources for OpenTelemetry GenAI conventions, FOCUS Cost and Usage, W3C PROV, W3C Verifiable Credentials/Data Integrity, in-toto/SLSA, and draft-only SCITT. It records adapter direction, version/maturity, unsupported fields, exact/partial/unknown evidence states, and explicit non-equivalence to Fiscus truth, causal, monetary, and decision semantics. RED-first standards-map contract test passes 1/1. **Limit:** this closes research/mapping, not a deployed exporter, credential workflow, or SCITT service.** |
| `WP-G05` | `.fiscuspack` | `COMPLETED` | Versioned manifests, canonical bytes, attachment-path validation, resource limits, and conservative verification now provide a portable bundle foundation. `signFiscusPack()` and `verifyFiscusPack()` add canonical Ed25519 signing, explicit attachment completeness, and separate integrity/authenticity/truth outcomes; embedded-key verification is integrity-only unless a caller supplies a matching trust anchor. Executable interoperability, an independent verifier, and CLI/API integration remain. **Closed at D-229:** the independent verifier exists (D-212) and the CLI integration is `fiscus pack export|verify|inspect` over the epistemic ledger, with omissions and redactions stated in the manifest and truth never evaluated. Executable interoperability beyond Node, hosted verification and external review of the scheme are outside the repository. |
| `WP-G06` | Independent verifier | `COMPLETED` | `standalone/fiscuspack-verifier.mjs` is an independent Node reference/executable that imports only the standard library and verifies committed conformance vectors without importing the producer, Store or application runtime. It checks canonical manifest/attachment integrity, resource limits, Ed25519 signatures, embedded-key identity, optional trust-anchor pinning and explicit truth non-evaluation; `fiscuspack-verify` is included in the npm artifact. Focused conformance is 6/6 plus producer/standalone 16/16. Cross-runtime implementations, hosted execution and independent operational review remain. **Closed at D-226: steps 1–2 of the staged approach exist and are independent of the producer (D-212); step 3 and external review are outside the repository.** |
| `WP-H01` | Database integrity review | `COMPLETED` | Added an adversarial append-only schema integrity regression covering tampered trigger protection (D-113). Store startup now checks configured SQLite pragmas and pre-repair full integrity/append-only trigger authority; backup source and copied-artifact inspection reuse the same integrity boundary (D-139). Full SQLite migration-interruption, corruption/fault-injection, confidentiality and recovery review remains. **D-233: migration-interruption fault injection (seven-point DDL sweep, rollback verified). Remaining: confidentiality and recovery review; corruption beyond payload-digest and trigger tampering.** **Closed at D-251: byte-level fault injection closed two silent-drift classes (damaged schema text repaired over; out-of-vocabulary provenance read as the sentinel) and bounds the residue (no row digest on request rows). Confidentiality review is WP-I05's data-inventory scope.** |
| `WP-H02` | OIDC/JOSE production-grade decision | `COMPLETED` | **D-209 hardens the custom JOSE boundary:** JWT segments require canonical unpadded base64url with zero discarded bits, and JWK `alg`/`kty`/`use`/`key_ops`/`crv` must reconcile with RS256 or ES256 before verification. RS256/ES256, rotation, issuer-origin and algorithm-confusion protections remain green (OIDC 33/33; team-server 87/87). The production decision between this bounded implementation and a mature JOSE library, provider validation and deployment policy remains open. **D-223 decides it:** `jose` (team-server only, exact-pinned) verifies the signature; parsing, canonical base64url, JWKS discovery/caching/cooldown, metadata reconciliation and every claim rule stay local; `test/oidc-adversarial.test.ts` covers every dossier bullet with RED/old or mutant evidence and found three defects in the old verifier (sub-2048 RSA, coerced kid, unchecked discovery issuer), all closed. |
| `WP-H03` | Fuzzing, mutation, and fault injection | `COMPLETED` | Deterministic high-consequence mutation support now removes selected safeguards and checks that the mutation oracle detects each removal; focused H03 tests pass. This is a bounded mutation harness, not broad fuzzing, fault injection, or coverage of every control. **Closed at D-251: fault injection now covers the ledger file itself (stride sweep plus three targeted faults) beside the bounded mutation harness and the migration-interruption sweep (D-233); broad fuzzing of parsers is not claimed.** |
| `WP-H04` | Backup/recovery and migration assurance | `COMPLETED` | Backup manifests now expose the authoritative SQLite schema generation, reject unsupported newer generations, preserve legacy manifests that omit the field, and revalidate retained economic/historical-FX payload digests before restore. Old-schema restore/migration and no-overwrite/refusal paths are covered; epistemic payload replay and full migration-interruption coverage remain. **D-232:** epistemic payload digests are revalidated before restore and a restored kernel node replays through the readers. Remaining: migration-interruption fault injection (shared with WP-H01). **Closed at D-233: migration interruption is fault-injected across a seven-point sweep and rolls back to the legacy schema each time; with D-232 both named remainders are covered.** |
| `WP-H05` | Supply-chain assurance | `COMPLETED` | The checkout was already in a good state — actions SHA-pinned, `permissions: contents: read`, `npm ci`, every lock entry on `registry.npmjs.org` with `sha512` integrity — and nothing held it there. Five counterexamples verified by mutating the real files, each passing every existing gate: a runtime `dependencies` block added with no complaint; `uses: some-org/publish-action@v1` inserted while `ci-hardening` stayed green, because its pin check was an allowlist of two action names; `typescript` repointed at `evil.example.com` with a matching fake hash, since `npm ci` verifies tarballs against the lockfile's OWN integrity field; `.codex` appended to `files`, packing 17 internal operations records because the npm allowlist overrides `.gitignore`; and `npm ci` swapped for `npm install` plus a `curl | sh` step. `scripts/check-supply-chain.mjs` now audits all five as a pure function over read inputs, driven by tests against mutated copies of the real files, and `npm run verify:supply-chain` runs it sub-second (D-142). Twelve tests. No workflow was modified, so CI topology is unchanged. **Two of that remainder are now closed (D-163).** Every workflow install passes `--ignore-scripts`, enforced by the same auditor rather than by a second workflow-shaped test -- it already owns where a workflow's bytes come from, and what an install may EXECUTE is that question's other half; the rule covers `npm install` too, including the packed-tarball install the lockfile rule deliberately exempts, since that exemption is about provenance and those hooks would belong to the artifact CI is examining. The `prepare` build it displaces is replaced by an explicit `npm run build` in both jobs that run `npm test`, ordered between install and test -- explicit even though `pretest` currently builds fully anyway, because `pretest` has been silently narrowed to `--web` once already; the build count is unchanged. `prepublishOnly` now runs `typecheck:all` (root, browser app, team-server) plus team-server's own suite, closing a gate that checked one compilation domain of three -- the exact hole that reached CI twice as TS1294 at `31911cb` and sixteen TS2339/TS2353 errors at `c1f7ac5`. Along the way the lockfile rule's own counterexample was found to have gone vacuous, and failed rather than passing on nothing. Remaining, and all constraint-bound rather than merely undone: it reads declarations rather than an installed tree; a pinned sha is immutable, not trustworthy; `prepublishOnly` is pinned by composition rather than execution, because a functional run would make the root suite depend on a `team-server/node_modules` that root `npm ci` does not install; `--ignore-scripts` protects CI and says nothing about a developer's own machine; and there is no SBOM or signed provenance, both of which need a runtime dependency or `id-token: write` -- the first refused by this project's zero-dependency rule, the second owner-reserved. **Closed at D-237 as far as a zero-dependency repository can go: runtime SBOM generated by npm itself, checked by `scripts/check-sbom.mjs` in CI and uploaded as an artifact; no runtime dependency added. Signed provenance stays owner-reserved (`id-token: write`).** |
| `WP-H06` | Performance and scale | `COMPLETED` | `scripts/benchmark.mjs` measures canonical Evidence/Claim construction and persistent SQLite append/graph replay/as-of at bounded deterministic scale tiers, publishing non-vacuous quality counts for records, links, immutability, duplicate idempotency, divergent-payload refusal and missing-dependency refusal; `test/benchmark-contract.test.ts` gates the workload. Revocation closure, exact economic projections, large allocation runs, `.fiscuspack` verification, dashboard contract validation and proxy streaming remain unbenchmarked; no latency budgets are asserted. **D-238: revocation closure and `.fiscuspack` round-trip benchmarked with failing-capable quality blocks. Remaining: exact projections, allocation runs, dashboard contract validation, proxy streaming; no latency budgets (policy).** **Closed at D-253: exact projection, allocation run and the browser contract walk are benchmarked with quality blocks; proxy streaming is out of the harness by its no-network contract and latency budgets are release-owner policy, both stated in `docs/RELIABILITY-PERFORMANCE.md`.** |
| `WP-I01` | Claim Inspector as a real kernel viewer | `COMPLETED` | A read-only Claim Inspector profile section now exposes all ten transported ClaimProfile axes plus a separate figure-status row, preserving unknown/conflicted/refuted/uncosted semantics and adding no action surface (D-216). Full Inspector 2.0 work—derivation DAG, assumptions, raw Evidence and as-of reconstruction—remains. **Closed at D-257: `/api/kernel` serves one node with its record, edges both ways, derivations, assumptions, revocation and an as-of boundary; the Evidence view renders it as a reader. Remaining by design: one-hop neighbourhood, whole-graph replay per request, spine layers carry no kernel id.** |
| `WP-I02` | Additional epistemic UX surfaces | `COMPLETED` | `src/epistemic/ux.ts` composes a read-only Trace-the-Dollar lineage reader, bounded support/countermodel presentation, qualitative measure-next evidence gaps, and preference-sensitive Preference Map output. Missing source edges withhold the trace; countermodel bounds and empty reasons remain visible; no prior/VoI/probability or forced preference action is invented. `fiscus evidence ux --options <file> --json` is the bounded operator consumer; focused coverage 4/4, root typecheck/build green. This closes the structured semantic UX contract; a future browser route may bind it, but no second browser authority is introduced. |
| `WP-I03` | Progressive disclosure and parity | `COMPLETED` | The parity map is the product's own claim that its GUI/CLI coverage is inspectable rather than asserted, and its denominator was the size of the list rather than the size of the CLI. `src/cli.ts` dispatches 43 capability groups; six of them -- `start`, `init`, `economic`/`economics`, `backup`, `restore`, `diagnostics`/`diagnostic` -- reached a `case` with no capability row, so `paritySummary()`'s `total: 47` was a coverage fraction over a population the map had chosen. The error ran both ways: omitting the four with no GUI flattered the ratio, omitting `economic`, which has a real `/api/economic` binding, understated it. `test/dashboard-parity-population.test.ts` now holds the registry to the dispatch, the six rows exist, and `Coverage` has a fourth state, `not_applicable`, for a capability the GUI structurally cannot offer -- required to carry a `coverageNote`, counted in its own summary card, and rendered with its reason in the parity table cell beside the state (D-167). The converse holds and is gated too: no registry row names a verb the CLI does not dispatch. Remaining, and it is most of the packet: progressive disclosure itself is untouched -- nothing decides or tests what a first-run operator is shown before they have asked for depth, and the `territory`/`consequence` axes drive presentation without any test that presentation follows them. No row's `coverage` VALUE is checked against the GUI: `full` is a human claim that some screen exists and does the whole job, and nothing renders a view to confirm it. A row's `command` string is only read as far as its first word, so a documented subcommand or flag can be wrong without failing. The command string is now read in full — verb, subcommand words and flags, each pinned to the handler module — and the one vacuous row, a destructive CLI path that never existed, is corrected to a declared GUI-only route (D-205, `33b5ad9`). Remaining: progressive disclosure and `coverage` VALUE checks, as above. **D-239: coverage values are now checked against the app source and twelve overstated rows were corrected (six to `planned`). Remaining: progressive disclosure for a first-run operator, undecided and untested.** **Closed at D-256: progressive disclosure is a stated rule (first-run register gate; plain/precise formatters tested under both registers; consequence tags and territory-built navigation read from source) under `test/dashboard-progressive-disclosure.test.ts`; coverage values were gated at D-239.** |
| `WP-I04` | WCAG 2.2 AA and runtime accessibility | `BLOCKED_EXTERNAL` | Four defects repaired, each RED first. The skip link — the only control in the shell that exists purely for keyboard and screen-reader users — wrote `#main` into `location.hash`, which the shell routes on and which is not in `ALL_ROUTES`, so following it moved the operator to Metered; it now moves focus without going through the router, and the test extracts the route whitelist and asserts `main` is absent from it. A drawer commit's outcome had no role and no live region, and was rendered at the instant the same signal disabled the button focus was sitting on, so the one action in the GUI that changes state was the one message nothing spoke. The Data detection run announced neither its wait nor its success. The daily-cap input — the only field where empty and `0` mean opposite things — had its consequence in a loose sibling with no `aria-describedby` (D-143). Browser typecheck, 6/6 contract, and 64/64 across the twelve other files reading the browser app. **All five named remainders are now closed, each RED re-verified in this tree by reverting the source file rather than trusting a report** (`app.css` 1 failure, `drawer.ts` 1, `value.ts` 2, `system.ts` 1, `allocation.ts` 1). `app.css` clips `.ledger-head` to 1px at narrow widths instead of `display: none`, so the column headers stay in the accessibility tree; the value ledger gains the table semantics `spend.ts` already had, including a header row that did not previously exist; both `notyet` heading skips became `h2`, and a THIRD instance of the same skip was found in `allocation.ts` and fixed rather than left, since leaving it would mean the defect was not actually closed; the drawer's blocked reason moved from a `title` a disabled control cannot expose to a visible sibling referenced by `aria-describedby`, matching the daily-cap pattern; and the parity table gains an `aria-label` and `scope="col"` on all five headers. Five commits, one per defect. Remaining: still no runtime accessibility — every test reads source text, not a rendered DOM, and closing that would need jsdom or a headless browser, which this project's zero-runtime-dependency rule refuses. So nothing here establishes what a screen reader actually announces, and no full WCAG 2.2 AA audit of the dashboard has been performed: what is closed is the enumerated defect list, not the standard. **D-271: the runtime repository gate is now exercised by a real Chromium harness.** Playwright is dev/test-only; `test/dashboard-runtime-accessibility.browser.mjs` drives the built dashboard over loopback, verifies first-run focus/escape/registration, skip-link focus restoration, accessible names, responsive no-overflow, loopback-only network requests, clean page/console errors, and axe WCAG 2A/2AA violations (1/1 locally). CI has a dedicated browser-accessibility job. The remaining external gate is exact assistive-technology announcement behavior (NVDA/JAWS/VoiceOver), which is not fabricated by a DOM/axe run.** |
| `WP-I05` | Data inventory, privacy, retention, and evidence consequences | `PARTIAL` | `docs/DATA-BOUNDARIES.md` described kinds of traffic, but the authorization vocabulary an operator actually writes into `egress.rules` is a purpose token and a data-class token, and neither list appeared on the page a public reader trusts instead of the source. All nine declared paths are now tabulated with data class, what may cross, and the command that reaches it — including that `provider_inference` carries prompts, source snippets and the caller's credential, and `provider_cost_observation` sends `OPENAI_ADMIN_API_KEY` from the process environment — pinned against `EGRESS_PURPOSES` and `EGRESS_DATA_CLASSES` in both directions. Five retained columns that can carry a person's own material (`requests.cwd`, `requests.user`, `git_commits.subject`, `proposals.files_json`, `scan_snapshots.repos_json`) are named individually rather than folded into 'labels'. And deleting evidence now costs something: the receipt history's absence alone previously decided genesis, so deleting it left `fiscus egress verify` reporting a valid chain with no trace a longer one had existed; a surviving checkpoint sidecar beside an absent history is now a discontinuity, since a checkpoint is only published after a receipt was appended (D-147). Nine tests, RED verified in the integrating tree. Remaining: the table is checked against the tokens the code accepts, not against observed traffic; the column list is hand-maintained, so a sixth sensitive column added later needs the test updated too; deleting history AND checkpoint together is still indistinguishable from a fresh home, which needs an off-machine anchor; and no retention period, deletion command, or subject-access path is specified anywhere. A fourth instance of the absence-as-finding class was then found and repaired in the same subsystem: `fiscus egress verify` printed a green `Receipt chain valid` over `Receipts: 0` and exited 0 on a home that had never sent anything, and `fiscus diagnostics` said `Egress OK (0 receipt(s))` — a hash chain over an empty set verifying vacuously, so the most reassuring output the command can produce appeared exactly when Fiscus knew least. `verifyEgressReceipts` now returns a state, one of four bases (`no_record`, `chain_intact`, `chain_broken`, `discontinuity`), the window the chain covers, and both halves of what the result is worth; `no_record` is yellow rather than red, because dressing an absence of evidence as a fault is the same collapse in the other direction. The positive half is real and checked rather than assumed: every declared egress path goes through one chokepoint that appends before forwarding, and a test walks `src/` and fails if any module outside `src/egress/` reaches the network directly (D-148). Nine further tests, eight RED. A third instance of the same class was then found and closed in the retention path itself: `fiscus prune` deleted request rows and recorded nothing, so a period Fiscus never observed and a period Fiscus observed and then deleted read identically -- and `fiscus guide` read an emptied ledger as "no traffic yet" with metering marked NOT DONE. `retention_prunes` now records every boundary applied, its row count and its time; `Store.retentionFloor()` reports it three-valued, where a null boundary means NO PRUNE IS ON RECORD rather than nothing pruned, and is never backfilled from the oldest surviving row; and the guide says whether a count sits behind a boundary (D-170). Eight tests, RED at 7 of 8. `Store.windowCoverage(startMs)` then answered the same question per window, and `today`/`week`/`month`, `sources` and `export` read it -- truncation being a comparison that is false at the boundary instant `prune` retains, so the notice cannot become a warning on every report; `export`'s notice goes to stderr because a line on stdout would corrupt the export it protects (D-171). Six tests, RED at 5 of 6. The class was then searched rather than waited on, and the reconciliation was found to be a fourth instance and the only one about money: a prune inside a reconciled period turned a **-$2.00** residual refuting the off-path bound into a **+$4.00** residual asserting one, so `offPathBound` gained a third state, `conditions` a sixth member whose countermodel is `realized` because Fiscus recorded performing the deletion, and both compilers now refuse a wire or browser that omits the state (D-171 remainder, closed at D-173). Seven tests, RED at 4 of 7. Remaining beyond the above: the dashboard discloses nothing for WINDOW summaries -- `/api/overview` and the browser views still read them with no coverage field, which is a payload-contract change across three compilation domains; `report`, `usage` and the value surfaces read their own windows and are not covered; proposal prunes are recorded but nothing reads their floor; no refuting completeness witness is emitted, because no negative claim is made over the request stream to consume one; the coverage claim bounds what Fiscus recorded and not what the machine sent; the window would narrow silently under a retention policy, and no retention period, deletion command or subject-access path is specified anywhere; and the chokepoint premise is checked by a source walk a sufficiently indirect call could evade. **D-255: read against the code, three of the four remainder items above were already false — overview/browser disclosure, the report/usage/value surfaces, and the proposal-prune floor were already wired; only the request ledger's own `retentionDays` was genuinely undocumented, and `docs/DATA-BOUNDARIES.md` now names it (180-day default, `fiscus prune` as the deletion path, `fiscus pack export` as the subject-access path). No code changed. Still open: no refuting completeness witness, coverage bounds recorded traffic not sent traffic, silent narrowing on a retention-period change, and the source-walk chokepoint check.** |
| `WP-I06` | Documentation truth and reproducibility | `COMPLETED` | Documentation is the operator's first executable surface and nothing read it against the CLI. 244 `fiscus <action>` invocations sit inside code fences and inline spans across `README.md` and the 25 operator-facing files in `docs/`, covering 40 distinct commands and 49 distinct flags; every flag occurs in `src/`, every `npm run` script resolves, every documented port matches `DEFAULT_CONFIG`, and two command names did not resolve against the CLI dispatch. `test/documentation-commands.test.ts` now gates all four, so the surface cannot drift back without failing (D-166). `fiscus lab complexity` is allowlisted with the reason its own document already marks it as proposed, checked by a meta-test against that document; `fiscus lift` was set in code formatting in two sentences about an open item and those lines are corrected instead, establishing the rule the scope rests on: code formatting means "type this", so a command that does not exist must not be set as code even in a sentence saying it does not exist. The sweep found a defect in itself: its first working version matched ZERO fenced blocks in the CRLF-terminated `README.md` and reported no violations over a corpus it had not read. Only the vacuity test's corpus-size assertion noticed. Remaining, and this is most of the packet: nothing is EXECUTED, so no documented command is shown to work; a flag is not checked against the specific command it is documented under, because the CLI declares no per-action flag set to read; no output shown beside a command is compared with what that command prints; and reproducibility in the wider sense -- that a reader following a document end to end reaches the state it describes -- is untouched. **D-198 gave the fifteen module contracts their first check, and three were false.** `src/epistemic/` said three boundaries strengthen claims outside the kernel (the map carries one); `src/measurement/` said no production site resolves a model through `measurementRegistry` (`src/causal/measurement.ts` does, from inside the product closure); `src/store/` guaranteed integer microdollars and that no float reaches a column, and both halves were wrong. `test/module-contracts.test.ts` now checks file references, named symbols and reach claims -- the last against the walker extracted unchanged from the issuance-map test into `test/support/importGraph.ts`, so a contract and the map cannot disagree about reachability by counting differently. **The gate found none of the three**; they were found by reading. What it secures is that those forms cannot rot from here, and it reports `103 claims checked, 329 code spans and all prose UNCHECKED`, asserting that count cannot silently fall. **Unchecked and live:** all prose, and CONTRACT OMISSION in the other direction -- a module gaining an export its contract never mentions triggers nothing, which is the current state of `src/epistemic/` and `src/decision/` regarding the D-195 exports. **Closed at D-235 as far as a gate can go: commands, flags, ports, scripts and 103 contract claims are checked; a contract-omission gate was measured and rejected as a false-positive machine; prose truth stays unchecked by construction and is reviewed by reading.** |
| `WP-J01` | Adaptive experimentation and provenance-aware OPE | `COMPLETED` | `src/causal/ope.ts`, `src/store/ope.ts`, the `ope_action_observations` schema, and `fiscus causal ope --options <file>` provide a provenance-aware OPE boundary plus an append-only Store log: action/treatment identity, logging propensity, target-policy version/digest, pre-treatment context timing, overlap refusal, IPS/self-normalized IPS/DR estimates, explicit clipping/tail-risk disclosure, digest-authenticated replay, idempotent writes, a bounded review-only product consumer, and a typed exploration/budget/tail-risk policy declaration bound to the target policy. RED-first focused coverage is 13/13; ordinary retrospective model comparisons remain refused as OPE. Boundary: no online action execution or routing authority is implied; that is J02. |
| `WP-J02` | Constrained online control | `NOT_STARTED` | Dependency-gated by an approved control target, runtime action semantics, rollback/circuit-breaker, safe baseline and no-action authority. `docs/program/J01-J02-DEPENDENCY-GATES.md` records the gate; current control/decision modules remain review-only and no-action. No online spend/routing controller is implemented. |
| `WP-J03` | Complexity Lab | `PARTIAL` | Read-only design/precondition audit: the current code exposes a zero complexity surface (`0` files in `src/`, `0` routes, `0` benchmark operations), so no production complexity code, CLI action, or dashboard route is added. The ten promotion rules in `docs/TOKEN-GOVERNANCE-AND-COMPLEXITY-LAB.md` §12 and full audit `docs/program/WP-J03-COMPLEXITY-LAB-REPORT.md` are durable. Remaining: a real candidate sophistication, a separated benchmark against a simple baseline, calibration/decision-value evidence, and a rollback-gated admission decision. |
> **D-281 update to WP-J03:** `src/research/complexity/profile.ts` is now a deterministic, provenance-bearing research-only vector over content-free structural/execution observables (2/2 focused tests). It emits no complexity score, routing, budget, causal, CLI or dashboard authority; the ten promotion rules and held-out evidence gates remain open, so WP-J03 remains `PARTIAL`.

| `WP-J04` | AI capital, showback, spend decomposition, opportunity gaps | `COMPLETED` | `src/capital.ts` provides a versioned exact-money capital-account evaluator: committed/consumed/reserved/unused capacity; direct/allocated/full/realized/avoidable spend; marginal scenarios kept outside realized spend; explicit observation coverage; conservation and double-count refusal; counterfactual-only opportunity gaps; and descriptive policy-relative fairness. `fiscus capital evaluate --options <file> --json` is a bounded review-only consumer with RED-first focused coverage 5/5, root typecheck/build green. No provider commitment authority, external chargeback, autonomous action, causal attribution, or business-value claim is implied; durable invoice/request reconciliation remains an owner-approved future authority decision. |
| `WP-J05` | Current-market capability matrix | `COMPLETED` | `docs/program/MARKET-CAPABILITY-MATRIX.md` now covers primary sources for Langfuse, Datadog Agent Observability, Vanta, FOCUS, OpenTelemetry, OpenCost, IBM Kubecost and IBM watsonx.governance. The matrix separates build/interoperate/refuse decisions, records the 2026-09-21 source refresh, and explicitly avoids superiority, market-share and certification claims. Contract test passes with the broadened source set. Boundary: re-run the matrix before any future external positioning. |
| `WP-J06` | Originality, substitution, and complexity-theater review | `COMPLETED` | `docs/program/ORIGINALITY-SUBSTITUTION-REVIEW.md` classifies mature standards/substitutes, Fiscus-specific semantics, potentially distinctive combinations and unproven claims; the 2026-09-21 independent critique incorporates OPE methodology and current OpenCost/Kubecost/watsonx.governance substitutes, and a pre-claim review gate now requires a strongest counterargument before field-leading language. Contract test passes with the broadened review. Boundary: the composition remains unvalidated as a historical-originality or market-leadership claim. |
| `WP-J07` | Public-interest/open-source launch readiness | `COMPLETED` | Repository side, D-225: CONTRIBUTING, GOVERNANCE, SECURITY, THREAT-MODEL, RELEASE-PROCESS, COMPATIBILITY, SUPPORT, NEUTRALITY, NAME-COLLISION-REVIEW, gated by `test/launch-readiness-docs.test.ts`. Release, publish, tag, rename, license and brand are owner actions and stay outside the repository. |
| `WP-R01` | Evidence abstract interpretation | `COMPLETED` | Two findings, the first touching the distinction the product rests on. `assessDerivationLegality` iterates `PROFILE_STRENGTH_AXES`, which cannot include `monetaryBasis` because that axis has no ladder — so the rule places no constraint on it at all. Measured: a derivation whose input carries `monetaryBasis: 'estimated'` and whose output carries `'billed'` is `allowed: true` with ZERO required witnesses, and `appendDerivationWithinTransaction` stores it, because the ledger checks every input claim against a rule that never looks at the money axis. `metered usage != provider-billed cost` collapsed inside the component that exists to prevent it. Second, structurally: nothing bounded what a whole CHAIN licenses, so a conclusion several merges downstream of its leaves was compared with its neighbours and nothing else. `src/epistemic/abstract.ts` is the abstract interpretation — domain the axis lattice, transfer function the derivation rule, soundness "the concrete result is below the abstract bound" — reusing the ordered/unordered split `admissibility.ts` already declares so `monetaryBasis` acquires no ordering. `PROFILE_STRENGTH_AXES` is now exported and READ rather than restated. Every choice narrows: no inputs is BOTTOM not TOP, an unresolved input is BOTTOM, two inputs MEET at the weaker, disagreeing bases become `mixed`, a conflicted join admits only `conflicted`. `BASIS_DERIVATIONS` is deliberately empty — nobody has declared a legitimate re-basing, which is not the same as none existing (D-151). Fourteen tests, RED verified two ways. **The money axis is now wired, and it is the first wiring rather than another mechanism.** D-151 built a function that refuses the estimated-to-billed re-basing and nothing called it, so the kernel still accepted it; D-152 added a `monetary_rebasing` witness kind and pushed the requirement into `assessDerivationLegality`, which is what `appendDerivationWithinTransaction` runs, so the unwitnessed re-basing is now refused at the boundary that persists. A witness rather than a blanket refusal, because allocation is a legitimate re-basing and making it inexpressible would push the work outside the kernel. Two moves stay free and both are weakenings: keeping the basis, and dropping to `none` or `mixed`. Six further tests, five RED. The assertion that recorded the hole was INVERTED rather than deleted, and now checks that the per-step rule and the chain abstraction agree. **Remaining: the chain half is still unwired.** `analyzeDerivationChain` is called by nothing, so chain-level bounds remain unenforced — one axis moved from the abstraction into the boundary and the rest of the abstraction did not. The witness is a declaration and not a proof: nothing checks that its evidence supports the re-basing it licenses. `BASIS_DERIVATIONS` is still empty, so neither rule knows which re-basings are sound — they agree on refusing and on nothing else. And no existing Fiscus derivation re-bases, so nothing in the product exercises the new path: this closes a hole rather than enabling a feature. It never says a proposition is true; it takes each leaf at face value and bounds only what a chain ADDS; and a witness lifts its axis to that axis's top, so the abstraction is only as tight as the witness discipline it inherits. **D-222:** `analyzeDerivationChain` is the commit-time gate — every stored derivation upstream of a candidate is walked to its leaves and the whole chain is checked; `estimated → mixed → billed` in two hops is refused. Limitation: leaves are taken at face value and a witness lifts its axis to the top. |
| `WP-R02` | Epistemic Preservation | `COMPLETED` | Preservation helpers and tests retain claim/evidence distinctions, explicit unknown/partial/conflicted states, and non-escalating transformations across the bounded surfaces implemented in this tranche. Universal preservation review and product-wide integration remain. **Closed at D-235: product-wide preservation is enforced by the ledger at every append; the abstract checker is held to the ledger over the full evidence-comparable grid (`test/preservation-ledger-agreement.test.ts`). Terminal limit: the five axes Evidence cannot carry are compared only on the derivation path.** |
| `WP-R03` | No Granularity Laundering | `COMPLETED` | `grain.ts` now declares directed domain rollups and the direct claim boundary uses `grainIsSupportedBy`, so legitimate billing/provider-period rollups are accepted while undeclared dimension invention is refused (D-108). Scope now has the analogous `scopeIsSupportedBy` boundary guard: a claim cannot narrow, contradict, or otherwise change every cited evidence scope without a scope bridge; equal and broader aggregation remain admissible. RED-first scope tests cover narrower/disjoint refusal and broader/equal permission (D-114). Remaining: rollup/scope declarations are hand-maintained and not independently validated; dimension partition identity and scope hierarchies remain structural; derivation transformations still use their typed witness path. **Closed at D-244: every declared roll-up is bound to the producer that enforces its partition and gated (probe refusal, acyclicity, no dead declarations). Scope hierarchies stay structural and derivations keep their typed witness path by design.** |
| `WP-R04` | Negative-Claim Soundness | `COMPLETED` | The reachable coding-realization negative `clean` predicate is withheld from kernel issuance when modern exact/mature/realized snapshots lack a qualifying `CompletenessWitness` for both `commit_reverted` and `linked_incident`; compatibility snapshot persistence remains intact. Generic negative Claims now refuse incomplete cited Evidence across event type, target scope, and the full claim interval (D-109). RED-first coverage proves no kernel Claim is issued and valid complete persistence remains idempotent. Reconciliation's residual/off-path bound is already typed and carried through its Claim. Remaining: other negative claims need the same boundary audit; local git source only witnesses revert coverage, so coding `clean` remains unresolved without an incident-feed witness. **Closed at D-245: every issued predicate is classified by polarity under a gate; the one absence claim (coding `clean`) names its witness mechanism; `clean` stays unresolved without an incident witness by rule.** |
| `WP-R05` | Trust Non-Escalation | `COMPLETED` | Direct Claim persistence enforces a weakest-cited-evidence ceiling on three axes — integrity, authenticity and coverage — at the unavoidable `EpistemicLedger.appendClaim` boundary, so a caller cannot bypass it by constructing a Claim directly (D-104, `e7f2b79`). The ceiling is the WEAKEST cited evidence rather than the strongest, because every cited evidence is a prerequisite — the reading D-098 settled — so one verified invoice cannot launder any number of unverified notes. Six adversarial tests, three of which were RED first. Remaining: `monetaryBasis` is deliberately excluded because it is not a ladder and `mergeClaimProfiles` refuses to rank it; measurement, causality, finality and decision-fitness have no evidence-side counterpart to bound them against; a claim citing no evidence was recorded here as unbounded, and measurement showed that reading was already stale -- `claim()` has required at least one `evidenceIds` entry since the envelope was introduced, and the dead early-return that once let a zero-evidence claim past the ceiling check was removed at D-106. Confirmed by relaxing the constructor's own required flag: such a claim then read back maximal on every axis. The relaxation was reverted and a regression test added instead, so the property is now asserted rather than assumed; and coverage cannot compose — two partial exports that tile a scope are refused rather than unioned, which needs a `CompletenessWitness` rather than a comparison. **Closed at D-235: coverage non-composition is the rule and is now asserted (`test/coverage-non-composition.test.ts`); a `CompletenessWitness` over the union is the one thing that supports `complete`, and the ledger already accepts it.** |
| `WP-R06` | Monetary Conservation | `COMPLETED` | Three of four sites reconciling an exact amount against its USD-named float compared magnitudes only, so an exact EUR 100.00 agreed with `costUsd: 100` and was summed into `total_cost_usd` (D-096); one shared helper now performs that reconciliation and checks the unit, which `src/value/epistemic.ts` already did. FX chains and disguised allocation reversals cannot mint money that no rate or allocation produced (D-093), adjustment bases and aggregate negative adjustments are bounded (D-089), exact allocation persistence now reconciles source-event IDs, identities, and amounts against the economic charge ledger (checkpoint `110b3dc`), and new exact allocation runs now require an immutable finalized-close binding (checkpoint `c3e0034`). The explicit effective FX read model preserves raw source/correction lineage and applies one selected exact rate without turning the projection into a raw ledger balance (D-125). Exact allocation persistence now resolves full validated multi-hop correction chains to charge roots and requires complete cited lineage (D-127). Exact Store reconciliation now consumes effective Money before compatibility floats and refuses non-USD/sub-micro exact values without a declared quantization policy (D-129). Remaining: the compatibility field still carries its unit in its name rather than on the wire, provider-authoritative rate policy and universal mixed-currency consumer coverage remain open, and nothing reconciles a receipt or rollup against the ledger it summarises. **D-231: a receipt now reconciles against the ledger it summarises. Remaining: the compatibility field still carries its unit in its name; provider-authoritative rate policy is WP-C03; mixed-currency consumer coverage.** **Closed at D-236: realized exact amounts are reconciled against their float as total already was; `src/team/**` is inside the FX consumer sweep; the unit travels on the v2 wire as `amount.currency` and the v1 floats are a frozen layout whose rename would be a format break. Provider FX authority is WP-C03.** |
| `WP-R07` | Revocation Closure | `COMPLETED` | The closure itself was already correct; the three kernel-claim readers that serve `/api/billing` ignored it and returned revoked claims as `supported`/`verified` (D-094). They now apply the projection through one shared helper: the claim is still served, carries `revoked`, and its `epistemic` axis reads `unknown` while `integrity` is deliberately untouched. Two further contradictions in the same packet are closed: `minimalSupportingSets` read dependency edges as alternatives while the closure read them as prerequisites, so cut sets said an auditor must revoke both invoices to cut a claim that one revocation already cuts (D-098); and the `revocation` envelope that `Evidence` and `Claim` both carry was validated, stored and never projected, so a provider statement that said on its face it had been withdrawn read as live along with everything derived from it (D-099). **Two more are closed, and the second one's own cost with them.** An envelope's `eventId` is now cross-checked against the event table in BOTH directions -- appending a revocation event reusing an id already claimed by an envelope on a different target, and vice versa, both succeeded silently; only a MISMATCHED target is refused, so D-099's no-deadlock design (an envelope need not have a recorded event) survives. `RevocationProjection` gains an effective-time dimension: a node whose envelope declared `effectiveAt: 2026-08-05` was already in `revokedIds` at an as-of of 2026-08-03, two days early, and the existing envelope test pinned that reading as intended in its own comment -- that assertion is now inverted, with `pendingIds` holding known-but-not-yet-effective nodes. Table-recorded revocations are unchanged: `appendRevocation` has no effective time and is effective when recorded. **And the fix's own cost was measured and repaired (D-161).** `pendingIds` was consumed by nobody, so a decision certificate resting on evidence with a withdrawal already booked moved from `invalidated` to `valid` with an empty `invalidatedBy` and the booked withdrawal appearing nowhere -- silence replacing a wrong answer. `readDecisionCertificateBundle` now returns `pendingInvalidationBy` beside `invalidatedBy`; `status` deliberately stays `valid`, because at the instant asked it is. Remaining: only one of the three kernel readers is exercised end to end; those readers take no as-of boundary, so a revocation recorded today rewrites how every past read renders; nothing renders `pendingInvalidationBy` on any CLI or dashboard surface, since none reads a certificate bundle at all; and neither set function has a caller outside its own module, so cut sets reach no user. **D-220:** `pendingInvalidationBy` and the invalidating assumption sets are rendered on `fiscus budget --recommend` for every persisted budget-cap bundle. Remaining: the kernel readers take no as-of boundary. **Closed at D-230:** the three kernel readers and `/api/billing?asOf=` answer at a knowledge boundary — events recorded after it do not apply and nodes not yet available are not served. Limitation: cut sets reach no user surface, and `pendingInvalidationBy` renders only where a bundle is read (`fiscus budget --recommend`). |
| `WP-R08` | Decision-Certificate Soundness | `COMPLETED` | Strict interval dominance is now persisted as an Evidence-bound decision-fitness Claim with a witness and Derivation; undetermined intervals issue observation only. The adapter does not yet certify minimax-regret actions or integrate an approved action policy. **D-220/D-221:** the regret pick is certified under its own rule and never presented as dominance; the adapter can no longer write axes its evidence does not support. Action policy is WP-F06. |
| `WP-R09` | Minimal evidence acquisition | `COMPLETED` | **D-212 narrows the shipped instrumentation sensitivity ranking to its real authority:** it identifies the largest sensitivity/measurement exposure and carries no utility, probability, decision or acquisition-cost model. Unsupported `cheapest`/`buy next` language was removed from CLI/docs, while formal `valueOfInformation()` remains the separate decision-theoretic path with explicit `measurementCost`. Focused instrumentation/contract coverage is 5/5 and `test/value.test.ts` is 63/63. A product acquisition workflow and decision-certificate route remain open. **Closed at D-242: the acquisition workflow is the assurance gate's remedy list, attached to the budget decision (D-213) and to every frontier recommendation (D-240), printed as "next evidence" when the advice bar is not met; the certificate route is the gate. A probabilistic VoI would need a prior the product must not invent.** |
| `WP-R10` | Model pluralism | `COMPLETED` | Provider/model grouping now uses collision-free two-field tuple identities in the frontier and exact economic attribution paths; delimiter-bearing provider/model names no longer collapse into one observational comparison (D-217). Other composite-key audits, model-quality pluralism and routing policy remain open. **Closed at D-241: the composite-key audit is a gate over every identity-shaped delimiter join (four could alias; all tuples now). Model-quality scoring and routing policy are product refusals, not open work.** |
## Accelerated packet checkpoint (2026-09-16)

- `WP-B01` direct monetary-basis strengthening is now guarded at the ledger
  boundary: estimated or mixed cited Evidence cannot become billed or allocated
  without a supported registered `monetary_rebasing` Witness whose typed basis
  pair matches the transition. Legal weakening and unchanged bases remain
  admissible; no global monetary order was invented (D-215).
- `WP-D06` now renders existing per-channel alert coverage and dark reasons on
  both modern Control and the classic Overview, scoped only to `today`; the
  other AII-002 negative claims remain open (D-204/D-218).
- `WP-J03` remains `PARTIAL`, with its truthful read-only precondition audit
  is now durable at `docs/program/WP-J03-COMPLEXITY-LAB-REPORT.md`; no production
  complexity score or route is claimed.
