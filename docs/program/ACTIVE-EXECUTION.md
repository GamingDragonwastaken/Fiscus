# Active Execution

**Resumption state, not history.** Historical decisions and commit-bound evidence live in `docs/program/DECISION-LOG.md` and `docs/program/EVIDENCE-INDEX.md`. This file exists to keep the next executor from repeating settled work, and it is rewritten rather than appended to.

## Where

| | |
| --- | --- |
| Branch | `gpt56/magnum-opus-reconstruction` — never `main`, never force-pushed |
| Last CI-verified exact head | `36aab1940f18295368cbec71e85e6e9b966076e1` — run `34342008121`, **success on all eight configured jobs**, inspected job by job. Verified the same way in order before it: `983eca0` (run `34339948316`, success), `ef68b89` (run `34338870049`, success), `a77a4e1` (run `34338149908`, success), `40041b8` (run `34337221806`, success), `abe50be` (run `34336243081`, success), `baa617a` (run `34335494594`, success), `4e0eaba` (run `34334467204`, success), `fae969d` (run `34333844248`, success), `030cfd9` (run `34332465354`, success), `9d84d5c` (run `34317025313`, success), `eb32b41` (run `34316568367`, success), `b7028d2` (run `34315518166`, success), `2d655c9` (run `34314680129`, success), `07c68eb` (run `34314090355`, success). Verified the same way immediately before: `ab59f30` (run `34312731008`, success), which is where D-172's build-race fix was actually established -- the failure it repairs occurred on `test (windows-latest)` alone, at `cc8ef35` (run `34311975381`, failure), and only that job going green at the exact fixed head settles it. Before that, `a86b922` (run `34311206030`, success). Earlier in the chain, `707581b` — run `34175216032`, **success on all eight configured jobs** (`test` ×3, `team-server-test` ×3, `package-smoke`, `candidate-head`), inspected job by job, not by trusting the first green row. Verified the same way before it, each all-eight green: `aa5ec22` (run `34174439515`, success), `2aa7cac` (run `34174382288`, success), `51f3ec6` (run `34174019795`, success), `0f0811d` (run `34173907843`, success), `0ebb966` (run `34173069140`, success), `e0cd80f` (run `34172882201`, success), `dfcfc87` (run `34171128690`, success), `ac98395` (run `34170679364`, success), `6b91318` (run `34169977899`, success). |
| Code ahead of that head | This records commit only, and its own run is **PENDING** — the fixed point every such record hits. `a0c9bc5` (run `34174827068`) is the one FAILURE in this chain and is left named rather than tidied away: it went red on four jobs because the row above listed eight run identifiers behind a single outcome word, and the record gate is right to refuse that. |
| Local gates on the current tree | Root suite **1,853 total / 1,849 pass / 0 fail / 4 skipped**; the last suite run made deliberately under six concurrent CPU-bound processes was 1,666 / 1,662 / 0 fail, which is the standard this program now holds itself to; team-server **74/74**; all three TypeScript domains clean (root `tsconfig.json`, `src/dashboard/web/app/tsconfig.json`, `team-server/`) |
| Dossier source | `FISCUS_EXECUTION_DOSSIER_III.md` is **not in this checkout and not in git history** — it was an owner-supplied input. `docs/program/PACKET-INVENTORY.md` is the surviving mechanical enumeration of all 76 packets and is authoritative here. Do not re-derive a packet count from anything else. |

## Packet accounting

76 packets. **11 COMPLETED, 43 PARTIAL, 22 NOT_STARTED, 0 IN_PROGRESS, 0 BLOCKED_EXTERNAL, 0 SUPERSEDED.** Regenerate rather than trust this line:

```bash
grep -oE '\| `(NOT_STARTED|IN_PROGRESS|PARTIAL|COMPLETED|BLOCKED_EXTERNAL|SUPERSEDED_WITH_REASON)` \|' docs/program/PACKET-INVENTORY.md | sort | uniq -c
```

`PARTIAL` is not a nearly-finished `COMPLETED`. Every PARTIAL row names its own remainder; read the row before assuming a packet is nearly done.

## The local suite is now a deterministic gate, and it was not before

Directive item 4. Two separate defects made a full local run non-deterministic, and both were found by reproducing rather than by reasoning:

- **`test/plugins-host.test.ts` had a load-sensitive deadline** (D-154). Its shared fixture allowed 250ms of child startup for tests that spawn a real `node` child. Under twelve concurrent CPU-bound processes it failed on demand — three runs at 2, 4 and 2 failures. Every intermittent full-suite failure previously observed in this program was this file, and each time it had been set aside as "local contention", which was a description and not a diagnosis.
- **Two tests contradicted each other about the repository artifact** (D-155). `test/build-race.test.ts` asserts that an isolated build does not republish this checkout's `dist/cli.js`, defended by a comment claiming nothing else in the suite builds at ROOT. `test/egress-guidance-launcher.test.ts` ran `npm run fiscus`, whose `prefiscus` hook is `npm run build`. Running those two files together failed EVERY time with no artificial load.

Neither fix weakened an assertion; in both cases the behavioural claim was kept and the incidental dependence on the harness schedule was removed. **A full run under six CPU-bound processes is now 1,666 / 1,662 / 0 fail / 4 skipped.** If a future run is red, treat it as a finding rather than as noise — that reflex is what let both of these survive.

**A third instance was observed, went undiagnosed for two rounds, and is now CLOSED at D-172.** On one full run this round, `build-race.test.ts`'s "concurrent builds keep the compiled CLI runnable throughout publication" failed after 38 seconds. It passed in isolation immediately afterwards and passed on the next two full runs, and the failure message was not captured before the run scrolled. So there was one observed failure in roughly a dozen full runs and no diagnosis, which by this file's own rule is a finding and not noise.

**It was diagnosed the moment it failed somewhere the output was kept.** It reproduced on CI at `cc8ef35`, on `test (windows-latest)` alone: `build failed: browser app`, exit 2, from one of the two concurrent builders. `scripts/build.mjs` generates three files INTO `src/` before compiling, under the publication lock -- which serializes the two writers against each other and not against the other builder's `tsc`, which reads them holding no lock by design. So one builder rewrote a source file the other was compiling. Windows rejects that; POSIX mostly tolerates it, which is why one runner out of three saw it and a dozen local runs did not. The three writers now compare bytes before writing, and since the content is a pure function of the source the normal case is a skip (D-172). **The lesson generalises past this defect: a machine forgiving enough to hide a fault is not evidence the fault is absent, and the run that finally showed it was the one whose log was retained.**

**It was hunted with output captured, and did not reproduce -- but the hunt caught something else.** Three further full runs were captured to file. `build-race.test.ts` passed in all three, so that failure is still undiagnosed and still open. Run 3 failed for an unrelated reason that IS now diagnosed, and it is not a Fiscus defect: one `node --test` worker died before its file's tests ran with `CompileError: WebAssembly.Module(): Compiling function #352 failed: invalid branch depth: 4096`, thrown from `node:internal/deps/amaro/dist/index` -- Node v24.18.0's own TypeScript type-stripper failing to compile its SWC WebAssembly module under full-parallel load. `test/claim-profile.test.ts` was reported as `'test failed'` with no assertion and three of its tests never ran; the suite reported 1,747 tests where the other two runs reported 1,750. No Fiscus frame appears anywhere in the stack.

**It happened twice more in the same session, in two more shapes, and the count now matters more than the first instance did.** A later full run failed twice at once, both non-Fiscus: `test/billing-cli.test.ts` spawns a real CLI child, that CHILD died with the same amaro WASM compile error in a different function (`not enough arguments on the stack for f64.add`), and the parent test faithfully turned the child's stderr into an assertion message -- so the failure was reported against a billing assertion that never ran. In the same run `test/claim-support.test.ts`'s worker died on a V8 `Fatal error ... unreachable code` with a native stack and no JavaScript frame at all. The immediately following run was clean: 1,769 tests, 1,765 pass, 0 fail, and zero WASM or fatal-error lines. **Three infrastructure faults in six full local runs, in three different shapes; zero on CI across three operating systems in the same period.** A FOURTH shape was captured at D-187, and this time the log was retained on purpose: a full run went red on `test/causal-inference-ledger.test.ts` with `CompileError: WebAssembly.Module(): length overflow while decoding body size`, again thrown from `node:internal/deps/amaro/dist/index` with no Fiscus frame anywhere. Two consecutive runs before it had gone red on three DIFFERENT files, and the tell is arithmetic rather than textual: the suite reported **1,826** and **1,842** tests where a clean run reports **1,852**, because a worker that dies takes its file's whole test count with it. **The reported TOTAL is the cheapest discriminator between an assertion failure and a dead worker -- check it before reading the failure message.** All four files pass in isolation and the following full run was clean at 1,852 / 1,848 / 0 fail.

**The machine, measured rather than assumed.** `os.availableParallelism()` is 12 here, so `node --test` runs up to twelve worker processes, each instantiating Node's own SWC WebAssembly type-stripper, while several test files additionally spawn real CLI children that each do the same. 31.8 GB total with 12.9 GB free at the time of measurement.

**The hypothesis, named as a hypothesis, and what would settle it.** Peak memory and JIT pressure from that fan-out is the obvious candidate, and bounding `--test-concurrency` is the obvious lever -- it weakens no assertion. It is still not pulled, and now for a better reason than the count: **it cannot be shown to work.** Runs pass most of the time either way, so a single green bounded run proves nothing, and shipping a knob documented as a remedy would assert something unmeasured. What would settle it is a rate comparison -- N full runs at the default against N at a bounded concurrency, faults counted -- which is roughly an hour of wall clock per arm at three and a half minutes a run, and is a real measurement rather than a guess. Until then the default stands and the faults are recorded here with their exact stacks.

**What this already changes is how a red local run must be read.** A worker killed by an infrastructure fault is reported in exactly the same shape as a failing assertion -- a bare file-level `'test failed'` -- and when the fault is in a spawned child it is reported in the shape of a NAMED assertion in an unrelated subsystem, which is worse. So: before treating a red run as a Fiscus finding, read the captured output for a stack with no Fiscus frame in it. Both of the diagnosed defects above were found because a red run was taken seriously; these three are the reason a red run must also be READ. **Never conclude from a red run alone; never conclude from a green one either.**

## Recently closed, do not redo

`WP-D06` both halves (drift silence and alert coverage, CLI and browser), `WP-H05` supply-chain audit script and, at D-163, its `--ignore-scripts` and three-domain `prepublishOnly` remainders, `WP-I04` four accessibility defects and, at the five commits `aa8c551`..`c5319c3`, all five of its named remainders, `WP-E06` inference ledger and precision planning, `WP-D05` measurement backing registry, `WP-I05` boundary declaration, retention consequence and receipt-chain coverage, `WP-D07` surrogate bridges, `WP-F05` decision assurance levels, `WP-R01` evidence abstract interpretation. This round adds D-159 through D-166: the dead `validTime` on surrogate bridges (`WP-D07`), the inference ledger's one-message-for-three-conditions (`WP-E06`), both revocation-envelope linkage directions and the effective-time split with the disclosure its own fix cost (`WP-R07`), the rollup builder minting bodies the receiver refuses and the observation-window overlap (`WP-C06`), the publish gate plus CI lifecycle scripts (`WP-H05`), the causal study list's dropped rows and basis-free `latestAnalysis` (D-165), `WP-I06`'s first work -- a gate holding every documented command, flag, npm script and port to what the CLI and config actually carry (D-166) -- and `WP-I03`'s first work, holding the GUI parity map to the CLI's own dispatch after finding six commands with no capability row, which had made the coverage fraction wrong in BOTH directions at once (D-167). Both packets move off `NOT_STARTED`. D-140 through D-151 in the decision log carry the counterexample, the fix, and — in every case — what the fix does not establish.

One defect class now runs through eight of those: **absence of a result reported as a result.** A quiet drift e-process; six structurally dark alert channels; a validation field that passed by not matching one string; a deleted receipt history; an empty receipt chain verifying vacuously and printed in green; a `proxy_validated` rung asserting a relationship nobody had recorded; a snapshot list that was empty because nothing CAN be written, printed as though nothing had been. Every one reported "nothing found" where the honest answer was "nothing could have been found." A ninth arrived from an unusual direction (D-161): closing the effective-time hole in `RevocationProjection` moved a decision certificate whose evidence had a withdrawal already booked from `invalidated` to `valid` with an empty `invalidatedBy`, because the new `pendingIds` set was consumed by nobody. The old answer was wrong; the new one was silent, and silence reads as an established absence. **A fix can create an instance of this class.** Whenever a repair splits one state into two, check what reads the original -- if it reads only one half, the other half's information has been deleted rather than refined. A tenth and eleventh arrived together at D-165, on one payload: a study list that filtered rows out without saying so, and a `latestAnalysis: null` that meant "none can be written" while reading as "none has been". A twelfth turned up inside the tool built to catch it (D-166): a documentation sweep whose fence pattern required a bare newline matched ZERO fenced blocks in the CRLF-terminated `README.md`, and reported no violations over the repository's most important operator document without having read a line of it. Only its own corpus-size assertion noticed. **A narrowing that was honest when a record did not exist becomes an overclaim the moment it does, and nothing re-reads a docblock when a new record lands** (D-175): `meteredClaimSupport` said coverage "says nothing about whether the ledger sees every request the organisation made, which no local evidence can establish" -- true of traffic that never reached Fiscus, false of rows Fiscus deleted itself once D-170 made the deletion a record. **When you add a record, search for the sentences that were true only because it was missing.** And: **a sweep without a corpus-size assertion is indistinguishable from a sweep that matches nothing** -- write one into every sweep, including the ones already here. A thirteenth arrived in a new shape at D-170, and it is the most operator-visible one yet: `fiscus prune` deleted request rows and recorded nothing, so `fiscus guide` read an emptied ledger as **"no traffic yet"** with the metering step marked NOT DONE -- telling an operator who metered for months to go and configure a proxy they had configured already. Not an empty list rendered as a clean bill this time, but a journey step marked incomplete because the evidence that it was complete had been deleted by policy. **When a deletion is possible, the absence it produces must be distinguishable from an absence of events** -- and the fix has three states, not two, because a ledger pruned before the record existed is UNKNOWN and must not be backfilled. A fourteenth arrived immediately afterwards, in the money path, and it was found by searching the class rather than by waiting for a report (D-173): `fiscus prune` deleted request rows from inside a reconciled period, the local total silently became a strict undercount, and a residual that had been **-$2.00** with `none_local_estimate_exceeds_provider` -- the honest state saying no upper bound on off-path spend survives -- became **+$4.00** with `upper_bound_conditional`. A deletion RESTORED a claim the surviving evidence refutes, and re-labelled Fiscus's own metered traffic as spend the provider charged for and Fiscus never saw. The reasoning that fixed it is worth more than the fix: with `L' = L - D` for an unknown deleted amount `D >= 0`, the computed residual is `R' = R + D`, so `R' < 0` still implies `R < 0` and a refutation SURVIVES truncation while an upper bound does not. **Withdraw the half of a claim that deletion undermines, not the whole claim** -- refusing to classify any pruned run would have been simpler and would have thrown away a sound conclusion. It is the most durable finding of this program: fourteen instances, in eleven different subsystems, none of which shared code with another, and three of the last five were CREATED by work meant to close others -- one of them inside D-173 itself, where the browser's if-chain would have rendered the new state as a run that PREDATES the field it carries, until the chain was replaced by a `Record` the compiler must see filled. A fifteenth followed immediately, found the same way and in the surface an operator is most likely to act on (D-174): `fiscus usage --days 90` over a ledger whose tagged sessions retention had deleted printed **"No sessions without code signals in range. Tag sessions with X-Fiscus-Session-Id to measure them."** -- a false claim about the world with an ERRAND attached, sending someone to instrument a system they had already instrumented. **An empty list that also gives an instruction is the worst form of this class**, because the false absence is not merely displayed, it is acted on, and the operator concludes from the still-empty numbers that their instrumentation does not work. A sixteenth is the sharpest of all of them, because the number it moves is a DENOMINATOR (D-176): a commit with $6.00 of spend inside its eight-hour attribution window reports `attributedCostUsd: 0` after a prune, while the unit itself survives because it comes from git history. `costPerHundredLines` divides by that, `realizedSpendShare` divides by that, and the RoI ratio divides value by that -- so **a deletion does not merely hide spend here, it makes AI look free and then makes the return on it look better.** Every earlier instance misreported coverage, a bound or an absence; this one moves a headline in the flattering direction as a side effect of a privacy setting. **When hunting this class, rank the candidates by whether the absent number is a DIVISOR.** A seventeenth followed within the hour by applying that rule mechanically (D-177): the frontier divides `modelCostUsd` by unit count to decide which model to RECOMMEND, so a model whose units partly lost their spend reads as cheaper by exactly the deleted amount -- measured at **$2.33 per unit against a true $3.50**, roughly doubling the reported headroom for switching. **The obvious version of that counterexample was already refused** by an existing `costPerUnit > 0` filter, and the first test written for it PASSED against the unfixed tree; only partial contamination is live. That is the lesson to carry: **when a fix is aimed at a class rather than a report, the first counterexample is often the one the code already handles, and a passing first test is a signal to look for the narrower case rather than to conclude the defect is absent.** An eighteenth and nineteenth arrived together at D-179, in the same sentence, printed by two different commands. `fiscus prune` deletes PROPOSAL rows on a separate and much shorter retention policy, and a report over a repository whose proposals it deleted went from `firstPassAcceptance: 1` to null and printed **"Acceptance uninstrumented: no proposals captured (e.g. streaming-only)."** Proposals were captured; Fiscus captured them and then deleted them. **The store already knew** -- `retentionFloor()` has returned `proposalsPrunedBeforeMs` since the record was built, and the only reader of it anywhere was one line in a test. The nineteenth is the same sentence in `computeUsageRoI`, which filters its population to sessions with NO captured proposals and then printed that note on EVERY run: an instruction that cannot be acted on, because capturing proposals cannot move a number computed over a population that excludes proposal-bearing sessions by construction. **The D-174 lesson has a sharper form: an empty list that gives an instruction is the worst shape of this class, and an instruction the reader CANNOT ACT ON is the worst shape of that.** The same packet found the inverse risk and refused it: `clearProposals()` -- the dashboard's delete-everything privacy control -- recorded nothing at all, and the fix records with a boundary of NOW only when a row actually went, because that boundary marks every past window truncated. **A refutation may be HIDDEN by a deletion; it must never be INVENTED by one.** Two more rules fall out. **When a claim names a CAUSE for an absence, enumerate the causes that produce the same absence and check which ones the code can actually see** -- there were three here and it could see none. And **check the other callers of the sentence, not just the other instances of the bug**: the third caller, `computeFrontier`, builds the same RoI and discards its notes, which is a fact that had to be checked rather than assumed before it could be left alone. A twentieth is the same class crossing the machine boundary (D-181): `fiscus team push` minted a SIGNED rollup reading `coverage=complete` while carrying one intact work unit at **$0.00 that cost $6.00**, because `RollupBodyV1.coverage` -- a field the protocol already had, already validated, and already returned to the receiver -- was omitted at both mint call sites and **defaults to `'complete'`**. **The worst shape a never-wired mechanism can take is one whose unwired default is the most confident value the field can hold.** Two rules from it. **Before pricing a repair as a protocol change, check whether the protocol already has the field**: this work had been deferred on an estimate that was wrong by the whole of its reason. And **a deletion whose consequence lands only on the NEXT recomputation is a defect with a fuse on it** -- a prune does not rewrite persisted snapshots, so nothing was wrong at the moment of deletion and everything was wrong after the next `realize`. Looking only at the instant of the deletion would have cleared this. A twenty-first is the class on the surface that exists to prevent it (D-182): `alertCoverage`'s whole purpose is to say why each channel could not have fired, so an empty alert list means something -- and its spend-spike row said **"no prior active day exists YET, so there is no baseline to exceed"** for a ledger whose twenty-six active days retention had deleted. **When a surface's job is to give reasons, a wrong reason there is worse than a wrong reason anywhere else**, because it is the thing a reader consults precisely when they distrust the numbers. Beside it, the quieter half the frontier note HAD predicted: the p90 baseline went from **$3.16 over twenty-five samples to $3.39 over two** while still printing "your p90 day". The note had sized that surface, classified it as "a statistic quietly computed over less data, not a false absence", and filed it -- and **the sizing itself is what hid the simpler defect sitting next to it. A triage note that classifies a surface as one kind of problem can stop the search that would have found the other kind on the same surface.** The alert still fires on a narrowed baseline, applying D-173's rule: withdraw the half of a claim deletion undermines, not the whole claim -- and assert no direction, because deleting the oldest days can move a percentile either way. A twenty-second was found by MECHANICALLY sweeping the CLI's absence sentences rather than by following the thread (D-183), which is worth noting because the thread had run out and the sweep had not. `fiscus alloc` over a sixty-day period holding $42.00 -- $32.00 matched by a rule, $10.00 matched by nothing -- reported `76.2% allocated`. After a prune whose boundary sat INSIDE the period it reported **`100.0% allocated, $0.00 unallocated`**. **The deleted part was the evidence AGAINST the rules**, so the coverage figure moved 24 points in the flattering direction: not merely a shrunken divisor, but a divisor shrunk disproportionately on one side. **And `conserves` stayed TRUE** -- the run's own integrity check, the one the CLI refuses to print without, passes over a truncated input because it conserves what survives. **A correct invariant over a truncated input certifies a wrong number, and the strongest reassurance on a screen is the one most worth qualifying, because it is the one a reader stops at.** The persisted `result_json` and its kernel issuance now carry the coverage too, because the printed line is not the durable claim. A twenty-third is the class turned on the program's own instruments (D-184), and it was found by CHECKING A RANKING rather than by trusting it. `issuance-map.ts` says authority class "says nothing about whether anything RUNS it" and hands that question to `reach`; the check underneath walks the IMPORT graph. `alloc.exactRun` was declared `reach: 'product'` and passed, because `src/store/db.ts` imports it -- while the only mentions of `saveExactAllocationRun` anywhere in `src/` are its own definition and that forwarder. **A boundary on the Exact Money path was declared live in the product and is invoked by nothing.** `reach` decides QUEUE POSITION, so an overstated one misdirects exactly the work the map exists to direct -- and it had already done so: the previous packet's frontier note ranked the exact allocation path FIRST on the strength of that field. **A ranking is only as good as the field it reads, and the field that decides queue position is the one most worth checking against its own definition.** The remedy is a third state (`imported_uninvoked`), a declared invocation entry point per boundary, and a check that is honestly the weak half of the pair: **a mention is not a call, so it can prove that nothing invokes a boundary and never that something does** -- which is the right way round for a gate, because it fails only when nothing in the product so much as names the entry point. Four of seventeen were traced and the test asserted that count out loud, which is D-166's corpus-size rule: a sweep covering four of seventeen without saying so reads as coverage. **All seventeen are now traced (D-188), the other sixteen held, and the count became a gate** -- `invocation` is required on every boundary, so a new one cannot arrive with its reach inferred from the import graph the way that one did. **A partial sweep's count is a placeholder for a gate.** The completed sweep then found what the partial one had caused: D-184 corrected the field in the code and left `docs/program/ISSUANCE-MAP.md` publishing `alloc.exactRun` as `product`, because the test over that document compared the SET OF IDS while the document claimed to be its readable projection. **A projection test that checks membership certifies the document's existence, not its content, and the columns nobody checks are where a corrected record goes to rot.** Cells are checked now; the prose count above the table had gone stale twice and is rewritten to be re-derivable from the table beneath it. A twenty-fourth reaches the most expensive advice the product gives (D-185). `fiscus billing costs coverage` exists to answer one question before an operator spends anything -- is the local side ready, or would a reconciliation come back empty -- and the same command prints **"READ THIS BEFORE GETTING A CREDENTIAL."** A prune inside the provider's observed period turned `Declared route 10 live proxy request(s), $180.00` into **`0 request(s), $0.00`**, with the five-item blocker list byte-for-byte identical. **Rank instances of this class by what the advice COSTS to follow**, and minting an Admin credential against a real billing account is the top of that list. Two method notes came out of it. It was reached by tracing three absence SENTENCES individually, as frontier item 12(b) demanded -- and all three turned out sound; the defect was in the report one of them GUARDS. **Trace the sentence, then trace what the sentence guards.** And the fix is deliberately NOT a sixth entry in `blockers`: that tuple's meaning is "these five hold always", and **a list whose meaning is "these always hold" is not a place to put something that sometimes holds, however convenient the rendering** -- which is the same collapse this program refuses, one level up from the numbers. A twenty-fifth is the class in its strongest form, because the false reading was WRITTEN DOWN as the intended one (D-186). `ReconciliationReadiness.coverage` is null on an empty result set, and two docblocks told every consumer what that null meant: **"Null when no OpenAI spend exists at all, so there is nothing to warn about"** in `src/billing/reconcile.ts`, and "'no data', not 'no coverage'" in `src/dashboard/shared-types.ts`. A ledger with **$180.00** on the declared route returned that null after a prune. Every earlier instance had a comment that was SILENT about what an absence licensed; here the licence was explicit. **When a docblock tells a reader what an absence MEANS, it is a claim about the world and takes the same scrutiny as a printed number** -- and it is more dangerous than one, because a consumer writes a branch against it and then never re-reads it. Which is exactly what both consumers did: the CLI's `printReadiness` and the browser's `readinessPanel` gate the entire **"READ THIS BEFORE GETTING A CREDENTIAL"** block on `coverage` being non-null, so **a deletion turns the warning OFF at the moment the thing it warns about became certain.** **A deletion that silences a warning is worse than one that moves a number, because nothing on the screen is wrong -- there is simply nothing there, and an operator reads the absence of a warning as an all-clear.** The fix's own contribution is the predicate: the query behind this coverage has **no period bound at all**, so the honest condition is whether a prune is ON RECORD rather than whether one bit into a window -- including a prune that deleted nothing. **Derive the predicate from the query, not from the family of fixes it belongs to**; borrowing the previous packet's window comparison would have been a form that does not fit. A twenty-sixth is a NEW MECHANISM for the same class, and it was reached by fixing the ENUMERATION rather than by finding another surface (D-187). The sweep had been run over retention; `grep "DELETE FROM" src/` returns **five** statements and only three had ever been swept. **A sweep is only as complete as the enumeration it was run over, and "retention" was the wrong enumeration -- the class is DELETION, and retention is one caller of it.** `DELETE FROM project_aliases` traced sound (labels are fixed at query time, raw rows are never rewritten). `DELETE FROM active_provider_scope_routes` -- `fiscus billing scope clear`, a supported act -- turned ten proxy requests carrying the declaration and **$180.00** into `on $0.00 / imported $0.00 / off-scope $0.00`, with all ten rows still counted by `COUNT(*)`. The mechanism is SQL three-valued logic: the off-scope bucket is written as `NOT (<on-route>)`, the declaration id and its bound parameter are both nullable, and `x = NULL` is NULL -- so `TRUE AND NULL` is NULL, `NOT NULL` is NULL, and the row leaves both arms at once. **A predicate and its negation stop partitioning the moment either can be NULL, and SQL reports that as a smaller number rather than as an error.** Every earlier instance in this program lost a number to a DELETED ROW; this one loses it to an UNCOMPARABLE one, with the row still present. And it silences the same credential warning D-186 restored, by the opposite route: **D-186 turned the guard off by emptying the report, this turns it off while the report still looks populated, which is the harder one to notice.** The second half is hard rule 1 applied to a figure that had been exempt from it -- three buckets computed relative to one declaration id, with no trace of which, so "off-scope because these rows carry a different declaration" and "off-scope because there is no declaration to be on" were the same value and both surfaces printed the first, which after a clear is false. Search for the class, not the case -- and search first wherever an emptiness is followed by advice, or divided by, or is the only thing standing between a reader and an expensive errand.

A third class has now been seen once, and it is recorded because it attacks this file rather than the product: **a paired description where one half is gated and the other is prose.** `src/epistemic/issuance-map.ts` is read against the source tree and the import graph by a test; the account of it in `AUDIT-REGISTER.md` and in this file's own frontier was read by nobody, and had been wrong for two closures — naming three `unmigrated_authority` boundaries where the map had one, and ordering the next round's work on the difference (D-169). The general rule: **when a fact is stated in both a gated artifact and a program record, state it in the record in one fixed form and check it**, or the record becomes where the falsehood accumulates. Prose that explains history is fine and should stay prose; a fact the next executor will act on is not prose.

The second recurring class is narrower, just as reliable, and is the dominant one: **a mechanism built and never wired.** `assessMeasurementFitness` had no caller before D-146. `measurementRegistry`, `surrogateBridgeRegistry`, the WP-E06 inference ledger and the WP-F05 assurance gate still have none — and for the assurance gate the reason is sharper than oversight: `decision.certificate` has no production caller at all, so there is no surface to wire it to. A gate that no product path passes through is an available discipline, not an enforced one, and every record here says so explicitly rather than letting PARTIAL imply otherwise.

**This is the highest-value work in the program, and three rounds of it have now been done.** D-152 moved the money-axis half of `abstract.ts` into `assessDerivationLegality`, which is what `appendDerivationWithinTransaction` actually runs, so the one collapse this repository's first line forbids is refused at the boundary that persists. D-153 gave WP-D07 its first product boundary: `src/causal/epistemic.ts` now cites a resolvable measurement model through a declared surrogate bridge and takes its rung from what they earn, which moved Fiscus's own causal claims down from `proxy_validated` to `proxy_unvalidated`. D-157 gave WP-E06 its first callers at all: `causal_inference_acts` persists the act chain, `Store.reportCausalStudy()` is the reporting boundary, and both surfaces an operator reaches go through it, so a second reading of a study no longer presents itself as the first. All three were downward moves in what the product asserts — a weaker rung, a refused re-basing, a withheld conclusion — which is what wiring an honest rule to a real caller looks like. Wire, do not build.

Of the six unwired mechanisms this round began with, three now have callers. `measurementRegistry`/`surrogateBridgeRegistry` are reached only through `src/causal/measurement.ts` — one boundary resolving its own reference, not a repository-wide registry. `analyzeDerivationChain` and the WP-F05 assurance gate still have none, and for the assurance gate the reason is structural: `decision.certificate` has no production caller at all, so there is no surface to wire it to. `assessMeasurementBacking` is a fifth: D-168 enforced measurement backing at the append boundary WITHOUT it, because the containment rule it enforces needs no registry and the resolution rule that function performs needs one nobody can assemble yet. That is worth saying plainly rather than counting D-168 as wiring it — the mechanism is still unwired, and a nearby boundary now does a weaker thing that does not need it.

## What the parallel lanes were actually worth, measured

Six delegated Sonnet lanes ran this round. Every one of them started from a worktree branch that was **hundreds of commits behind** `gpt56/magnum-opus-reconstruction` -- one was 344 behind and predated `docs/program/PACKET-INVENTORY.md` entirely. Three reported root suite totals of 615, 625 and 1,688 against a tree that runs 1,738. That is not a lane failing; it is the worktree mechanism handing each agent a stale base, and it is the single most important thing to know before delegating again.

What that cost, concretely, and why integration could not be a merge:

- **Lane C (WP-C06)** reported three gaps closed. Measured against the real tree, GAP 1 was already half-done (the server has called `validateRollupBody` for some time) and GAP 2 was already closed at D-101, and closed MORE strongly: a scoped push is refused outright, where the lane proposed labelling it with a boolean `complete` flag -- on a branch that already carries a three-valued `RollupCoverage`. Only the remaining half of GAP 1 and all of GAP 3 were real, and both were re-derived here (D-162, D-164) rather than cherry-picked.
- **Lane A (WP-R05/R07)** was accurate and its two real gaps were cherry-picked cleanly. Its WP-R05 finding was that the gap was already closed -- established by relaxing the constructor and measuring, which is the right way to retire a stale remainder.
- **Lane F (WP-H05)** was accurate about both gaps but wrong about this tree: it stated `pretest` runs a `--web` partial build, which was true of its base and is not true here, and it counted three `npm ci` invocations where there are five. Its tests were imported, corrected, and one of them dissolved into the existing supply-chain auditor rather than kept as a second mechanism.
- **Lane E (WP-I04)** was accurate, and its five fixes were cherry-picked after re-verifying RED by reverting each source file in turn.

**Every lane's RED was re-verified in this tree before its work was kept.** That is not ceremony: two of the six lanes' reports were materially wrong about the current tree while being internally honest about the tree they saw. Delegation bought real breadth here -- six packets' worth of counterexample-hunting in parallel -- and bought no integration. Budget for that: the integrator re-measures everything, and on a fast-moving branch that is most of the work.

## Next executable frontier

Highest value first, each stated as the boundary that is missing rather than as an area to look at:

1. **Enforce what WP-D05 and WP-D07 only made possible — one boundary is done, the general rule is not.** The causal claims now resolve their own model and bridge (D-153), but `claim()` still accepts ANY non-null `measurementModelRef` without resolving it, so the kernel's own gate is still the honour system and any other issuance boundary can repeat exactly the defect D-153 fixed. **The ledger-append half is done at D-168 and the `claim()` half is now known to be harder than this item assumed.** `assertClaimWithinItsEvidence` refuses a claim whose `measurementModelRef` no cited Evidence declares — a containment check that needs no registry, in the pattern D-152 set — and it found a real instance: both causal claims cited the protocol's quality model while both cited evidence records carried `null`. What remains is RESOLUTION, and it does not belong in `claim()` after all: the only reference the product writes names a model SYNTHESIZED from a stored protocol, so no static registry can hold it and a resolver would put the kernel's pure constructor behind a database. The open question is therefore where a resolving boundary can live — `Store`'s issuance path is the candidate, since it already has the database — and it is a design question, not a missing line. No repository-wide registry of Fiscus's models is assembled anywhere, and the evidence side can name a model that does not exist just as the claim could.
2. **The estimator calls that remain outside the reporting boundary.** D-157 routed the two operator-facing surfaces through `Store.reportCausalStudy()`, but `issueCausalStudyToKernel` still calls `estimateCausalStudy` directly — that one is an issuance rather than a report, so it may be RIGHT that it does not count as a look, and nothing has decided which. That undecided question is the remaining work here, not a straightforward miss. (`saveCausalAnalysis`'s dead path was the other half and is closed: D-158 made it refuse by name and made its empty snapshot list state why it is empty.)
3. ~~**`latestAnalysis` on the dashboard study rows is still a bare null.**~~ **Closed at D-165**, and it turned out to be two silences rather than one: the row's null now carries `analysisBasis` saying whether an analysis CAN be written, and `studiesOmitted` reports the version-2 studies the list filters out — which, with one version-1 study present, were reported nowhere at all. The payload contract change did not have to wait for the version-2 projection after all; stating an omission is not the same as projecting it.
4. ~~**AII-036's three `unmigrated_authority` boundaries.**~~ **Two of the three were already closed when this item was written, and the item was wrong for two rounds (D-169).** `causal.qualification` and `causal.estimate` are `kernel_primitive` in `src/epistemic/issuance-map.ts` and their own map notes say when and how they stopped being unmigrated: `causal.issuance` binds the conclusion to the randomization through a Derivation. What remains is `decision.certificate`, which the map has as `unreached` — so it is the SAME remaining work as items 5 and 6 below and as AII-025/AII-026: **a product path that consumes a decision certificate.** That is now the single highest-value open boundary in this program, and it is not a missing line. Wiring it naively would refuse every budget recommendation, because the gate requires randomized causality for a `changes_spend` consequence and no Fiscus claim earns that (D-153). So the executable question is which consequence class Fiscus's advisory surfaces belong to, and whether an advisory that changes no spend by itself is `changes_spend` at all. **That is a product-behaviour decision: it is not on directive §17's forbidden list, but it changes what the product refuses to print, so flag it and do not decide it unilaterally.** Boundaries still classified `unmigrated_authority`: `decision.certificate`. That line is checked against `src/epistemic/issuance-map.ts` by `test/program-record-issuance-classes.test.ts`, which requires this file and `AUDIT-REGISTER.md` to carry it and to agree with the map. The map is the gated source for every other classification, and no total is stated here.
5. **AII-025's missing gate.** The observational frontier's label is honest; no surface yet refuses to accept an observational separation as an input to an action that changes spend.
6. **The remaining AII-002 negative claims** — no provider charge, no duplicate, no policy violation — still carry no completeness requirement, and nothing yet emits a refuting witness.
7. **The chain half of `abstract.ts` is still unwired.** The money axis is done — D-152 added a `monetary_rebasing` witness kind and `assessDerivationLegality` now requires it, so the estimated-to-billed re-basing is refused where derivations persist. `analyzeDerivationChain` is still called by nothing, so a conclusion several merges downstream of its leaves is still bounded by nothing, and `BASIS_DERIVATIONS` is still empty, so neither rule knows which re-basings are sound. The next step is a caller that bounds a whole chain, not another rule.
8. **DAL2 is unreachable for quality-based claims, and that is a finding rather than a bug.** `src/decision/assurance.ts` requires at least `proxy_validated`; after D-153 no Fiscus claim about quality earns it, because nothing in this repository records an empirical association between a quality metric and the quality construct. Registering one — an independent measurement of the construct, against which a metric can be compared — is the work that would change it. Lowering the bar is not.
9. **The new witness is a declaration, not a proof.** Nothing checks that a `monetary_rebasing` witness's evidence actually supports the re-basing it licenses, only that someone recorded one — the same gap the witness registry closes for other kinds, and the same one `surrogateBridgeRegistry` was built for and nothing calls.

10. **A file that states a rule about another file, and does not read that file, is the general form of what D-167 found — and the class was searched.** `registry.ts` claimed for its whole life that adding a CLI verb without a row was the change it existed to make awkward, and nothing compared the two files, so the claim cost nothing to violate. The three nearest candidates were checked and all three are already gated: `src/dashboard/shared-types.ts` against the canonical contracts (`test/dashboard-shared-types.test.ts`), `DASHBOARD_API_CONTRACTS` against the routes the server actually registers, including cardinality (`test/dashboard-route-contract.test.ts`), and `package.json`'s `files` against the launcher's own sibling imports (`test/package-surface.test.ts`). So the parity map was the open instance among the boundaries inspected, not the first of a queue. The search was by inspection of the dashboard and packaging boundaries and is **not** exhaustive: no mechanical sweep exists for "a docblock asserts an invariant across files", and building one is a real packet rather than a note.

11. **The retention sweep has NO live instance left that has been found, four sub-items closed (D-178 (a) partly, D-179/D-180 (d), D-181 (b), D-182 (c)), and three named remainders that are latent or finer-grained -- and D-179 established that it had been run over ONE STREAM when there are two.** `fiscus prune` deletes request rows AND proposal rows, on separate policies with separate boundaries, and until D-179 every packet in this sweep read only the request boundary -- `proposalsPrunedBeforeMs` had existed since the record was built with no production reader at all. `clearProposals()`, the dashboard's delete-everything privacy control, recorded nothing whatsoever. **Before sweeping a class over a surface, enumerate the STREAMS the surface reads; a sweep that is thorough over one of two is not a thorough sweep, it is a half-swept surface that now looks finished.** Every surface that turns a count into a claim has to be able to tell that emptiness from an absence of events -- seven instances found so far (D-170 guide, D-171 window commands, D-173 reconciliation, D-174 usage, D-179 acceptance twice, and the store record they all read). **(a) `/api/overview` is done (D-175), `/api/value` is HALF done (D-178), and what is left of it is narrower than it was.** The overview carried `coverage: 'complete'` for a window it had deleted rows from; the metered claim's coverage axis now reads the boundary, the note says why, and the Metered screen states it beside the total rather than only in the Claim Inspector drawer. D-178 closed the realization half: `Matured` now DECLARES `spendWindowTruncatedUnits` and `spendWindowUnknownUnits`, which the server had been sending whole since D-176, and the value screen renders both beside the return they qualify. **The lesson from that packet is where its RED had to be taken.** The runtime test -- boot the server against a pruned store, assert both counts are numbers -- PASSED against the unfixed tree, because the fields really were in the JSON; the defect was that `shared-types.ts` did not declare them and the browser compiles against the declaration, so no screen was permitted to read them. The counterexample was therefore taken at the browser compiler: the rendering was written first and produced six `TS2339` errors. **When the class is 'built and never wired', a runtime test can pass over the exact defect -- write the CONSUMER first and let the compiler be the counterexample.** What is left: `/api/value` also sends `usage` whole and `ValuePayload` declares no `usage` field at all, so `UsageReport.retention` (D-174) still reaches no screen through this route; and `ValuePayload.frontier` declares `modelSwitches: Array<{ confidence: string }>` and nothing else, so D-177's two counts are unreadable the same way. **(b) `fiscus team push` was measured, held, and is CLOSED at D-181 -- and the prediction written here was half right, which is the useful part.** It said the repair would be "a client refusal or a coverage field in the SIGNED body -- the second is a protocol change with a compatibility story". The coverage field was ALREADY in the signed body: `RollupBodyV1.coverage`, validated by `validateRollupBody`, normalized by `normalizeRollupCoverage`, returned to the receiver by `verifyRollup` -- and both mint call sites omitted the argument, so both took the default, and **the default is `'complete'`**. So there was no protocol change to weigh; there was an unwired parameter whose unwired value was the most confident thing it could say. **Before pricing a repair as a protocol change, check whether the protocol already has the field** -- the cost estimate that would have deferred this work was wrong by the whole of its reason. Measured: one intact work unit at $0.00 that cost $6.00, in a signed body reading `coverage=complete`. **And it takes TWO steps, which is why looking at the moment of deletion misses it**: a prune does not rewrite persisted snapshots, so nothing changes until the next `computeRealization(..., persist: true)` -- which `fiscus realize` and every dashboard load perform -- re-derives the unit against the pruned ledger and bakes `attributedCostUsd: 0` into the snapshot the rollup reads. **A deletion whose consequence lands only on the next recomputation is a defect with a fuse on it.** Still open beside it, and stated at the strength the evidence supports: `ProjectTaskStratum` carries `units`, `realizedUnits` and `costUsd` and no coverage of its own. They are NOT unqualified -- they ride in the same signed body, the body-level `partial` covers them, and `team-server/src/store.ts` reads `body->>'coverage'` with a COALESCE to `'unknown'` and combines it into the aggregate status, which was read and confirmed rather than assumed. What a receiver cannot do is tell WHICH stratum lost spend, so a fixed-basket comparison is distorted in a direction it cannot locate. **That is a precision gap, not a false claim, and the first draft of this line called it a false claim** -- which is this program's own failure mode turned on its own record, and is worth leaving visible here. **(d) The proposal stream is now open where the request stream was, and it is a smaller job than (b) or (c).** D-179 closed the Acceptance lens and the `clearProposals` record. Three candidates were enumerated and RANKED BY WHETHER ANYTHING READS THEM, which is the check that separates a live false claim from a latent one. **Ranked first, and it is live -- CLOSED at D-180: the `proposed` gate's own detail string.** `computeRealization` rendered `'no complete proposal captured'` whenever `hadProposal` was false and capture coverage was neither `truncated` nor `legacy_unknown` -- and a proposal prune produces exactly `'unknown'`, measured. So the gate ladder, the structure an operator reads to ask why a unit did not realize, stated the same false cause D-179 removed from the lens note. **Two lessons from that packet, both general.** The rung's VERDICT was already correct -- `unknown`, which is exactly right once the evidence is gone -- and only the detail string asserted a cause; every epistemic review of that ladder passes, which is precisely why nobody read the sentence. **Wherever a state is correct and a string explains it, the string is unreviewed.** And its ordering guard PASSED against the unfixed tree on its first fixture, because a boundary outside the window deletes the truncated captures too, so the two causes could never be live together and there was nothing to order; the overlap needs a boundary INSIDE the window. **A guard that passes may be guarding an impossible state, and a guard over an impossible state is worse than none, because it certifies an ordering nothing exercises.** The `judge` payload builds the same `'unknown'` for a session with no surviving proposals, which is the correct sentinel and not a false claim -- checked, not assumed, and left alone. **Ranked last despite being the divisor: `proposalCoverage`** (units with a captured proposal / total units) is a ratio whose numerator a proposal prune empties while its denominator, coming from git, survives -- the D-176 shape one stream over -- but it is rendered by NO view and NO CLI surface, so it is latent rather than live. It is on the wire and declared; that is all. Also latent, and by the strictest measure: the contribution-evidence path builds candidates only `if winProposals.length > 0` and so produces `undefined` for a deleted window -- and `contributionEvidence` has NO consumer anywhere in the repository, not even a payload declaration. **So after D-180 this stream has no found LIVE instance left and two named latent ones. That is a weaker claim than "swept", and it is the one the evidence supports.** **(c) CLOSED at D-182, and the prediction written here missed the sharper half entirely.** It said the spend-spike baseline was "a statistic quietly computed over less data, not a false absence". The quiet narrowing was real and measured -- p90 **$3.16 over twenty-five samples became $3.39 over two** while the alert still said "your p90 day". But the FALSE ABSENCE was there too, and on the worst possible surface: `alertCoverage`'s spend-spike row read **"no prior active day exists yet, so there is no baseline to exceed"** for a ledger whose twenty-six active days retention had deleted. **That is the false-cause defect on the one surface whose entire job is to say why a channel is dark.** **A triage note that classifies a surface as one kind of problem can stop the search that would have found the other kind on the same surface** -- item (c) had been sized, filed as "needs a different remedy", and the sizing itself was what hid the simpler defect sitting next to it. The alert still fires on a narrowed baseline, by D-173's rule: withdraw the half of a claim deletion undermines, not the whole claim.

12. **The retention sweep is now a SWEEP rather than a thread, and this is what it has left.** D-183 was found by mechanically reading the CLI's absence sentences rather than by following the previous packet, and it produced the sharpest money-side instance so far -- `fiscus alloc` moving from `76.2% allocated` to **`100.0% allocated, $0.00 unallocated`** because the deleted rows were disproportionately the ones no rule matched. Continue the sweep the same way, and in this order, because each was checked for a READER before being ranked: **(a) `allocatePeriodExact` was ranked first here on a field that lied, and D-184 corrected both.** The ranking said "read and unswept" and read that off `issuance-map.ts`'s `reach: 'product'` for `alloc.exactRun`. That field measures IMPORT-reachability while the file's prose claims it answers "does anything RUN it", and nothing in the product calls `Store.saveExactAllocationRun` at all. So the exact path is **unswept and also UNREACHED**, which moves it DOWN the queue, not up. **A ranking is only as good as the field it reads, and the field that decides queue position is the one most worth checking against its own definition.** The retention gap there is still real and still open; it is simply latent. **(b) TRACED and CLOSED at D-185, and the tracing found something none of the three sentences was.** All three were checked individually: `'No direct provider observation runs recorded.'` reads `openai_cost_observation_runs`, `'No OpenAI billing export has been imported.'` reads the import tables, and **no `DELETE` anywhere in the store touches either** -- both retention-sound, left alone. The third, `'No fully paginated OpenAI Costs snapshot is available...'`, is sound too, but it GUARDS a report that was not: capture coverage buckets local rows over the PROVIDER's period, so a prune inside that period turned `Declared route 10 live proxy request(s), $180.00` into **`0 request(s), $0.00`** with the blocker list byte-for-byte identical. **Tracing per sentence did not confirm the sentences; it found the thing behind one of them -- so trace the sentence and then trace what the sentence guards.** And the surface matters: this report exists to tell an operator whether minting an Admin credential is worth it, which makes it the emptiness-with-an-errand class at its most expensive. **Rank instances of this class by what the advice COSTS to follow.** The remainder this left -- `printReadiness`'s own `ReconciliationReadiness.coverage` -- was traced immediately afterwards and turned out sharper than what it followed (D-186): that coverage is null on an empty result set, two docblocks documented the null as "no OpenAI spend exists at all", and both consumers gate the credential warning on it -- so a prune SILENCES the warning rather than moving a figure. **Nothing on this command is now untraced.** **(c) The GUI allocation screen renders a COUNT of runs and not their totals** (`AllocationRunRecord.result` is `Record<string, unknown>`), so the flattered percentage reaches no browser today. That makes it latent, not live -- and it is the reason D-183 put the coverage in the PERSISTED result: the day that screen renders a run's totals, the limit is already in the record it renders from. **The method to keep: enumerate the sentences a surface can print, then rank by whether a deletion can make each one false AND whether anything reads it.** Both halves matter -- (c) is false-able and unread, (a) is read and unswept. **(d) The enumeration itself was wrong until D-187, and fixing it produced the next defect.** This item had been sweeping RETENTION; the store contains **five** `DELETE FROM` statements and only three of them are retention. Of the other two, `project_aliases` is sound and `active_provider_scope_routes` -- `fiscus billing scope clear` -- was the twenty-sixth instance, and it loses money to an UNCOMPARABLE row rather than to a deleted one (SQL `NOT (x = NULL)` is NULL). **Sweep the OPERATION, not the feature that happens to call it.** The generalisation left open and NOT swept: `= ?` against a nullable column is the general shape of that defect, and only the negated-predicate form (`NOT (`, exactly two occurrences, both in the one query) was searched exhaustively. That is the next enumeration if this thread is picked up again. **(e) THE SWEEP IS NOW COMPLETE OVER ITS THREE ENUMERATIONS, and the last two passes found nothing, which is the result worth recording.** Stated with its corpus, per D-166: (i) all **five** `DELETE FROM` statements in `src/` -- `requests` and `proposals` (retention), `proposals` wholesale (`clearProposals`), `project_aliases`, and `active_provider_scope_routes`; the first three were swept D-170 through D-186, and the last two at D-187, which found the defect in the fifth. (ii) The CLI's absence sentences, traced per command at D-183, D-185, D-186 and D-187 -- **nothing on `fiscus billing` is untraced**. (iii) The browser's empty states, enumerated across all **seven** files in `src/dashboard/web/app/views/` and traced individually. Every one is sound or already mitigated: `spend.ts`'s `Nothing recorded in this window.` sits under the D-175 truncation banner rendered from `Overview.retention`; `value.ts`'s `Nothing has tripped the drift watch over N pieces of work` already appends either *'which is not the same as knowing the rate held steady'* or *'too few for this check to notice a change at all yet'*, because `src/value/drift.ts` reports a non-alarm as `unknown` and never as `refuted`; `data.ts`, `allocation.ts` and `evidence.ts`'s remaining sentences read observation, import and rule tables that **no `DELETE` in the store touches**. **A sweep that finds nothing is only worth its enumeration, so the enumeration is written here rather than the verdict.** What is NOT swept and is the honest next thread: `= ?` against a nullable column is the general shape of D-187, and only the negated-predicate form was searched exhaustively.
## Known blockers and standing constraints

- Publishing to a registry, deploying, tagging a release, merging to `main`, and any paid or public commitment are **owner-reserved**. A verification-only PR is never merged merely because it exists to trigger CI.
- Live Postgres execution and trust-anchor governance remain external for team rollups.
- `.codex/` and `.agent-worktrees/` are gitignored on purpose. Never publish either; neither is a release input.
- No credentials are retained; any encountered are `[REDACTED]`.
- `scripts/generate-plugin-contract.mjs` is untracked work in progress left by an earlier executor and has deliberately not been touched.
- **A program record is a gated artifact, not prose.** `test/program-evidence-contract.test.ts` requires every 11-digit CI run identifier in `docs/program/**` and `docs/RELEASE-GATE.md` to state that run's outcome within 200 characters. A list of run ids with one outcome word at the front turns CI red — it did, at `a0c9bc5`, on four jobs. Edit these files and then run the suite; a docs-only change is still a change the gate reads.
- **The owner disabled Claude commit/PR attribution** in `.claude/settings.json` on `main` at `1572f54`, and this branch did not carry that file, so commits made here before `99e4384` have a `Co-Authored-By` trailer the owner's configuration asks not to be added. The file is now cherry-picked onto the branch and later commits omit the trailer. The earlier ones are NOT rewritten: this branch is pushed and force-pushing it is forbidden, so the honest record is a note here rather than a silently corrected history.
- Five delegated lanes were cut off mid-packet by provider rate limits. Four have since been finished by the integrator and committed — WP-D07, WP-F05 and WP-R01 in full, each with its counterexample re-measured and its RED re-verified in this tree rather than taken on the lane's report. **WP-I01 remains open and its held output is two dashboard claim-inspector test files with NO implementation**; they are outside the repository and are not work that landed. WP-E02 produced nothing at all and is still `NOT_STARTED`.
