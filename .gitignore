node_modules/
*.log
*.db
*.db-journal
*.db-wal
*.db-shm
.segreant/
.segreant-home/
.segreant-build.lock/
.segreant-build-*/
.segreant-ws-*/
dist/
.DS_Store
.env
.env.*
!.env.example
coverage/
*.local

# Local agent scratch / session working notes (kept locally, not tracked)
_workstate.md
findings.md
progress.md
task_plan.md
_*.ts

# Internal planning pack (business/diligence docs, kept local until the sale package)
docs/planning/

# Local Codex orchestration control plane. Promote selected records deliberately;
# do not publish task history or internal decision notes by accident.
.codex/

# Local agent-lane worktree copies. Orchestration scratch, never a release input.
.agent-worktrees/

# Local SBOM output (`npm run sbom > segreant-runtime.cdx.json`); CI uploads its own copy as an artifact.
*.cdx.json
